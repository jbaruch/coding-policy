name: Fleet Policy Review (Codex, central)

# Central cron poller: reviews every open same-repo PR across the reviewer App's
# installation against the jbaruch/coding-policy rules, using ONE ChatGPT-
# subscription credential held only in this repo. Replaces the per-repo
# review-codex.yml fleet (blast radius for CODEX_AUTH_JSON drops from 22 to 1).
#
# Secrets required in THIS repo only:
#   CODEX_AUTH_JSON       — the ChatGPT-subscription token (see review-codex.yml header)
#   FLEET_APP_ID          — the coding-policy-fleet-reviewer GitHub App's App ID
#   FLEET_APP_PRIVATE_KEY — that App's private key (PEM)
#   TESSL_TOKEN           — to `tessl install jbaruch/coding-policy` in CI
#
# The App (Pull requests: RW, Contents: RO, Metadata: RO) must be installed on
# every reviewed repo. It posts reviews as `<app-slug>[bot]`; a pass posts as
# APPROVE and a violation as REQUEST_CHANGES (post-review.sh). The App identity
# can APPROVE (unlike github-actions[bot], which gets HTTP 422 and falls back to
# COMMENT), so a clean re-review supersedes an earlier REQUEST_CHANGES — no
# stale-review dismissal needed in the reviewed repo.
#
# Token isolation: fleet-review-one.sh scrubs the App token from the workdir and
# the environment before the sandbox-bypassed Codex step runs; the Codex auth.json
# is guarded by assert-no-secret-leak.sh. See that script's header.

on:
  schedule:
    # Every 15 min: steady-state polls are near-instant no-ops (the stateless
    # dedup reviews only new/updated heads), and a 15-min interval comfortably
    # exceeds a typical full-batch run, so runs do not pile up under the
    # cancel-in-progress:false concurrency guard below.
    - cron: "*/15 * * * *"
  # PR-time path: a consumer's thin review-trigger.yml calls this with the PR
  # coords for an immediate single-PR review; the cron above is the backstop.
  workflow_dispatch:
    inputs:
      repo:
        description: "Consumer repo name (under jbaruch/) to review a single PR of"
        required: false
      pr:
        description: "PR number to review"
        required: false
      base:
        description: "Base branch of the PR"
        required: false

permissions:
  contents: read

concurrency:
  # Single-PR dispatches get a per-PR group so they run immediately instead of
  # queuing behind a running cron poll; a new push to that PR cancels its prior
  # review. The cron poll keeps its own group and never cancels in-progress.
  group: fleet-review-${{ github.event.inputs.repo || 'poll' }}-${{ github.event.inputs.pr || 'all' }}
  cancel-in-progress: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.pr != '' }}

jobs:
  review:
    # Dormant until activated: set repo variable FLEET_REVIEWER_ENABLED=true once
    # the GitHub App is installed and FLEET_APP_ID / FLEET_APP_PRIVATE_KEY /
    # CODEX_AUTH_JSON / TESSL_TOKEN secrets are set. Until then scheduled runs
    # are skipped (neutral, no failure noise) and workflow_dispatch is a no-op.
    if: vars.FLEET_REVIEWER_ENABLED == 'true'
    runs-on: ubuntu-latest
    # Backstop so a stuck run cannot hold the concurrency slot and block the queue.
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v4
        with:
          node-version: "24"

      - uses: tesslio/setup-tessl@v2
        with:
          token: ${{ secrets.TESSL_TOKEN }}

      # renovate: datasource=npm depName=@openai/codex
      - name: Install Codex CLI
        run: npm install -g @openai/codex@0.142.5

      - name: Mint the reviewer App installation token
        id: apptoken
        uses: actions/create-github-app-token@v3
        with:
          app-id: ${{ secrets.FLEET_APP_ID }}
          private-key: ${{ secrets.FLEET_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}

      - name: Write auth.json from the secret (runtime-only, never persisted)
        env:
          CODEX_AUTH_JSON: ${{ secrets.CODEX_AUTH_JSON }}
        run: |
          set -euo pipefail
          if [ -z "${CODEX_AUTH_JSON:-}" ]; then
            echo "error: CODEX_AUTH_JSON secret is empty — re-seed it (see review-codex.yml header)" >&2
            exit 1
          fi
          export CODEX_HOME="$HOME/.codex"
          mkdir -p "$CODEX_HOME"; chmod 700 "$CODEX_HOME"
          printf '%s' "$CODEX_AUTH_JSON" > "$CODEX_HOME/auth.json"
          chmod 600 "$CODEX_HOME/auth.json"

      - name: Redact the credential from CI logs
        run: bash .github/codex-review/mask-secrets.sh "$HOME/.codex/auth.json"

      - name: Build the review worklist
        env:
          GH_TOKEN: ${{ steps.apptoken.outputs.token }}
          REVIEWER_LOGIN: ${{ steps.apptoken.outputs.app-slug }}[bot]
          DISPATCH_REPO: ${{ github.event.inputs.repo }}
          DISPATCH_PR: ${{ github.event.inputs.pr }}
          DISPATCH_BASE: ${{ github.event.inputs.base }}
        run: |
          set -euo pipefail
          if [ -n "${DISPATCH_REPO:-}" ]; then
            # PR-time single-PR path: review exactly the dispatched PR. No dedup —
            # the trigger fires on each push, and fleet-review-one reviews the live head.
            # Validate inputs up front so a bad dispatch fails here, not deep in a git fetch.
            if ! [[ "${DISPATCH_REPO}" =~ ^[A-Za-z0-9._-]+$ ]]; then
              echo "error: dispatch input 'repo' must be a bare repo name under jbaruch/ (no owner/slash), got '${DISPATCH_REPO}'" >&2
              exit 1
            fi
            if ! [[ "${DISPATCH_PR:-}" =~ ^[0-9]+$ ]]; then
              echo "error: dispatch input 'pr' must be a number, got '${DISPATCH_PR:-}'" >&2
              exit 1
            fi
            if [ -z "${DISPATCH_BASE:-}" ]; then
              echo "error: dispatch input 'base' is required when 'repo' is set" >&2
              exit 1
            fi
            jq -nc --arg r "$DISPATCH_REPO" --argjson n "$DISPATCH_PR" --arg b "$DISPATCH_BASE" \
              '[{owner:"jbaruch", repo:$r, number:$n, base_ref:$b, head_sha:"(dispatch)"}]' > worklist.json
          else
            # Cron backstop: poll all marked repos for PRs needing review.
            bash .github/codex-review/fleet-poll.sh > worklist.json
          fi

      - name: Review each PR that needs it
        env:
          GH_TOKEN: ${{ steps.apptoken.outputs.token }}
          CENTRAL_DIR: ${{ github.workspace }}
        run: |
          set -uo pipefail
          # Explicitly validate the worklist shape: a malformed worklist.json must
          # abort loudly, never pass silently with zero (or null-field) reviews
          # (rules/error-handling.md aggregate-reporting carve-out — the setup step
          # carries its own check). Require a top-level array whose every item has
          # all fields fleet-review-one.sh consumes.
          if ! jq -e 'type=="array" and all(.[]; has("owner") and has("repo") and has("number") and has("base_ref") and has("head_sha"))' worklist.json >/dev/null 2>&1; then
            echo "error: worklist.json is not an array of well-formed work items {owner,repo,number,base_ref,head_sha} — inspect the poll step output" >&2
            exit 1
          fi
          items=$(jq -c '.[]' worklist.json)
          count=$(jq 'length' worklist.json)  # worklist.json validated as an array above
          echo "PRs needing review: ${count}"
          fail=0
          while IFS= read -r item; do
            [ -n "$item" ] || continue
            o=$(jq -r .owner     <<<"$item")
            r=$(jq -r .repo      <<<"$item")
            n=$(jq -r .number    <<<"$item")
            b=$(jq -r .base_ref  <<<"$item")
            echo "::group::review ${o}/${r}#${n} (head $(jq -r .head_sha <<<"$item"))"
            if bash .github/codex-review/fleet-review-one.sh "$o" "$r" "$n" "$b"; then
              echo "reviewed ${o}/${r}#${n}"
            else
              echo "::warning::fleet review failed for ${o}/${r}#${n}"
              fail=1
            fi
            echo "::endgroup::"
          done <<< "$items"
          exit "$fail"

      - name: Drop the Codex credential
        if: always()
        run: rm -f "$HOME/.codex/auth.json"
