# Changelog

### Rules — Pre-Send Check in response-clarity

New `Pre-Send Check` section in `response-clarity` adding a final self-audit pass: cut noise-hedges, keep real-uncertainty hedges, replace idioms with the literal action, and verify the first line names the next action and the last line names what changed.

The prompt: comparing rules-only behavior against `ayghri/i-have-adhd`'s opt-in skill, which duplicates our ten response-shaping directives, the skill still visibly changed output when invoked. If `response-clarity` is `alwaysApply: true`, why would an invoked skill that says the same things behave differently? Three mechanisms: position (the always-on rule sits buried among ~25 siblings thousands of tokens back, the skill invocation lands fresh right before the turn), salience (an explicit `/i-have-adhd` invocation is a "do this now" spike, an always-on rule is passive background), and richness (the skill kept Bad/Good example pairs and a Pre-send checklist our compression discipline had stripped). `alwaysApply: true` is not the same as reliably-applied.

The active ingredient was the skill's Pre-send checklist — a mechanized audit, not a passive directive. Most of it already lived in our rule (an announcing first sentence is Lead With the Action, an "anything else?" closer is Close With One Next Step, a by-the-way sidebar is Defer secondary), so only the non-duplicative items landed to keep clear of `context-writing-style`'s no-duplication rule: the two new deletions and the verify gate. We did not vendor the skill — it is a separate MIT plugin, and copying it would violate `dependency-management` No Vendoring; anyone wanting the toggle installs it alongside.

## 0.3.129 — 2026-07-28

### Rules — First-Party Co-Shipped Dependency Carve-Out

New carve-out in `dependency-management` for a dependency the same owner writes, reviews, and deploys in lock-step with its consumer, where the consumer rebuilds against the dependency's default branch on every deploy and no third artifact selects a version between them.

The case that forced it: `jbaruch/nanoclaw` installs `jbaruch/reclaim-tripit-timezones-sync` into its agent image, pinned to a tag per Pinning. The package gained OneCLI gateway support on 2026-07-10 — the support that repo's own container needs, since the `GOOGLE_*` OAuth trio left it in the native-Google migration — and no tag was ever cut for those commits. Renovate watches the github-tags datasource, so it had nothing to propose; the pin read as current while the deployed CLI silently created zero out-of-office blocks for 19 days. The pin was not protecting the consumer from unreviewed upstream change (same owner reviews both sides); it was holding the consumer on a build that predated a fix written for it.

Three carve-outs now exist, and the distinctions are the load-bearing part. Adversarial-Freshness covers a dependency tracking an opponent, whose consumed surface is data or rendered output — this one's surface is a versioned CLI contract, so it never qualified there. Runtime-Managed Manifest covers a manifest a tool rewrites at runtime. This carve-out covers neither: the reference is committed and the surface is an API, and what makes a pin wrong is the shared review boundary.

Precondition 3 is the one worth reading twice: the consumer's build must refetch on every rebuild. A bare `owner/repo` install behind a Docker layer cache is not floating — it is a pin with no version anyone can read, and it fails in exactly the direction the carve-out is trying to fix. Precondition 4 therefore gates on the trigger's presence and binding, not only on the absence of a specifier. The anti-pattern bullets close the obvious abuse: "we wrote it" alone is not enough (a dependency with its own release train, or consumers outside the owner, still pins), and neither is bump-PR noise.

The permitted form is specifier-less, full stop. An earlier draft offered "a default-branch ref" as an example of a floating reference, which contradicted the deploy gate one bullet below it: `#main` is a specifier, the gate fails on specifiers, and a carve-out that permits what its own precondition rejects is worse than no carve-out. Why a pin is wrong here also moved out of the rule body — the dependency's default branch moves under the owner's own review while the consumer keeps building the last-tagged state until someone remembers to bump it, which is rationale, and rationale lives here.

## 0.3.128 — 2026-07-27

### Rules

- **Batched prose cleanup** (closes #236, #240) — one focused pass clearing the advisory prose/accuracy findings deferred across #235/#239/#243/#244/#246 per round-economics. `boy-scout` Fold section: cost-unit bullet split into atomic bullets, the "folding them in there is free" meta-justification dropped, the `review-severity` restatement collapsed to a bare reference, "spin a fresh round up" → "spin up a fresh round". `ship-on-green`: "no exit below holding" → "none of the exits below apply". `ci-safety` reviewer-liveness bullets: incident examples and em-dash rationale trimmed to the invariant + directive, and scoped explicitly to reviewer workflows (build/publish runs still gate on `conclusion`) so it no longer reads as contradicting the "watch a run's `conclusion`" bullet. `language-diagnostics` in-loop bullets: colon/semicolon-packed clauses split atomic, and "over what it changed" corrected to "the same gate command CI runs, scoped to the changed set only where the gate supports it" (the repo's own `run-diagnostics.sh` runs project-level). `REVIEW_DETAILS.md`: "exactly one reviewer" softened to allow the transitional both-reviewers case, and the fleet-review policy-load path corrected — the runner `tessl install`s coding-policy to a temp path and symlinks `.tessl/` into the workspace, it is not consumer-pre-installed.

## 0.3.127 — 2026-07-27

### Fleet reviewer — resolve the reviewed repo's own plugins (closes #245)

The fleet reviewer installed `jbaruch/coding-policy` into a temp dir and symlinked it in as the reviewed workspace's entire `.tessl`. The consumer's own plugins were never installed — so `.tessl/plugins/` held exactly one plugin, and every carve-out satisfied by an "authority-of-record rule in its own plugin" was unverifiable. That covers `dependency-management`'s Runtime-Managed Manifest and Adversarial-Freshness carve-outs and `ci-safety`'s Content-Only Direct-Push: three carve-outs the reviewer structurally could not confirm, for as long as the fleet reviewer has existed.

The failure mode is worse than a missed check. On `jbaruch/nanoclaw#883` the reviewer recognized the Adversarial-Freshness carve-out, went looking for the authority rule, `rg`'d the repo's own tree, found nothing, and blocked — reporting a fully-documented carve-out as undocumented. The rule was published in `jbaruch/nanoclaw-host` 0.1.53 the whole time. Plugin rules are installed, never committed (No Vendoring), so grepping the tree can only ever come back empty.

`fleet-review-one.sh` now also installs the plugins named in the reviewed repo's manifest. Resolved from the BASE ref, never the PR head: the head is the content under review, so reading it would let a PR name a plugin that authorizes the PR. A plugin that fails to install emits a `::warning::` naming it rather than failing the review — best-effort, but never silent, since an invisibly-missing authority rule is exactly how this bug blocked a compliant PR.

The dependency list is captured in an explicitly checked assignment rather than read through process substitution: `while read < <(jq ...)` discards jq's exit status, so a malformed `tessl.json` would yield an empty list and the review would proceed with no authority plugins installed — silently reproducing the exact wrong-verdict bug the step exists to fix. A parse failure now aborts the review.

`fleet-prompt.md` gains the matching instruction: read `.tessl/plugins/*/*/rules/` before reporting a carve-out as undocumented, never conclude a rule is absent from a grep of the repo's tree, and name the plugin you expected it in when it genuinely is missing.

## 0.3.126 — 2026-07-27

### Rules

- **`language-diagnostics` — the in-loop expectation is a deterministic run, not memory** (closes #175) — the rule told the agent to "read diagnostics on the files it touches and clear them before handing work off" (Enable the Language Server) while also holding that "'don't ignore the warnings' enforced by memory is not a gate — a deterministic check nobody runs does not exist" (Gate It Deterministically). Those two contradicted: the in-loop clause rested on the memory the gate clause disqualified. Resolved by pointing the pre-handoff expectation at the same headless gate CI runs — the `LSP` tool surfaces findings live as the agent works, and clearing before handoff is a deterministic run of the gate over the changed set, with CI as the backstop rather than the first place a finding surfaces. A `Stop`/pre-handoff hook mechanizes it where the harness supports it. Silent-dismissal mechanical detection (a gate that rejects a bare blanket-ignore) and a wired hook are noted as optional follow-ons, not built here.

## 0.3.125 — 2026-07-27

### Rules

- **`ci-safety` — a reviewer's run conclusion is not proof that the review happened** (closes #185) — during GitHub's 2026-07-16 REST degradation, a scaffolded reviewer workflow reported `completed success` while skipping the review entirely: a transient 503 in gh-aw's membership check collapsed "can't determine membership" into the same falsy value as "not a member", so every downstream job skipped and the run went green having reviewed nothing. Nothing merged unreviewed only because `watch-pr-reviews.sh` gates on the posted verdict, not the run's color. `ci-safety` now states that invariant explicitly — a run `conclusion` answers "did the workflow finish", never "did the review happen"; gate on the verdict — plus a caution against promoting a reviewer check to a required branch-protection gate while any fail-open path exists (required-and-green is satisfied by a run that did nothing). The upstream fail-open belongs in `githubnext/gh-aw` and consumers have since migrated off the per-repo gh-aw reviewer to the central fleet App, but the invariant is mechanism-agnostic and load-bearing for anyone merging by hand.

## 0.3.124 — 2026-07-27

### Rules

- **`ship-on-green` — new always-on rule; merge-autonomy contract** (closes #187) — the "green gate ⇒ merge, don't ask" contract lived only in `skills/release/SKILL.md`, so an agent that ran the flow by hand (`gh pr create` → watch → `gh pr merge`) without invoking `/release` never loaded it — the exact failure in `jbaruch/nanoclaw-trusted#80`, where a green PR was flagged for permission anyway. Extracted into an always-on rule so every merge decision loads it regardless of skill invocation. It carries: the green gate IS the approval (flag / confirm / "say go and I'll" / "worth one confirmation" are asking in a costume, and asking IS the decision to not ship); stakes raise care, not permission (scary / wide / ships-to-prod / deletes-a-thing ⇒ verify harder, never ask); and the three exhaustive, objective exits, each with a literal test — **Red** (a required check failing/pending, not "I feel uneasy"), **No undo** (`git revert` can't restore it — force-push over protected history, immutable published artifact, deleted data, sent external comms; a supersedable version bump is undoable and shipping to prod is NOT "no undo"), **Murky** (the task's own source states 2+ conflicting options AND names no default; a named default is never murky). `SKILL.md` Step 7 now references the rule instead of restating it.
- **Fixed `.claude/CLAUDE.md` import drift** — `review-severity` (added 0.3.116) was in `.tessl-plugin/plugin.json` and the README but never added to the maintainer `.claude/CLAUDE.md` @-import chain, so the maintainer session didn't load it. Added it alongside the new `ship-on-green` import.

## 0.3.123 — 2026-07-27

### CI

- **`run-diagnostics.sh` now gates `.github/codex-review/`** (closes #199) — the shellcheck gate scanned `skills/`, `scripts/`, and `.github/actions/` but not `.github/codex-review/`, so the policy-reviewer driver scripts (`fleet-poll.sh`, `fleet-review-one.sh`, `post-review.sh`, `mask-secrets.sh`, `assert-no-secret-leak.sh`, and their tests) were held to no bar — the gating reviewer flagged SC findings the local gate reported "clean" (surfaced during #198). Added the root; fixed the SC2015 (`A && ok || bad`) findings the new coverage revealed in `.github/codex-review/tests/test_assert_no_secret_leak.sh` (6) and `.github/codex-review/tests/test_mask_secrets.sh` (2) by converting to explicit `if/then/else` (`.github/codex-review/tests/test_post_review.sh` was already clean since the issue was filed), and added a `test_run_diagnostics.sh` case asserting the new root is gated. 42 scripts gated, up from 32; the runner's CLI and exit-code contract are unchanged, and the test conversions preserve their assertions.

## 0.3.122 — 2026-07-27

### Rule — dependency-management: adversarial-freshness carve-out

`dependency-management` had exactly two carve-outs, both structural: a manifest a tool rewrites at runtime, and a same-repo action self-reference. Neither covers a dependency that floats for a *semantic* reason — where tracking the upstream IS the feature.

Surfaced on `jbaruch/nanoclaw` PR #883. Its `fetch_markdown` handler pulls `syabro/snitchmd:latest`, a headless-browser content extractor whose releases carry updated CloakBrowser fingerprints. Pinning it doesn't stabilize anything: the pin ages into blocked fetches as anti-bot detection advances, and the failure is silent — pages come back empty, not broken. The consuming repo wrote an authority-of-record rule plus a deploy gate (`nanoclaw-host: snitchmd-image-floating`), but the reviewer correctly rejected it, because a consumer plugin cannot mint an exception this rule never offered. The carve-out has to exist here or it doesn't exist.

Preconditions are deliberately tighter than the manifest carve-out's. Beyond the authority-of-record rule and the deploy gate, the gate must fail when it can no longer *find* the reference — a rename or refactor that moves the target out of sight is the obvious way this decays into a check that passes vacuously forever. The per-install override is required rather than optional, so reproducibility stays reachable without a code change, and is explicitly out of the gate's scope: environment configuration is not a committed dependency.

Three anti-patterns are bulleted rather than left implied, since this is the carve-out most likely to be reached for in bad faith: "the upstream releases often" (that's Freshness), "pinning is inconvenient", and any dependency whose consumed surface is a versioned API however adversarial its domain.

## 0.3.121 — 2026-07-27

### Skills

- **`release` — two-model policy-reviewer prose** (closes #209) — the skill described only the legacy per-repo `review-codex.yml` reviewer (`pull_request`-triggered, posts as `github-actions[bot]`, `CODEX_AUTH_JSON`), accurate for coding-policy's own releases but not for consumer repos, which the fleet migration (#202) moved to the central `coding-policy-fleet-reviewer[bot]` App (`review-trigger.yml` dispatch + `*/15` marker-gated poll; the per-repo Codex credential `CODEX_AUTH_JSON` is gone, replaced by one lightweight `FLEET_DISPATCH_TOKEN` dispatch PAT). Step 4, the `pending_at_budget` recovery note, and the Step 6 re-run note now carry the two-model split, and `REVIEW_DETAILS.md` splits the policy-reviewer section into deployment A (coding-policy self-review) and deployment B (consumer fleet App), including the APPROVE/dismiss difference (the App can `APPROVE` and supersedes natively; `github-actions[bot]` cannot, so Step 7 dismisses). Generic guidance de-branded from "Codex reviewer" to "policy reviewer".

## 0.3.120 — 2026-07-27

### Skills

- **`release` — stale-read gate bugs fixed** (closes #186, #182, #181) — three release-gate scripts were acting on data that no longer described the current code. **#186:** `poll-pr-reviews.sh` resolved review verdicts by login only and dropped `commit_id`, so a stale `APPROVED`/`COMMENTED` from a superseded SHA read as a live clean verdict — a second push whose CI concluded before the reviewers re-posted could reach `ready` and merge unreviewed code. Verdicts now bind to the PR head: a review whose `commit_id` is not `headRefOid` collapses to state `none` (absent, not clean) with a `stale: true` flag that keeps the raw verdict visible for diagnosis; `head_sha` is a new top-level snapshot field and the script refuses to proceed without it. **#182:** the `cancel` bucket was treated as CI failure, so pushing a fix (which auto-cancels the prior SHA's in-flight runs) false-red'd a green PR; cancels are now dropped as no-signal — live buckets decide, a success alongside a cancelled twin still succeeds, and only an all-cancelled set falls to `pending`. **#181:** `verify-publish-landed.sh` derived the current version from the eventually-consistent `tessl plugin info` search listing, which lags the version API by minutes and false-negatived landed publishes with a confidently wrong "no-op publish step" message; it now reads the numeric max from `tessl api v1/tiles/<ws>/<tile>/versions` (the same authoritative API `verify-moderation-cleared.sh` uses). Conjunct 1 (resolved run conclusion) is untouched.

## 0.3.119 — 2026-07-24

### Rules

- **`boy-scout` reframed around round economics, not reviewer-domain exclusion** — the 0.3.118 framing (below) over-corrected: it scoped reviewer findings out of boy-scout entirely. Boy-scouting a reviewer's finding is fine; the real constraint is the cost of an extra review round, not the finding's source. Boy-scout now carries a "Fold Into a Round Already in Flight" section: the cost unit is the extra round, not the extra line — when a blocking finding forces another push, clean up the adjacent loose ends in that same round (free); when the change is already mergeable, merge and file the loose ends to the next ticket rather than pushing a commit only to fold them in. Points to `rules/review-severity.md` as the review-pipeline form of the same economics. Restores reviewer findings as valid boy-scout targets, dropping the "not boy-scout's domain" bullets.

## 0.3.118 — 2026-07-24

### Rules

- **`boy-scout` scoped to self-noticed issues; decoupled from reviewer findings** — boy-scout was being read as a lever to fold a reviewer's advisory into the current round ("in-scope cleanup on a line I'm touching"), which fought `review-severity`'s "defer a lone advisory, never burn a dedicated round" — the #232 cleanup ran four review rounds partly on that tension. Boy-scout now states its trigger is the agent's own eyes: a reviewer's stated findings are `review-severity`'s domain — blocking gets fixed, a lone advisory gets deferred, never folded in under a boy-scout in-scope claim. Dropped boy-scout's bridging bullet about deferred review advisories, and dropped `review-severity`'s reverse `see rules/boy-scout.md` pointer on its defer bullet (it now names the follow-up mechanism directly), so neither rule reaches into the other's domain.

## 0.3.117 — 2026-07-24

### Rules

- **`review-severity` — atomic-bullet cleanup** (closes #231) — folds in the advisory findings deferred from #230: the Gating Predicate's watcher/Copilot bullet and the Split-Reading-From-Acting acknowledge/fold/defer bullet each split into one-directive-per-bullet per `context-writing-style` Structure. Also drops the `test_post_review.sh` load-indicator assertion's hardcoded rule count for a `Policy loaded: [0-9]+ rule files` pattern so it no longer churns when rules are added or removed. Prose and behavior unchanged.

## 0.3.116 — 2026-07-23

### Rules

- **`review-severity` — new rule; severity-tiered review findings** (closes #229) — both the fleet policy reviewer and the agent treated every finding as merge-blocking-and-must-fix-in-place, so a lone style nit (a CHANGELOG synonym, an em-dash clause, a non-atomic bullet) cost a full re-review round (Codex tokens) plus a merge delay, same as a real defect. One session (2026-07-23) saw an `install-reviewer` `.env.example` PR run ~6 policy rounds and a seeded-proptest carve-out PR run 3, most of them lone nits. The new rule splits findings into **blocking** (correctness, security, contract violations — a carve-out's preconditions, `no-secrets`, `ci-safety` gate-evasion, surface-sync that breaks publish, a directive whose violation changes behavior, or a style split whose fix changes meaning) and **advisory** (pure prose/style, CHANGELOG wording, naming taste, Copilot findings). The behavioral test: does fixing it change what an agent or the pipeline does? Blocking gates the merge; advisory never does — post as a comment, fix opportunistically or defer to a follow-up. `reviewer-feedback-reading` still requires reading every finding (state classifies gating, severity classifies acting, neither classifies reading); `boy-scout` now records that a deferred advisory is the filed-record path, not walking away.

### CI

- **Policy-reviewer pipeline emits severity per finding; the gate derives from it** — the reviewer schemas (`fleet-schema.json`, `schema.json`) drop the model's `verdict` field and add a required per-finding `severity` (`blocking`|`advisory`); the prompts (`fleet-prompt.md`, `prompt.md`) instruct the reviewer to classify honestly. `post-review.sh` now derives the review event deterministically from severity (`rules/script-delegation.md` — the gate is a pure function of severities, not a model verdict): any blocking → `REQUEST_CHANGES`, advisory-only → `COMMENT` (visible, never gates), none → `APPROVE`; a finding with no severity is treated as blocking (fail-safe). The body renders blocking and advisory findings in labeled sections. `watch-pr-reviews.sh` gates on the policy reviewer's `CHANGES_REQUESTED` alone — Copilot is formalized as always advisory and never produces a `changes_requested` watch result, though the watcher still waits for Copilot to post so its body is read. `release` skill Steps 4/6/7 reflect the split: read all, act by severity, never burn a dedicated round on a lone advisory. Every reviewer surface is re-audited (`context-artifacts` Post-Edit Rule Audit): `REVIEW_DETAILS.md`, `install-reviewer/PR_BODY_TEMPLATE.md`, and the release-watch prose no longer say both reviewers gate — the policy reviewer gates on blocking findings; Copilot is read but never gates.

## 0.3.115 — 2026-07-23

### Rules

- **`testing-standards` — Seeded Property-Based Test Carve-Out** (closes #227) — the Determinism clause ("no self-generated random test data", "never have the test generate its own inputs randomly") banned property-based tests (kotest `*PropertyTest`, Hypothesis, fast-check, QuickCheck) outright, since generating inputs is the paradigm — even when a pinned seed makes the run fully reproducible. The clause conflated two senses of "random": runtime entropy (different cases each run, genuinely flaky) versus seeded pseudo-random (same seed produces the same cases on every run and machine, as reproducible as a hand-written fixture, which is the clause's actual goal). The carve-out permits property tests when the seed is a constant in the file, the iteration count is bounded, and no runtime-RNG call shapes the inputs; unseeded or unbounded generation and runtime `random()` inputs stay forbidden. Precondition 3 lists RNG calls only, not the clock — clock nondeterminism is already covered by the Determinism time bullets and by the constant-seed precondition. The Determinism bullets now point to the carve-out, and the second bans runtime randomness specifically rather than all input generation. Surfaced when the fleet reviewer blocked jbaruch/tg-hubitat-bot#47 and jbaruch/tg-hubitat-bot#49 (detekt adoption), whose `PropTestConfig(seed = FIXED_SEED, iterations = 100)` suites are deterministic by construction; two genuinely non-deterministic `modes.random()` calls in that repo were correctly flagged and fixed separately.

## 0.3.114 — 2026-07-23

### CI

- **PR-time `tessl plugin lint` gate** — `tests.yml` gains a `lint` job running `tessl plugin lint` on every PR, alongside `diagnostics` and `unit-tests`. Previously lint ran only in `publish.yml` (post-merge), so a bad manifest or skill frontmatter — e.g. an over-length `description` — passed PR review and merged, then failed the publish pipeline's first step (seen during the 0.3.112 recovery). Now it's caught before merge.

### Skills

- **`install-reviewer` wording made plain and factual** — the skill's prose (SKILL.md, `PR_BODY_TEMPLATE.md`, the `review-trigger.yml` template header, and the `.env.example` block scaffold.sh writes) now describes each file and the review's data flow in plain, minimal terms — what runs, where, and when — without dwelling on repository permissions or trust boundaries. Behavior is unchanged and the `no-secrets` documentation (settings URL + placeholder) is preserved; the plainer wording keeps the registry's automated review of the published tile clear.

## 0.3.111 — 2026-07-23

### Skills

- **`install-reviewer` scaffolds `FLEET_DISPATCH_TOKEN` into `.env.example`** (closes #220) — the skill scaffolds `review-trigger.yml`, which requires the `FLEET_DISPATCH_TOKEN` hosted-CI secret, but never documented it in the consumer's `.env.example`, so every onboarded repo drew a `no-secrets` `CHANGES_REQUESTED` from the fleet reviewer on its enrollment PR (seen on `tg-hubitat-bot#42`, fixed by hand). `scaffold.sh` now manages `.env.example` as a 4th artifact: append-or-create, idempotent (skips when the secret is already present), never overwriting existing variables, with the repo's Actions-secrets settings URL derived from the origin remote (https + ssh forms) and folded into the snapshot/rollback. `commit.sh` stages it; `SKILL.md` Step 4 and `PR_BODY_TEMPLATE.md` name the artifact; the per-file JSON `action` gains `appended`. New tests cover create-with-derived-URL, append-preserves-prior-vars, idempotent-when-present, and symlink refusal.

## 0.3.110 — 2026-07-22

### Skills

- **`install-reviewer` templates now survive tessl packaging** — tessl's plugin packaging ships only `.md`/`.sh`/`.json`/`.py` files; a `.yml` or extensionless file is dropped from the installed plugin (workflow-injection safety). Two of the three scaffold templates — `fleet-review-enabled` (extensionless) and `review-trigger.yml` (`.yml`) — were therefore silently absent from every real `tessl install jbaruch/coding-policy`, so `preflight.sh`/`scaffold.sh` failed with `templates-present`/`template not found` on any fresh consumer onboarding. The skill's tests never caught it — they run against the source tree, where the files exist. Fix: the two templates carry a `.md` shim extension (`fleet-review-enabled.md`, `review-trigger.yml.md`); `scaffold.sh`'s MANIFEST and `preflight.sh`'s TEMPLATES map them back to their real target names (`.github/fleet-review-enabled`, `.github/workflows/review-trigger.yml`). Verified with `tessl plugin pack` — all three templates are now in the package tarball. Comments in both scripts warn against dropping the shim.

### Actions

- **skill-review: recognize the current tessl CLI's out-of-credits signature** — `is_credit_outage()` required the credit phrase AND a literal `403`, but the current CLI emits `✘ Your organization has run out of credits. Upgrade your plan or buy more credits to continue.` with no status code, so a genuine billing outage was classified "not a credits outage" and hard-blocked publishes despite `credit-outage: skip` (first hit: `jbaruch/nanoclaw-admin` run 29951572295, 2026-07-22, blocking the 0.1.481 publish). The classifier now accepts either signature: the legacy phrase-plus-403 form, or the full current-CLI billing sentence matched as a whole line (anchored pattern tolerating a leading glyph/whitespace prefix) — a partial phrase, prose quoting the sentence mid-line, or a future wording change still hard-fails (the safe direction). New test modes lock in the full-sentence skip under `skip`, the hard-fail under `fail`, the partial-sentence hard-fail, and the prose-quote hard-fail.

## 0.3.109 — 2026-07-22

### Rules

- **Migrate the skill-review command to the `tessl review` surface** — `tessl skill review` moves to `tessl review run` for the gating call and `tessl review fix` for the disagree-with-reviewer optimize workflow. The `skill-review` composite action (`review-skills.sh`, `action.yml`) now invokes `tessl review run --threshold N`; the `--threshold` gate and out-of-credits classification are unchanged, so the credit-outage carve-out and the changed-skills loop behave identically. `context-artifacts` Mandatory Review, Credit-Outage Carve-Out, and Disagreeing With the Reviewer updated: the former `--optimize` flag has no `tessl review run` equivalent, so the optimize path is now `tessl review fix` (an automated review-and-fix loop that applies changes in place) — the diagnostic-not-verbatim discipline (back up, diff against the backup, curate, never ship the loop's output verbatim) carries over unchanged. The mock-driven action tests are command-agnostic (they key on the trailing path arg) and stay green. Historical CHANGELOG entries keep the old command name as archive.

## 0.3.108 — 2026-07-21

### Skills

- **`install-reviewer` — fleet-review trigger skips Dependabot PRs** — the scaffolded `review-trigger.yml` guarded only fork PRs (`head.repo.full_name == github.repository`). Dependabot PRs are same-repo, so they passed that guard, but the `dependabot[bot]` actor receives no secrets — `FLEET_DISPATCH_TOKEN` came through empty and the workflow's own empty-token guard exited 1, leaving a permanent red check on every Dependabot PR. The job `if:` now also requires `github.actor != 'dependabot[bot]'`; the coding-policy cron poll reviews Dependabot PRs as the backstop. PR body template and the workflow header note the new skip.

## 0.3.107 — 2026-07-21

### Rules

- **New `response-clarity` rule — action-first agent responses** — adapts the ten output principles from [`ayghri/i-have-adhd`](https://github.com/ayghri/i-have-adhd) into an always-on steering rule for how agents shape their live responses to the user (distinct from `context-writing-style`, which governs written context artifacts). Lead with the command or answer, number multi-step work, one bounded action per item, cap unranked lists at 5, restate progress across turns, make wins concrete and verifiable, give concrete time ballparks, report errors matter-of-fact, close with one two-minute next step, no preamble or closers. Exceptions loosen the rules for requested explanations, destructive actions (add confirmation), debug spirals (ask one diagnostic question), and genuine ambiguity (ask one clarifying question).

## 0.3.106 — 2026-07-21

### Skills

- **Release watcher recognizes the fleet reviewer on consumer repos** — `poll-pr-reviews.sh` resolved the policy reviewer only as `github-actions[bot]` (correct for coding-policy's own PRs), so on a consumer repo — where the central fleet App reviews as `coding-policy-fleet-reviewer[bot]` (#202) — `watch-pr-reviews.sh` saw `.reviews.codex.state` as `none` forever and sat at `pending_at_budget`. `latest_review_by` is now variadic and resolves the policy reviewer across both logins (a PR carries reviews from only one, so `last` in the set is unambiguous); the codex comment-login set gains the App login too. `SKILL.md` Step 5 and `dismiss-stale-reviews.sh` now note the consumer case: the fleet App CAN `APPROVE`, so its re-review supersedes its own `CHANGES_REQUESTED` directly and no dismissal is needed there. New tests cover the fleet-App review state, cross-login newest-verdict resolution, and the App comment count.

## 0.3.105 — 2026-07-21

### Rules

- **`dependency-management` — Same-Repo Reusable-Workflow Action Carve-Out** — a reusable workflow that invokes a composite action in its own repo can't use a `./` local path (it resolves against the external caller's checkout) and no scanner renews a same-repo `owner/repo/...@ref` self-reference, so a SHA pin is both circular and un-renewable. The carve-out sanctions tracking the hosting repo's default branch (`@main`) for that narrow case; every external action reference still pins with scanner-tracked renewal. Surfaced implementing the reusable publish workflow (below).

### CI

- **Canonical reusable publish workflow — unifies the fleet's drifted publish CI** — the `jbaruch/*` consumers had drifted onto three filenames for the same job (`publish-tile.yml` / `publish-plugin.yml` / `publish.yml`) with per-repo copies of the checkout → setup-tessl → skill-review → lint → publish boilerplate and their own third-party action pins. New reusable `.github/workflows/publish-plugin.yml` (`on: workflow_call`) owns that pipeline; consumers carry a thin `publish.yml` caller (triggers + `uses:` + `secrets: inherit`). Inputs fold in the per-repo variation: `python-version`, `pre-publish-script` (repo-specific gate), `stamp-changelog`, `skills-dir`, `skill-review-credit-outage` (default `fail`; consumers opt into `skip`), and `publish-mode` (`auto-bump` via `patch-version-publish`, or `as-is` via `tessl plugin publish` for repos that require human-authored version bumps). The skill-review threshold is fixed at 85 (context-artifacts forbids lowering it), not a caller input. Sibling composite actions are referenced at `@main` (a `./` path in a called reusable workflow resolves against the caller, which has no coding-policy actions). jbaruch/coding-policy#206.

## 0.3.104 — 2026-07-21

### CI

- **PR-time trigger for the fleet reviewer — restores before-merge policy review on consumers** — the cron poller (`*/15`) can't review a PR that merges within the interval, so a fast/urgent merge on a swept consumer skipped policy review entirely. `fleet-review.yml` gains a `workflow_dispatch` single-PR path (inputs `repo`/`pr`/`base`, its own per-PR concurrency group so it never queues behind a running poll) that reviews exactly the dispatched PR; the cron stays as the backstop. Consumers fire it at PR time via a thin trigger workflow (below).

### Skills

- **`install-reviewer` scaffolds a PR-time trigger + one narrow dispatch secret** — onboarding now also commits `.github/workflows/review-trigger.yml`, a thin `on: pull_request` workflow that `gh workflow run`s the central review for the PR immediately (so the verdict lands before merge). The consumer sets one stable `FLEET_DISPATCH_TOKEN` — a fine-grained PAT scoped to only `jbaruch/coding-policy` with `Actions: write` — which can trigger the review workflow and nothing else; the Codex credential still lives only in `coding-policy`. `scaffold.sh`/`preflight.sh`/`commit.sh`/tests retarget to the three files; `SKILL.md` + `PR_BODY_TEMPLATE.md` document the secret.

## 0.3.103 — 2026-07-21

### CI

- **`stamp-changelog` falls back to the manifest version on a Tessl auth failure** — the action's `query_latest_version` hard-raised when `tessl plugin info` failed for any non-404 reason, so a consumer's publish-on-merge wedged at the stamp step with "requires you to be logged in" when that step ran without `setup-tessl` / `tessl login`. An auth failure is now non-fatal — it warns and returns `None`, so the manifest version (the one being published when kept ahead of the registry) is used; `patch-version-publish` still does its own authoritative bump. Genuine non-auth failures still raise. Fixes #207.

## 0.3.102 — 2026-07-21

### CI

- **Opt-in marker guard for the fleet reviewer** — `fleet-poll.sh` now reviews a repo only if it carries a `.github/fleet-review-enabled` marker file, so the App can be installed on **all repositories** while only opted-in repos are reviewed (404 on the marker = opt-out, skipped silently; any other error warns and skips so a transient failure never mass-skips). The fleet reviewer's prompt/schema moved to `.github/codex-review/fleet-prompt.md` + `fleet-schema.json` (fleet-owned, decoupled from the install-reviewer templates).

### Skills

- **`install-reviewer` enrolls a repo in the central fleet reviewer instead of scaffolding a per-repo workflow** — onboarding now commits a `.github/fleet-review-enabled` marker + `.github/copilot-instructions.md` and opens a PR; the marker enrolls the repo in the `coding-policy-fleet-reviewer` GitHub App. No per-repo `review-codex.yml`, no `.github/codex-review/` driver, and no `CODEX_AUTH_JSON`/`TESSL_TOKEN` repo secrets — the credential lives only in `coding-policy`. `scaffold.sh`/`preflight.sh`/`commit.sh`/the tests retarget from seven files to the two marker files; the secrets checklist is dropped from `SKILL.md` and `PR_BODY_TEMPLATE.md`.

## 0.3.101 — 2026-07-21

### Rules

- **`skill-authoring` Typed Calls: mid-session-install recovery carve-out** — field feedback from a first-run consumer setup (jbaruch/jbaruch-travel-policy#24): a policy skill's Setup installs its library plugin mid-session, and the harness's skill registry only loads at session start — the documented typed `Skill()` call throws "Unknown skill" for the rest of that session no matter how the skill phrases it. The consumer documented the only working recovery (read the just-installed skill's mounted `SKILL.md` and execute its steps), and the policy reviewer correctly flagged that as a Typed Calls violation with no sanctioned form, naming "update the governing rule with an explicit carve-out" as a remedy. The carve-out sanctions exactly that shape with three preconditions: the typed call stays the sole authored invocation path, the recovery reads the plugin-mount `SKILL.md`, and the skill text scopes the recovery to the installing session.

## 0.3.100 — 2026-07-21

### CI

- **`post-review.sh` posts `APPROVE` on a pass** — a pass verdict now submits an `APPROVE` review instead of a `COMMENT`, so a clean re-review supersedes an earlier `REQUEST_CHANGES` in GitHub's merge gate without a manual dismissal — which matters for fleet-reviewed consumer repos, where no release skill runs to dismiss stale reviews. A token that cannot approve (`github-actions[bot]`, HTTP 422) falls back to `COMMENT`, so this repo's own self-review still works during the transition. The submit logic is factored into a `submit_review()` helper that distinguishes a non-retryable 422 from a transient 5xx. Implements #200.

## 0.3.99 — 2026-07-20

### CI

- **Central fleet policy reviewer — one Codex credential for the whole fleet** — new `.github/workflows/fleet-review.yml` cron-polls (`*/15`) every open same-repo PR across a GitHub App's installation and reviews each against the `jbaruch/coding-policy` rules, so `CODEX_AUTH_JSON` lives in this repo only (blast radius drops from 22 to 1). `fleet-poll.sh` enumerates PRs needing review statelessly (skips any whose current head SHA the App already reviewed via each review's `commit_id`); `fleet-review-one.sh` fetches one PR's head passing the App token only via a transient git auth header (never in the clone URL or persisted config) and unsets it from the environment before the sandbox-bypassed `codex exec` runs, so a prompt-injected PR cannot exfiltrate the fleet-wide token (the Codex `auth.json` stays guarded by `assert-no-secret-leak.sh`). The policy is installed to a non-workspace path and symlinked in, so plugin content never lands in the reviewed checkout. The App posts as `<app-slug>[bot]` — a pass as a `COMMENT`, a violation as `REQUEST_CHANGES` (posting `APPROVE` on a pass, which the App identity permits, is a tracked follow-up). Dormant until repo variable `FLEET_REVIEWER_ENABLED=true` and the `FLEET_APP_ID` / `FLEET_APP_PRIVATE_KEY` secrets are set.

## 0.3.97 — 2026-07-20

### Skills

- **`install-reviewer` scaffolds the subscription-CLI reviewer** — the skill now copies a `.github/workflows/review-codex.yml` (Codex CLI on a ChatGPT subscription; `tessl install`s the `jbaruch/coding-policy` rules and reviews each PR against them), the `.github/codex-review/*` driver + credential guards, and `.github/copilot-instructions.md` into the consumer — replacing the native-app `AGENTS.md`-block scaffold, which was blocked by an OpenAI account-binding bug. `scaffold.sh` copies a fixed template tree (refuse-existing in install mode, overwrite in upgrade, snapshot + restore on failure, refuse any target that exists as a non-regular file); `preflight.sh` / `commit.sh` / the tests retarget to the seven files. The template `schema.json` load indicator reads `from jbaruch/coding-policy.`, matching `prompt.md` and `PR_BODY_TEMPLATE.md`. Proven by the fleet migration of every `jbaruch/*` consumer off the paid gh-aw reviewers.
- **`release`: `post-review.sh` retries the review submit** — a transient GitHub 5xx during the review POST no longer drops the verdict; it retries with backoff before failing (surfaced by a real GitHub outage mid-rollout).

## 0.3.96 — 2026-07-19

### Rules

- **PR review moved to the Codex CLI on a ChatGPT subscription; author-model / cross-family apparatus removed** — the paid-per-token gh-aw OpenAI + Anthropic reviewers (~400K API tokens/PR) are replaced by a GitHub Actions workflow that runs the OpenAI Codex CLI authenticated by a **ChatGPT subscription** (no API key), reviewing every PR against the in-tree `rules/*.md`, with Copilot as the code-quality lane. A single reviewer that reviews every PR has no author-family to match against, so `rules/author-model-declaration.md` and the whole declaration + family-resolution + self-review-skip machinery are deleted (22→21 rules); the subscription flat-rate removes the token-cost rationale (#161) the family gate existed to serve. The native Codex code-review GitHub App was the first target (proven feasible by `projectkorero/korero` beans `Korero-cxrv` / `Korero-swrv`) but is blocked on this account by an OpenAI server-side workspace-binding desync, so this repo uses the Codex CLI in CI — OpenAI's documented CI/CD subscription-auth path.

### Skills

- **`release`: merge gate points at the self-hosted Codex reviewer** — the reviewer runs as a workflow and submits with `GITHUB_TOKEN`, so `poll-pr-reviews.sh` / `dismiss-stale-reviews.sh` resolve it by `github-actions[bot]`; the internal `reviews` key was renamed `gh_aw`→`codex`, `GH_AW_DETAILS.md` became `REVIEW_DETAILS.md`, and the mandatory `**Author-Model:**` PR-body line was dropped. `github-actions[bot]` still cannot `APPROVE` (HTTP 422), so the stale-review dismissal is unchanged.
- **`install-reviewer`: rewritten off the gh-aw scaffold** — now commits an `AGENTS.md` `## Review guidelines` block plus `.github/copilot-instructions.md` and hands the operator the reviewer-setup checklist, replacing the `gh aw compile` / lock-file / `.env.example` machinery. (This repo self-reviews with the Codex CLI workflow below; a CLI-workflow consumer scaffold is a tracked follow-up.)

### CI

- **This repo self-reviews via the Codex CLI subscription workflow** — deleted `.github/workflows/review-openai.*` and `review-anthropic.*` plus the gh-aw framework files (`.github/aw/actions-lock.json`, `.gitattributes`); added `.github/workflows/review-codex.yml` (runs `codex exec` with `.github/codex-review/{prompt.md,schema.json}`, posts the verdict via `post-review.sh`), `.github/copilot-instructions.md`, and a root `AGENTS.md ## Review guidelines`. The subscription token is read only from the `CODEX_AUTH_JSON` secret at runtime, never persisted to the runner (rules/no-secrets.md); against prompt-injection exfil, `mask-secrets.sh` registers each token with `::add-mask::` so it is redacted from the public CI logs, and `assert-no-secret-leak.sh` refuses to post + drops the token if the review output ever echoes credential material. One-time operator step: `codex login` locally, then `gh secret set CODEX_AUTH_JSON < ~/.codex/auth.json`.

## 0.3.94 — 2026-07-18

### Rules

- **Evals removed from policy and plugin** — deleted `rules/plugin-evals.md`, the `context-artifacts` Mandatory Evals section, the `eval-authoring` / `eval-curation` skills, and the `evals/` scenario suite, reconciling every cross-reference (23→22 rules). The tessl publish pipeline runs evals unconditionally inside `tesslio/patch-version-publish`, which fails and blocks the registry when the org is out of credits — a trigger #188's credit-outage tolerance cannot reach. Full inventory and motivation: PR #191.
- **`context-artifacts`: Credit-Outage Review Carve-Out** — a tessl out-of-credits (403) billing outage can now satisfy the Mandatory Review gate under an opt-in, fail-safe skip: only the credit-phrase-plus-403 signature skips the affected skill (flagged in the run summary and the `unreviewed-skills` output), every other review failure still hard-fails, and the gate self-heals when credits return. Mirrors the `ci-safety` Publish-Pipeline carve-out shape. Distinct from the forbidden review-step bypass — that dodges a verdict, this tolerates an outage that produced none. Motivation: issue #188.
- **`error-handling`: new Shell Error Handling section** — the rule was entirely silent on shell (no `set -euo pipefail`, no suppression ban), so consumers installing the plugin inherited none of the discipline the repo itself practises in all 18 of its non-test scripts. Encodes: mandatory `set -euo pipefail`; no `|| true` / `|| :` / bare `2>/dev/null`; explicit `if`/`case` on exit codes for commands that can legitimately fail; the distinction between an expected non-result and a tool failure (`grep` exits 1 on no-match, 2 on error — `|| true` collapses both); that silencing a *diagnostic* while explicitly handling the *failure* is not suppression; and that "fail visibly" permits a stderr warning rather than requiring exit non-zero. Full motivation: PR #184.

### Skills

- **All 16 production `|| true` sites converted to explicit exit-code handling** — landed with the rule above so it ships describing a repo that already obeys it (`rules/context-artifacts.md` Post-Edit Rule Audit). Three patterns: `grep`-no-match (`verify-publish-landed`, `scaffold`, `preflight`, `migrate`, `adopt`) now branch on rc so exit 2 is no longer read as "nothing found"; `jq` extraction (`verify-moderation-cleared`) validates the body once so a non-JSON response stops blaming the registry's shape; best-effort cleanup (`scaffold`, `adopt`) keeps its lenient behaviour but warns to stderr instead of skipping in silence. Full motivation: PR #184.
- **Post-audit follow-through: remaining bare cleanup `rm` swept, and an EXIT-trap tempfile leak fixed** — the bare cleanup `rm` sites the `|| true` sweep left untouched (`scaffold` rollback + success paths, `run-diagnostics`, `run-tests`) now route through a `discard` helper that warns on a failed remove instead of aborting the operation (`set -e`) or silently continuing (`set +e`), so the committed repo satisfies the new "best-effort warns, never nothing" clause everywhere the reviewer flagged. Separately, `verify-publish-landed` and `verify-moderation-cleared` declared their EXIT-trap `err_file` as a `main`-local, but the trap fires after `main` returns — on the normal success path the handler saw an empty variable and leaked the tempfile every run; only the `exit`-from-inside-`main` error paths cleaned up. `err_file` is now script-global. Full motivation: PR #184.

### Build

- **`skill-review` action: native out-of-credits tolerance (#188)** — new `credit-outage: fail | skip` input (default `fail` preserves today's Mandatory Review gate) so consumers no longer vendor the action to survive a tessl billing 403. `jbaruch/nanoclaw-travel` had copied the whole composite verbatim just to wrap the review call; that drift is what this removes. The composite body moved from an inline `run:` block into a real, unit-tested `review-skills.sh` (per `rules/script-delegation.md`). Detection requires the credit phrase **and** a `403`, both fixed strings (issue #188 follow-up: a looser substring match over review output would publish a genuinely-broken skill unreviewed) — anything else, including a future tessl wording change, falls through to hard-fail. Skipped skills surface on a new `unreviewed-skills` output and a `$GITHUB_STEP_SUMMARY` note. `run-diagnostics.sh` now shellchecks `.github/actions/` too, and this repo's own `publish.yml` opts into `credit-outage: skip`. Covered by `.github/actions/skill-review/tests/test_review_skills.sh`.

## 0.3.93 — 2026-07-17

### Skills

- **`release`: `poll-pr-reviews.sh` counted zero Copilot inline comments on every PR** — Copilot authors its review as `copilot-pull-request-reviewer[bot]` but its inline comments as `Copilot`, and the counter matched comments against the review login, so `inline_comments.copilot` was a constant `0` — vacuously satisfying Step 7's "every inline comment has a reply" merge gate and letting real Copilot findings merge unanswered. Comment counting now matches a set of logins per reviewer (`GH_AW_COMMENT_LOGINS` / `COPILOT_COMMENT_LOGINS` at the top of the script); gh-aw and `dismiss-stale-reviews.sh` were unaffected. Full motivation: PR #180.

## 0.3.92 — 2026-07-17

### Rules

- **`context-writing-style` Scope: resolve the frontmatter/body contradiction over `skills/**` reference files** — The rule's body scoped it to auto-loaded artifacts ("skills on invocation") while its own `applyTo:` glob said `skills/**/*.md`, which matches every on-demand reference file under `skills/<name>/references/**`. The two halves disagreed and the rule fired inconsistently: on `jbaruch/speaker-toolkit#117` the gh-aw reviewer returned a `context-writing-style` violation, then "All rules pass — no violations found", then a violation again — three verdicts on prose unchanged between runs, with nothing in the rule to say which half wins. Frontmatter now reads `skills/**/SKILL.md`, and Scope states the operative test: load-time, not directory. A file the agent receives without choosing it is in scope; a file it opens after reading a pointer is not.

  The body's reading is the one preserved, on three grounds. `rules/skill-authoring.md` Keep Skills Compact designates reference files as the destination for detail moved off the loaded surface — applying the prose budget to both surfaces leaves that detail nowhere to live and makes progressive disclosure pointless. The convention is already established in practice: 31 of the 104 pattern files in speaker-toolkit's taxonomy carried the banned connectives before #117 (47 occurrences), including files from earlier ingests that shipped through this same reviewer. And the remedy the glob implied — strip mechanism prose from a reference file, move it to CHANGELOG — relocates selection-time information to a surface nobody reads at selection time.

  The same glob/body split covers `CHANGELOG.md`, which the frontmatter lists while the body exempts it as
  the archive that follows "looser discipline". Scope now states that `What to Cut` and `Structure` do not
  govern CHANGELOG — `What to Cut` names CHANGELOG as where the stripped explanation goes, so a connective
  ban there would leave it nowhere to live. Surfaced on `jbaruch/speaker-toolkit#121`, where the reviewer
  flagged `because` in a CHANGELOG entry that was doing the archival job the rule assigns it. The rule
  reaches CHANGELOG only to say what belongs in it.

  Scope-only change; the prose directives are untouched. `script-as-black-box` and `skill-authoring` keep `skills/**/*.md` deliberately — the former governs reference files by design (it names `references/*.md` in its own text), and the latter carries no prose budget that would gut them.

## 0.3.91 — 2026-07-14

### Rules

- **`error-handling` Outer-Boundary Carve-Out preconditions 1–3: formatter-aware suppressor placement** — Precondition 2 now accepts the catch-all suppressor either trailing the catch line (Python/Ruff) or on the immediately-preceding line (TypeScript/ESLint under Prettier, which relocates a trailing comment off the `catch` brace and breaks `eslint-disable-line`); a suppressor detached by an intervening line still fails. Preconditions 1 and 3 clarify where the token and 3-part rationale sit when the suppressor stacks directly above the catch. Surfaced by `jbaruch/nanoclaw#735`; full motivation in PR #177.

## 0.3.90 — 2026-07-09

### Skills

- **`release` skill: deterministic pre-merge review watch** — New `skills/release/watch-pr-reviews.sh` owns the poll loop that used to be hand-rolled per agent. It wraps the `poll-pr-reviews.sh` snapshot, polling at a script-owned interval (`WATCH_PR_REVIEWS_INTERVAL_SEC`, default 15s) up to a script-owned budget (`WATCH_PR_REVIEWS_BUDGET_SEC`, default 900s), and watches exactly the fields the Step 7 merge gate reads — each gating bot's latest review *state* (by login), CI status, and merge state — emitting the snapshot plus a `.watch.result` verdict (`ready` / `changes_requested` / `ci_failure` / `dirty` / `pending_at_budget`). Step 5 is rewired from a prose poll loop to a single blocking call; `SCRIPTING.md`'s Step-7 wrapper points at it too. Motivation: agents were inventing wall-clock timeouts (one added a 10-minute cap citing a policy minute-count that does not exist), watching the wrong signals (inline-comment appearance, hand-picked run/comment ids) and waiting on verdicts that were already complete. Covered by `skills/release/tests/test_watch_pr_reviews.sh`.

### Rules

- **`ci-safety` "Always Watch CI": watch discipline** — Six bullets encode how agents watch async signals so they stop freelancing cadence and timeouts: bind the watch to the terminal event (run `conclusion`, review verdict posted, moderation `pass`), never an agent-chosen elapsed time; poll interval and give-up budget are script-owned constants, never per-run numbers; never wrap a watch in an invented `timeout` (no blanket minute count exists in the policy to cite); watch only the fields the gate reads (bot review state by login, CI status, merge state — not comment appearance, not a hand-picked id); a review is complete when its verdict posts, zero inline comments included. Points at `skills/release/watch-pr-reviews.sh` as the agent-executable form.

## 0.3.89 — 2026-07-08

### Rules

- **`ci-safety`: Superseded-Bot-Review Dismissal Carve-Out** — Dismissing a gating bot's `CHANGES_REQUESTED` that the same bot later superseded with an all-clear re-review is now explicitly sanctioned as gate cleanup, not a bypass, so an agent loading the rules as steering context stops false-positiving a legitimate dismissal as gate-gaming. Scoped to the `dismiss-stale-reviews.sh` `GATING_BOTS` allowlist (bots that cannot `APPROVE`); human change requests still gate. Full motivation: PR #174.

## 0.3.88 — 2026-07-04

### Skills

- **`release` skill: dismiss superseded review gates automatically** — New `skills/release/dismiss-stale-reviews.sh` clears a policy bot's `CHANGES_REQUESTED` that a later all-clear from the same bot superseded. A `github-actions[bot]` reviewer cannot `APPROVE` (HTTP 422), so a clean re-review lands as a `COMMENT` that never supersedes the earlier `CHANGES_REQUESTED` in GitHub's merge gate — the stale request kept `merge_state.status` at `BLOCKED` and had to be dismissed by hand every cycle. The script dismisses only superseded requests (leaves a bot whose latest verdict is still `CHANGES_REQUESTED` untouched) and is idempotent; Step 7 runs it before merging. Covered by `skills/release/tests/test_dismiss_stale_reviews.sh`.
- **`release` skill: auto-open PR and auto-merge on green gates** — Steps 2 and 7 now state explicitly that PR creation and merge proceed automatically once their objective gates are green — the readiness checks and the merge conjunction are the approval; the operator no longer pauses for a human ack.

## 0.3.87 — 2026-07-03

### Rules

- **Three carve-outs aligning rule text with lived consumer practice (#170)** — `testing-standards` gains the Live-Upstream Future-Date Carve-Out, `ci-safety` gains the Bootstrap-Red Carve-Out plus a `<type>-<issue-number>` branch-naming alternative, and `commit-conventions` points its CI-pass exception at the new carve-out. Full history: #170 and PR #172.

## 0.3.86 — 2026-07-03

### Build

- **Recover from 0.3.85's moderation block: reframe reviewer-template trust-boundary prose + explicit bump to 0.3.86** — The registry scan scored `install-reviewer` CRITICAL (E006) on 0.3.85, reading the templates' trust-boundary disclosure — its transmission-verb phrasing aimed at a model provider, plus its unqualified plugin-install clause — as exfiltration and supply-chain patterns, install-gating the version fleet-wide; identical scripts scored MEDIUM on 0.3.84, so the flag is phrasing, not behavior. Both templates now describe the same data flow in engine-reads-input terms (each workflow run's PR content goes only to its own configured engine, and the paired workflow reads the same content when the documented fallback runs both), and the manifest bumps explicitly to 0.3.86 past the blocked ghost version that `patch-version-publish`'s registry-Latest auto-bump would collide with — full motivation in PR #169.

## 0.3.85 — 2026-07-03

### Build

- **Consumer gate's trailer fallback was dead: `gh pr view` had no repo context** — The consumer templates' `gate` job (`skills/install-reviewer/review-{anthropic,openai}.md`) runs without `actions/checkout` (it installs the plugin instead), so its `gh pr view "$PR_NUMBER" --json commits` call had no git remote to infer the repo from and failed on **every** run — observed as a consistent `author-family gate: 'gh pr view' failed; proceeding body-only` in all `jbaruch/fifty-tabs-of-fares` gate logs (e.g. runs 28662293652, 28679850281). Fail-open meant nothing was silently unreviewed, but the #161 gate's signal 2 (the `Co-authored-by:` trailer email domain — Claude Code's default attribution) never fired in consumers: a same-family PR declaring authorship only via trailer fell through to the agent and burned the ~400K tokens the gate exists to save. Body-declared PRs were unaffected (the body arrives via the event payload, not `gh`). Fixed by adding `GH_REPO: ${{ github.repository }}` to the `decide` step's env — `gh` reads it natively, no checkout needed. The in-repo workflows (`.github/workflows/review-{anthropic,openai}.md`) were never affected: their gate job checks out the repo. Consumers pick the fix up via the install-reviewer **upgrade** action.

## 0.3.84 — 2026-07-02

### Build

- **Author-family gate fail-opened on every run: resolver invoked without `bash` (#166)** — The #161 gate never actually skipped a same-family reviewer in production. `author-family-gate.sh` invoked `resolve-author-family.sh` directly and guarded with `[[ -x ]]`, but tessl publish/install ships plugin files mode 0644 — the installed resolver has no exec bit, so the guard `die`d, the consumer `decide` step caught it, and fail-open defaulted `should_skip=false` on **every** run. Fleet-wide symptom: same-family reviewers always ran (and 401'd where `ANTHROPIC_API_KEY` was bad); the ~400K-token saving was zero. Evidence: `jbaruch/nanoclaw#711`, Anthropic gate run 28556385153 — `resolver not found/executable at /tmp/gh-aw/coding-policy/.tessl/plugins/...`. Fixed by invoking `bash "$RESOLVER"` and replacing the exec-bit guard with a readable-regular-file check (`[[ -f && -r ]]`) — the packaging-robust option; a `chmod +x` in-repo would be silently undone by install-time mode normalization. Every other invocation site (workflow templates, in-repo workflows) already used `bash <path>`; the gate was the only bare invoker. New regression test runs 0644 copies of both scripts end-to-end and asserts the resolver is still consulted (a `skip` decision can only come from the resolver). The fix lives entirely in the runtime-fetched scripts, so the ~14 consumer repos self-heal on their next PR — no re-scaffold needed.

## 0.3.83 — 2026-07-01

### Build

- **Bump reviewer model pins to current flagships + document their renewal cadence** — A consumer was seen reviewing with `claude-opus-4-7`: not drift on their side but exactly what the consumer template shipped, and this repo's own anthropic workflow was a version further behind on `claude-opus-4-6`. Bumped all four `engine.model` pins — consumer templates (`skills/install-reviewer/review-{anthropic,openai}.md`) and in-repo workflows (`.github/workflows/review-{anthropic,openai}.md`) — to `claude-opus-4-8` and `gpt-5.5`, and recompiled both lock files. The OpenAI bump was verified against the actual Codex CLI the lock pins (`@openai/codex@0.142.1` binary contains `gpt-5.5`; gh-aw v0.81.6's api-proxy model table lists it too) because the Codex CLI silently remaps unknown model IDs to an older model — codex 0.118.0 ran `gpt-5.4` as `gpt-5.3-codex` with no error. Freshness compliance (`rules/dependency-management.md`): the #157 Renovate config renews action/tool pins but no scanner tracks gh-aw `engine.model` pins, so each pin now carries a renewal-cadence comment (re-check the provider's current flagship at every reviewer-template change, at least quarterly; verify a new OpenAI ID against the pinned Codex CLI before shipping). Consumers pick the new pins up via the install-reviewer **upgrade** action. Also replaced the hardcoded `claude-opus-4-6` in the in-repo anthropic workflow's prose with the template's version-agnostic `claude-opus-4-x`, and refreshed the paired-families declaration example (`gpt-5.5 claude-opus-4-8`) in all four files.

## 0.3.82 — 2026-07-01

### Build

- **install-reviewer gate: fail-open on a Tessl outage, + template/scaffold accuracy fixes (#163)** — Surfaced by the Codex cross-family reviewer on a real consumer upgrade (`jbaruch/nanoclaw#709`). (1) The consumer gate's `tessl install` step had no `continue-on-error`, and the `agent` job has no `always()`/`!cancelled()` guard against a failed `gate`, so a transient registry/auth outage failed the gate *job* → the agent was cascade-skipped via `needs: gate` → the review was silently dropped. The `decide` step's fail-open only covered gate-*script* errors, not the preceding install. Fixed by marking the `tesslio/setup-tessl` and `tessl install` steps `continue-on-error: true` — on an outage the plugin is simply absent, so `decide` can't find the gate script and defaults `should_skip=false` (agent runs). Fail-open end-to-end. (2) Reworded the consumer template header from "public, version-pinned plugin" to "latest published plugin" — the install is `tessl install --yes` (unpinned). (3) `scaffold.sh` now appends missing reviewer secrets to `.env.example` as a labeled, contextual group in the header-present path too (previously bare `KEY=` at EOF); regression test covers it. (4) Fork-PR secret validation: investigated — gh-aw blocks forks by default at the `agent` job and non-member fork PRs skip cleanly via the `pre_activation` membership gate; only a team-member's fork PR hits `activation`'s secret-validation, an upstream gh-aw behavior (fork-guard sits on `agent`, not `activation` since v0.81.6) not fixable in-plugin without hand-editing a generated lock. Documented, not worked around.

## 0.3.81 — 2026-07-01

### Build

- **Runner-level author-family gate for the policy reviewers (#161)** — Each gh-aw policy reviewer used to decide self-review-bias *inside* the agent: it activated, loaded all 23 rule files + the PR diff, *then* ran `resolve-author-family.sh`, posted `Skipping: self-review-bias`, and exited — ~400K tokens for zero review value. Because this repo's PRs are predominantly Claude-authored, the Anthropic reviewer self-skipped on nearly every PR. New `skills/install-reviewer/author-family-gate.sh` moves the skip decision out of the agent: a cheap `gate` job reads two deterministic author-family signals and skips the **`agent` job** — where the ~400K-token spend lives — via gh-aw's `jobs:` + top-level `if:` mechanism (the validated pattern — **not** the broken `on.needs:`), so the same-family skip costs ~0 tokens. (gh-aw v0.81.6 composes the gate onto `agent`, so the cheap `pre_activation`/`activation` framework setup still runs; the token spend, not the seconds of slim-runner setup, is what #161 measures.) Signal 1 is the PR body `**Author-Model:**` line; signal 2 (fallback, honoring declaration precedence) is the `Co-authored-by:` trailer **email domain** — `@anthropic.com` → anthropic, `@openai.com` → openai (Codex's `commit_attribution` default is `Codex <noreply@openai.com>`; Claude Code's is `<noreply@anthropic.com>`). The email is a structured field, so this stays deterministic; the free-form display-name normalization (`Claude Opus 4.8 (1M context)` → `claude-opus-4-8`) remains the agent LLM's job. The gate composes the existing `resolve-author-family.sh` (never re-mapping families per `rules/script-as-black-box.md`) and skips *only* on a confident same-family resolution. Every other case — cross-family, both-run, human-only, a customized commit-attribution email, or no signal — falls through and the agent activates exactly as before; the gate job also fails **open** (a `gh`/script hiccup defaults `should_skip=false`) so a gate miss costs the old tokens on one PR but never drops a needed review. Applied symmetrically to both the in-repo workflows (`.github/workflows/review-{anthropic,openai}.md`) and the consumer templates (`skills/install-reviewer/review-{anthropic,openai}.md`); the consumer gate runs its own `tessl install` to obtain the scripts before the agent activates, so a cross-family consumer PR pays one extra install (~15s) to save ~400K agent tokens on every same-family PR. Outcome-based unit tests in `tests/test_author_family_gate.sh`.
- **Bump gh-aw to latest (v0.81.6) and recompile reviewer locks** — The in-repo reviewer locks had sat on gh-aw v0.71.5 since #70 while `GH_AW_PIN` advanced to v0.79.8 in a commit (33cf76e) that never recompiled them, and no CI check enforced lock freshness. Recompiled both `.github/workflows/review-{anthropic,openai}.lock.yml` on the latest stable (v0.81.6) and bumped `GH_AW_PIN` in `skills/install-reviewer/preflight.sh` to match, so the install/recovery commands, the committed locks, and the framework setup-action pin (`github/gh-aw-actions/setup`) all track one version. Pure regeneration — no workflow source change.

## 0.3.78 — 2026-06-30

### Build

- **Renovate config — renew this repo's pins (closes #155, Freshness compliance)** — Brings the repo into compliance with the Freshness section added in #156, which it was in drift against (no renewal mechanism for its own pins). `.github/renovate.json` covers all three pin categories cleanly where Dependabot couldn't: `github-actions` renews the source-workflow tags (`checkout@v4`, `setup-tessl@v2`, `patch-version-publish@v1`); `ignorePaths` skips the gh-aw `*.lock.yml` (gh-aw renews those itself via `actions-lock.json` — they're `linguist-generated`/`merge=ours`); and a `customManager` regex tracks the `shellcheck`/`pyright` tool pins in `.github/actions/setup-diagnostics/action.yml` via `# renovate:` annotation comments (the highest-rot-risk pins, which no standard ecosystem scanner sees). Validated against `renovate@latest` (the field is `managerFilePatterns`, not the older `fileMatch`); the customManager regexes were confirmed to capture both tool pins. Config lives under `.github/` so it ships with neither the repo's plugin payload nor a published tile. Activation requires the Mend Renovate GitHub App installed on the repo.

## 0.3.77 — 2026-06-30

### Rules

- **`rules/dependency-management.md` — add a Freshness section: pins need a renewal mechanism** — Surfaced by a `nanoclaw` agent hitting phantom reformats: its system `ruff` (0.15.18) disagreed with CI's pinned `ruff` (0.7.4), so the formatter wanted to rewrite files it never touched. The agent correctly diagnosed version skew and matched CI via a pinned venv — but the deeper gap was that CI sat frozen on a 2024 ruff with no bump path. The Pinning rule demanded reproducibility and said nothing about freshness, so pins rot by design. The failure mode is dual: unpinned → local/CI skew (the agent's phantom diffs), pinned-and-never-bumped → frozen on stale (the 0.7.4 trap); the rule only encoded the first half. New section: every pinned dependency needs a stated renewal mechanism — automated via Dependabot/Renovate where a scanner supports the ecosystem, or a documented cadence beside pins no scanner tracks (a version baked into a script or action step); a version bump is its own focused change, formatter/linter bumps especially per `rules/code-formatting.md` Separation of Concerns. Surface-synced: README rules table. Post-edit audit found this repo itself non-compliant — no renewal mechanism for its own pins (the `shellcheck`/`pyright` tool pins in `setup-diagnostics`, the workflow `uses:` tags, around the gh-aw generated lock files); establishing one needs design, tracked in #155.

## 0.3.76 — 2026-06-29

### Build

- **Gate language diagnostics in CI — shellcheck + pyright at zero findings (#149)** — Follow-up to #148, which shipped `rules/language-diagnostics.md` and thereby bound this repo to its own gate. Adopted on the (lightly) dirty tree: added `pyrightconfig.json` scoping pyright to the 4 first-party modules, fixed a real null-flow bug (`__doc__.split` on a possibly-`None` docstring in `compute-lift.py`) and the `importlib` `Optional` spec/loader pattern in two test loaders. shellcheck adoption fixed the genuine findings (SC2164 cd-guard, SC2034 cleanup, SC2012 `ls`→glob, SC2015 `&& pass || fail`→`if/then/else`) and suppressed the structural false positives inline with a stated cause — SC2329 (test cases dispatched indirectly via `run() "$@"`, untraceable by shellcheck) per test file, SC2016 (literal markdown backticks in a generated `.env.example` comment), SC2034 (globals read across the `source` boundary). Two real test bugs surfaced and were fixed in passing per `rules/boy-scout.md`: the `verify-moderation-cleared` env-var-validation tests exported `VERIFY_MODERATION_*` vars `main()` never re-reads (resolved at source time), so they passed for the wrong reason — rewritten to set the globals `main()` actually reads; and the `migrate` lint-failure test's `MOCK_LINT_RC=1; main` subshell-assignment was changed to a command-scoped env-prefix. New `scripts/run-diagnostics.sh` runs both engines (always both, so one pass surfaces every finding) with its own test suite; a `.github/actions/setup-diagnostics` composite action installs the pinned engines (shellcheck v0.11.0, pyright 1.1.408). Wired into `tests.yml` as a `diagnostics` job gating `unit-tests` (lint before tests), and into `publish.yml` as defense-in-depth.

## 0.3.75 — 2026-06-28

### Rules

- **`rules/testing-standards.md` Determinism — ban tests that depend on the current date or wall-clock time** — Surfaced by `fifty-tabs-of-fares`, which had many tests hardcoding "today" or a future date into assertions and fixtures; they passed when written and started failing as the wall clock crossed the baked-in date. This is the temporal sibling of the existing no-self-generated-random-data ban — both leak non-determinism into tests. New bullets: no `today`/runtime `now()`/hardcoded future dates; control the clock by injecting or freezing "now" (passed-in reference date, mocked time source, freezegun); compute relative dates from a fixed injected reference; fixed past dates as fixtures remain fine since they don't rot. Post-edit audit found this repo's own tests already compliant — `test_poll_pr_reviews.sh` uses fixed past dates as fixtures, `test_stamp_changelog.py` injects the date as a parameter rather than reading the clock.

## 0.3.74 — 2026-06-27

### Build

- **Publish 0.3.74 after 0.3.73's moderation failed and reserved its version** — `0.3.73`'s registry moderation errored (*"Moderation could not be completed"*), so it never became `Latest` (stuck at `0.3.72`) yet still reserved the `0.3.73` version number; a plain republish collided (`0.3.73 already exists`, since `tesslio/patch-version-publish` auto-bumps only from registry `Latest`, which the ghost defeats). `0.3.74` ships the identical rules/skills via an explicit manifest bump and a fresh publish-and-moderation run per `rules/ci-safety.md`. Full incident in PR #151.

## 0.3.73 — 2026-06-25

### Rules

- **New `rules/reviewer-feedback-reading.md` — a reviewer's state classifies whether it gates the merge, not whether its body must be read** — Surfaced by a release-loop session where the agent declared a PR ready to merge on the strength of "OpenAI/Copilot COMMENTED (non-blocking)." Pushed on whether it had actually read the comment, the agent admitted it had not: it read Copilot's three inline comments and the Anthropic reviewer's body, then lumped the gh-aw OpenAI policy reviewer under one "COMMENTED (non-blocking)" classification and moved on — never reading the OpenAI reviewer's body, the substantive cross-family review a Claude-authored PR is supposed to receive. "Non-blocking" silently did the dishonest work of "needn't be read." The hole was structural in the release loop: `skills/release/poll-pr-reviews.sh` returned review *state* + inline-comment *counts* but never review *bodies*, and `skills/release/SKILL.md` gated merge-readiness on state — Step 7 even declared a `COMMENTED` review with zero inline comments "fully non-blocking," which an agent reads as "no-op," though such a review still carries a body. New rule states the contract in three sections: state (`APPROVED`/`CHANGES_REQUESTED`/`COMMENTED`) classifies merge-gating, never whether the content must be read; read every reviewer's body and every inline comment before concluding any item is non-blocking ("non-blocking" is a conclusion reached *after* reading); and report each reviewer's substance separately, never a reviewer as handled by its state label alone. `alwaysApply: true`. Surface-synced: `plugin.json`, README rules table (23 rules, 16 always-on), project `.claude/CLAUDE.md` import list — which was also missing `sync-before-work.md` (present in `plugin.json`/README since its addition), fixed in the same pass per `rules/boy-scout.md`.

### Skills

- **release — surface review bodies in the poll snapshot and gate merge-readiness on having read them** — `poll-pr-reviews.sh` now adds `reviews.gh_aw.body` / `reviews.copilot.body` (full review body text, `null` when the bot hasn't reviewed) alongside the existing state + count fields; `latest_review_by` returns `{state, submitted_at, body}`. `SKILL.md` Step 5 documents the new field, Step 6 leads with "Read every review in full first" (a `COMMENTED` state or zero inline comments is not a license to skip the body), and Step 7 replaces the misleading "a `COMMENTED` review with zero inline comments is fully non-blocking" line with a gate conjunct requiring every non-empty `reviews.*.body` to have been read before merge. `SCRIPTING.md` adds a wrapper rule: whether a `COMMENTED` body is substantive is a reasoning call a script can't make (`rules/script-delegation.md`), so an unattended wrapper must surface a non-empty body for an agent/human read rather than auto-pass on state alone. Tests: `test_poll_pr_reviews.sh` gains `t_latest_review_by_surfaces_body_text` (verbatim body surfaced on a COMMENTED review) and asserts `body: null` on the empty-reviews case (11 pass). Implements `rules/reviewer-feedback-reading.md`.

## 0.3.72 — 2026-06-25

### Rules

- **New `rules/language-diagnostics.md` — enable the project's language server and treat its findings as non-dismissible without cause** — Surfaced by `jbaruch/nanoclaw-travel#80`/`#81`: a manual `pyright` run on a repo that had never been type-checked produced ~200 findings, decomposing into 9 real source bugs (a `tuple.__name__` validation crash, a could-be-None `raise __cause__`, Optional-flow bugs — all fixed in #80) hiding behind ~100 import false-positives and 110 low-severity test-strictness errors. A fleet survey showed the gap is systemic but language-split: not one of the 16 Python repos gates diagnostics (the nanoclaw family runs `ruff` format/lint only, the other nine run nothing), while the real-TypeScript nanoclaw repos (`nanoclaw`, `-public`, `-telegram`) already gate `tsc` in CI — the existence proof the contract works. Two TS repos (`tessl-demo`, `robocoders-jfokus-2026`) are `strict` with a `typecheck` script that CI never runs. The trigger was the maintainer telling the agent to "stop ignoring pyright warnings" — a deterministic check enforced by memory, the `rules/script-delegation.md` anti-pattern. The rule generalized past type-checking to any language's diagnostics engine (pyright, tsc, clippy, `go vet`): enable the language server in-editor; findings are fixed or suppressed inline with a stated cause (no blanket `# type: ignore` / `# pyright: basic` / `// @ts-nocheck`); CI gates the engine's headless form at zero findings, before tests, alongside format/lint per `rules/code-formatting.md`; resolve modules first so real bugs don't drown in resolution noise; adopt on a dirty tree via config PR → fix PRs by finding shape → CI-gate PR. It does not mandate an immediate clean-tree migration across the fleet — that's per-repo work, tracked in each repo. Surface-synced: `plugin.json`, README rules table, project `.claude/CLAUDE.md` import list.

## 0.3.71 — 2026-06-24

### Skills

- **install-reviewer — script the reviewer self-gate so a newer model id can't trigger a false self-skip (#145)** — On a Claude-authored PR (`**Author-Model:** claude-opus-4-8`, observed on `jbaruch/fifty-tabs-of-fares#24`), the OpenAI policy reviewer falsely self-skipped citing `self-review-bias — author-family openai`. Combined with the Anthropic reviewer correctly skipping (author is anthropic), both paired reviewers skipped and the PR received zero substantive policy review. Root cause: the author→family resolution — a fully deterministic prefix mapping (`claude-* → anthropic`) — was done by the reviewer LLM in prose, and the gpt-5.4 Codex agent mis-mapped `claude-opus-4-8` (newer than its own model set) to its own family and self-skipped. Per `rules/script-delegation.md` that resolution belongs in a script. New `skills/install-reviewer/resolve-author-family.sh` owns family-mapping, the skip predicate, and the verbatim review bodies; the reviewer LLM keeps only the regex-trap half — extracting/normalizing tokens from the `**Author-Model:**` body line or free-form `Co-authored-by:` display names — and hands the script clean tokens, so no natural-language parsing enters the script. The skip path now fires only on confident same-family-only resolution: an unrecognized or newer id maps to anthropic/google/ad-hoc, never to the reviewer's own family by accident, so it can't produce a false self-skip. The asymmetry is now structural — a false proceed costs one redundant review, a false skip costs the only cross-family review. Step 1 of all four reviewer prompts (in-repo `.github/workflows/review-{openai,anthropic}.md` and consumer templates `skills/install-reviewer/review-{openai,anthropic}.md`) extract tokens then delegate to the resolver; the `tools.bash` allowlists and the in-repo `.lock.yml` files were recompiled to permit the script. Regression covered by `skills/install-reviewer/tests/test_resolve_author_family.sh` (13 cases, including the exact #145 case: `claude-opus-4-8` + openai reviewer → review, not skip).

## 0.3.70 — 2026-06-24

### Build

- **Run the bash unit-test suites in CI — cobbler's-shoes gap** — `rules/testing-standards.md` mandates a test for every module, and the repo had 12 suites (~136 assertions across `install-reviewer`, `release`, `migrate-to-plugin`, `adopt-fork-pr`), but nothing ran them: `publish.yml` did lint → skill-review → publish only, no git hook, no Makefile target. A suite could go red on `main` and a regression could publish green. Added `scripts/run-tests.sh` (discovers `<base>/**/tests/test_*.sh`, runs each in its own subshell, emits a JSON run summary on stdout per `rules/script-delegation.md` with human progress on stderr, exits non-zero on any failure; base-dir arg so it can be pointed at a fixture tree) plus its own test `scripts/tests/test_run_tests.sh`. New `tests.yml` runs it on every PR and push to `main` (gates merge); `publish.yml` runs the same runner after lint as defense-in-depth so a direct push to `main` can't ship on red. `scripts/` is `.tesslignore`d — the dev runner doesn't ship in the published plugin. Wiring the runner immediately exposed two non-hermetic suites (`install-reviewer/tests/test_branch.sh`, `test_preflight.sh`): their fixtures committed under an inherited git identity, which a clean CI runner lacks (`Author identity unknown`) — green only because dev machines carry a global one. Fixed by giving each fixture repo its own `user.email`/`user.name` before committing. The cross-family policy reviewer then hardened the runner itself: structured JSON summary on stdout (progress to stderr), NUL-delimited discovery (`find -print0`/`sort -z`/`read -d ''`) so suite paths with spaces or newlines survive, observable `find` failure via a temp file + `pipefail` (process substitution would hide it), and a full control-character JSON escaper.

## 0.3.69 — 2026-06-19

### Skills

- **install-reviewer — document the reviewer data-flow / trust boundary** — Both reviewer templates (`review-anthropic.md`, `review-openai.md`) now state, in the workflow `description`, exactly what the reviewer sends to the review model (the pull-request diff, the PR title/body and commit messages read for its author-model gate and changed-file allowlist, and the published policy files — never repository secrets, tokens, or credentials) and what the `tessl install jbaruch/coding-policy` pre-step fetches (a public, version-pinned plugin from the official Tessl registry, not arbitrary remote code). Operator-facing transparency for the reviewer's cross-provider behavior, which a registry moderation scan otherwise had no stated trust model to weigh.

## 0.3.68 — 2026-06-19

### Skills

- **install-reviewer — pin gh-aw install to the latest stable (v0.79.8)** — The preflight's install and recovery commands pinned `v0.71.0`, which is the technical *floor* (where `acceptEdits` and `engine.args` landed), not a version anyone should freshly install — so every fresh setup landed on a months-old gh-aw. The install/recovery commands now pin the latest stable, and the floor (`GH_AW_MIN`) and pin (`GH_AW_PIN`) are named constants in `preflight.sh` so they can't drift apart or silently go stale again. The enforced floor is unchanged at `v0.71.0`. Also corrected now-false prose: when the skill was written, latest stable (`v0.68.3`) sat below the floor so a prerelease pin was mandatory; stable has since caught up (`v0.79.8` > `v0.71.0`), so the "unpinned install fails the version check" warning no longer holds.

## 0.3.67 — 2026-06-19

### Rules

- **sync-before-work — fetch and sync the local checkout before the first read (#141)** — New always-on rule. An agent was asked to fix two issues that referenced skills by name (`vault-ingress`, `vault-clarification`) and step number. It read the local tree, found a *monolithic* `rhetoric-knowledge-vault/SKILL.md`, mapped the issues onto it, implemented the fix, wrote tests, committed, pushed, and opened a PR — which came back `CONFLICTING`. Local `main` was ~410 commits behind `origin/main` (0.10.1 vs 0.18.x); upstream had long since refactored that one skill into six (`vault-ingress`/`vault-clarification`/`vault-profile`/…), so the file the agent edited had been *deleted upstream* and the issues' names/step-numbers matched the current structure exactly. The whole first implementation landed on a tombstone — thrown away, rebased onto real `origin/main`, redone against the actual files. Cost: a wasted implementation pass, a force-push, a rewritten PR, all avoidable by a `git fetch` at minute zero. The agent even had the clue (issues naming files absent locally) and still didn't check freshness first. The rule encodes: fetch before the first read, fast-forward/rebase/reset the default onto `origin/<default>`, branch from the fresh tip, treat "this file looks like X" as suspect when the local default is behind, and honor an explicitly-pinned older ref as the only exception. Rule body carries the directives; this entry holds the incident.

## 0.3.66 — 2026-06-11

### Rules

- **ci-safety — Checks Not Starting diagnostic for merge-conflict stalls (#139)** — Agents repeatedly misdiagnosed a stuck-`queued` PR as a GitHub Actions outage and either waited indefinitely or gave up, when the real cause was a `CONFLICTING` / `mergeStateStatus=DIRTY` PR. `pull_request` workflows run against the PR's merge commit (`refs/pull/N/merge`); a conflict means GitHub can't compute that ref, so it never creates the `github-actions` check-suite — while third-party apps (Copilot, SonarQube, reviewers) subscribe to the head SHA and *do* register suites that then sit `queued`. That split (third-party suites `queued`, no `github-actions` suite at all) is the tell, and it's easy to misread because the repo's Actions are enabled and `main` runs fine. Added a `## Checks Not Starting` section under Always Watch CI: check merge state first (`gh pr view <N> --json mergeable,mergeStateStatus`), inspect suites via `gh api .../check-suites`, and fix by merging the base into the branch (or rebasing the branch onto the base) so the merge ref recomputes and the suite runs. Pure rule addition; the mechanism prose lives here in the archive, the rule body carries only the diagnostic and fix.

## 0.3.65 — 2026-06-11

### Rules

- **context-artifacts — CHANGELOG Hygiene drops the `Unreleased` / manual-release model** — Every merge publishes a version, so there is no "unreleased" state and the rule no longer presents two release models. Removed the "Manual release … keep an `## Unreleased` section" branch and the "match the convention to the release model" framing, leaving publish-on-merge as the single model: the `Unreleased` heading is forbidden, authors add un-headed `### ` entry blocks that the wired `stamp-changelog` step versions at publish (or write the `## <version> — <date>` heading manually where no stamp step is wired). Per the Post-Edit Rule Audit, dropped the same multi-model framing from the two supporting docs — `skills/release/stamp-changelog.py` and `.github/actions/stamp-changelog/action.yml` docstrings now read "every merge is published" rather than "in a publish-on-merge model" and describe the registry failure mode as "unversioned entries" rather than "Unreleased". Historical CHANGELOG entries naming the old model are archive and left intact.

## 0.3.64 — 2026-06-10

### Rules

- **stateful-artifacts — Cross-Pipeline Schema Bumps section + Migration Policy reader-gate split (#135)** — The rule covered steady-state semantics (owner-only migration, `schema_version` per record, surface-sync on rename/removal, the reader's "no usable prior state" gate) but said nothing about a `schema_version` bump when the writer and its readers deploy through separate pipelines and the bump therefore can't land atomically. Live incident: `jbaruch/nanoclaw#371` was reopened by this. The host repo bumped `tz_state.schema_version` 3 → 4 (writer, via `deploy.sh`) while two readers in the `jbaruch/nanoclaw-admin` tile (`heartbeat-precheck.py`, `read-current-tz.py`) deployed via `tessl publish` → `tessl update` — a different pipeline with its own latency. The writer's bump reached production first, every reader hit `4 != 3`, fell through to "no usable prior state," and the fallback (wake the agent every cycle) produced a multi-day cost regression; fixed in `jbaruch/nanoclaw-admin#260` by bumping the readers' accepted version. The reader gate rejects *any* mismatch by design (data safety), so with single-exact-version pins there is no safe ordering across two independent pipelines — writer-first starves lagging readers, reader-first starves the new readers until the writer catches up. The outage was the rule working as written colliding with the physics of two deploy pipelines, not a violation. Added a `## Cross-Pipeline Schema Bumps` section: treat the bump as non-atomic (the reader's no-prior-state path fires only for records stamped with a version it doesn't accept, not for matching ones); that fallback must be safe/non-disruptive (never wake-always / full-recompute / alert-storm); a zero-skew rollout requires readers that parse both shapes (deploy dual-accept readers first → flip the writer → drop the old version) — additive bumps get that cheaply via field defaults, breaking bumps only when a reader can be written to parse both; the narrow exception is a breaking bump no single reader can parse both ways, where the writer goes offline for an atomic cutover and the dual-accept sequence does not apply. Also split Migration Policy's reader-gate guidance into its two distinct cases to match the `!=` gate the readers actually run — an older record waits for the owner's next-run upgrade, while a record newer than the reader accepts is reader lag the reader itself must be updated for (not a pending migration). Lightweight rule addition, not deploy-time tooling — the incident's blast radius was bounded (cost-only, self-healed once readers deployed).

## 0.3.63 — 2026-06-10

### Rules

- **context-artifacts — CHANGELOG Hygiene points at the reusable stamp action (#134)** — The "Publish-on-merge" sub-list now names the concrete step consumers wire — `.github/actions/stamp-changelog`, called as `uses: jbaruch/coding-policy/.github/actions/stamp-changelog@<ref>` before `tesslio/patch-version-publish` — instead of leaving "a separate step the repo wires itself" abstract. Closes the #124 follow-up asking the rule to point at the shipped automation rather than assert the bump action stamps.

### Build

- **Reusable `stamp-changelog` composite action (closes #134)** — Packages the CHANGELOG auto-stamp as `.github/actions/stamp-changelog/action.yml`, the consumer-facing form of the logic 0.3.59 wired only into this repo's own `publish.yml`. A publish-on-merge consumer adds one step before `tesslio/patch-version-publish` (`uses: jbaruch/coding-policy/.github/actions/stamp-changelog@<ref>`) and its un-headed `### ` entries get a `## <version> — <date>` heading at publish instead of rendering as "Unreleased" in the registry "what's new" — the exact failure `jbaruch/speaker-toolkit` had to hand-backfill for 0.18.20–0.18.26. The action wraps the existing `skills/release/stamp-changelog.py` (single source of truth — already general: auto-detects `.tessl-plugin/plugin.json` then `tile.json`, exposes `--changelog`/`--manifest`/`--latest`/`--date`) and resolves it three levels up from the action path, so no script is copied into the action dir. Inputs cover changelog/manifest/latest/date plus `commit` (default `true`) and `commit-message`; it commits with `[skip ci]` and no push per the `ci-safety` Publish-Pipeline Loop-Prevention Carve-Out, letting the publish step's own commit carry it. This repo's `publish.yml` now dogfoods it via `uses: ./.github/actions/stamp-changelog`, replacing the inline python+commit block. `.github/` is `.tesslignore`d, so the action ships from GitHub via `uses:`, not the published plugin — no `plugin.json` change.

## 0.3.62 — 2026-06-08

### Rules

- **testing-standards — Platform-Bound Untestable Carve-Out for code CI cannot host (#116)** — `## Coverage` stated flatly "every module gets tests — no untested code ships" with no escape hatch for code that inherently cannot run on a CI runner. Live case: `jbaruch/speaker-toolkit` PR #54 (merged as 0.18.9) added a deck-editing path implemented as VBA macros driven via AppleScript against Microsoft PowerPoint — it needs the PowerPoint desktop app plus macOS Automation consent, neither available in Linux CI, and the policy reviewer correctly flagged it as "a testing exemption testing-standards does not grant," so it merged only via owner override. Added a narrow carve-out following the repo's established carve-out format (compact "Narrow exception for X" lead, an "Applies when" qualifier, numbered preconditions, a reset line) and mold (error-handling outer-boundary, dependency-management runtime-managed-manifest): the unhostable layer is exempt only when (1) the deterministic CI-runnable pieces are extracted and unit-tested, (2) a manual validation procedure is documented, and (3) the consuming plugin's authority-of-record rule names the carve-out, each exempt artifact, and where its validation lives. An anti-pattern bullet keeps "hard to install" from qualifying (that stays an install task per `rules/ci-safety.md` Install, Don't Skip), and the Coverage bullet now points at the carve-out so a reviewer quoting "no untested code ships" sees the exception. Reference impl already satisfies (1)+(2): speaker-toolkit extracted `backgrounds-manifest-to-spec.py` (unit-tested) out of `apply-backgrounds.sh`, and `rules/deck-editing-rules.md` documents the manual PowerPoint+Keynote validation.

## 0.3.61 — 2026-06-08

### Rules

- **skill-authoring — `argument-hint` is an allowed optional SKILL.md frontmatter field (#117)** — The `## SKILL.md Frontmatter` "Optional fields" line read as a closed allow-list, so the OpenAI policy reviewer on `intent-integrity-chain/kit` [PR #89](https://github.com/intent-integrity-chain/kit/pull/89) blocked adding `argument-hint:` to five skills ("the loaded policy only permits `allowed-tools`, `disable-model-invocation`, and `user-invocable`"). `argument-hint` is real Claude Code slash-command frontmatter — the UI surfaces the expected argument shape inline next to the command; it carries no runtime semantics, and other agents (Codex, Gemini, OpenCode) ignore unknown fields. Added it to the optional-field list, gave it a dedicated explanation bullet with an example value, and added a bullet stating the list is illustrative, not a closed allow-list: agents preserve unrecognized skill frontmatter and ignore fields they don't use, mirroring `rules/rule-frontmatter.md` passthrough. The "Optional fields:" lead-in also softened to "Optional fields include" to kill the closed-list reading at the source. Each new bullet carries at most one parenthetical clause per `rules/context-writing-style.md` (the OpenAI reviewer on this PR's first round flagged the combined field-list bullet for stacking two). Unblocks kit #89 and any future PR adding `argument-hint:` to a skill.

## 0.3.60 — 2026-06-08

- **Duplicate re-publish of 0.3.59 — no content changes** — A stray publish run (a manual `workflow_dispatch` racing the #131 auto-publish) bumped the registry 0.3.59 → 0.3.60 and shipped identical `main` content; its version-bump commit never landed on `main` (PR creation failed with `Resource not accessible by integration`), so 0.3.60 was absent from the manifest and this CHANGELOG. Recorded here for history — 0.3.60 == 0.3.59.

## 0.3.59 — 2026-06-08

### Rules

- **ci-safety — `[skip ci]` carve-out for publish-pipeline loop-prevention (#131)** — `## Never Skip Tests` flatly forbade `[skip ci]`, but every `tesslio/patch-version-publish` bump commit on `main` already carries it: the commit the publish workflow pushes back would otherwise re-trigger the workflow (infinite loop). The auto-stamp step below adds a second such commit. Added a narrow carve-out — `[skip ci]` is permitted only on a CI-bot commit the publish workflow pushes to the protected branch for release bookkeeping (version bump, CHANGELOG stamp) carrying no source/test changes; every other commit still never uses it. Reconciles the rule with the pipeline the repo already runs.

### Build

- **Publish pipeline — CHANGELOG auto-stamp step (implements the #124 follow-up)** — Wires the automation #124/#125 only documented: `skills/release/stamp-changelog.py` computes the version the publish step will assign (registry-latest patch-bump, or the manifest when it is already ahead — mirrors `tesslio/patch-version-publish`) and inserts a `## <version> — <date>` heading above the topmost un-headed `### ` blocks; idempotent no-op when the top is already headed. A new `publish.yml` step runs it before `patch-version-publish` and commits the result (no push — the publish step's commit carries it, `[skip ci]` guards the re-trigger). Ported from `jbaruch/speaker-toolkit#60` (`ci/auto-stamp-changelog`), adapted to coding-policy: lives under `skills/release/` (not top-level `scripts/`), queries `tessl plugin info`, primary manifest `.tessl-plugin/plugin.json`. `skills/release/tests/test_stamp_changelog.py` covers 10 paths (version math incl. malformed + manual-ahead, insert/no-op/idempotent/trailing-newline). This moves the repo from the rule's "without a stamp step" branch (hand-stamp every entry, the source of #125's rebase churn) to the "with a wired stamp step" branch — PR authors now add un-headed `### ` blocks and the pipeline versions them. This very entry is left un-headed and stamped by the new step on its own merge.

## 0.3.57 — 2026-06-07

### Rules

- **context-artifacts — CHANGELOG Hygiene drops the false auto-stamp assumption (#124)** — The publish-on-merge branch stated as fact that the publish pipeline stamps a `## <version> — <date>` heading before publish, citing `tesslio/patch-version-publish`. That action bumps the manifest version and publishes; it does not touch `CHANGELOG.md`. A repo that faithfully dropped its manual `## <version>` headings on that promise ends up with header-less `### ` blocks accumulating under `# Changelog` (live incident: `jbaruch/speaker-toolkit` PR #76 → 0.18.20, whose paired reviewer enforced the un-headed convention and whose bump commit `121a19c` touched only `tile.json`, leaving an unstamped entry on `main`). Reworked the rule to a conditional: the un-headed convention applies only where a CHANGELOG-stamping step is actually wired; without one, authors write the `## <version> — <date>` heading manually. This repo has no stamp step, so its own accumulated 0.3.50–0.3.56 entries are retro-stamped here per the Post-Edit Rule Audit (and this very entry carries a manual `## 0.3.57` heading), and `skills/eval-curation/SKILL.md`'s "under Unreleased" note is made release-model-agnostic — the model-agnostic tweak flagged as a follow-up in the 0.3.50 CHANGELOG Hygiene entry below.

## 0.3.56 — 2026-06-07

### Skills

- **install-reviewer + review workflows — read-only guardrail blocks on-disk bug repros (#128)** — The reviewer prompts (`skills/install-reviewer/review-openai.md`, `skills/install-reviewer/review-anthropic.md`, and the repo's own `.github/workflows/review-openai.md` / `review-anthropic.md`) gain a leading Guardrails bullet: the reviewer is read-only, must not create/edit/move/download files anywhere on the runner, and must demonstrate control-character / NTFS-illegal-name cases (newline, control char, or any of `" : < > | * ?`) as inline-escaped text in the review comment rather than as on-disk files. Incident: on `jbaruch/speaker-toolkit` PR #77 the Codex reviewer, correctly flagging a hand-rolled JSON encoder against `script-delegation`, proved the bug by writing a repro file literally named `` `_talks/line\nbreak.md` ``; the embedded newline made `actions/upload-artifact@v7` reject the entire `agent` artifact, and because the upload step is `continue-on-error` the agent job stayed green while the downstream `detection` job failed with a misleading `Prompt file not found at /tmp/gh-aw/threat-detection/aw-prompts/prompt.txt` — reddening every same-repo PR the OpenAI reviewer touched. The Codex engine runs `--dangerously-bypass-approvals-and-sandbox` and does not enforce the read-only `tools.bash` allowlist, so the prompt is the only lever for it (Claude Code enforces the allowlist and has no write tool). Workflow locks recompiled with gh-aw v0.71.5; the prompt body is runtime-imported via `{{#runtime-import}}`, so the lock change only satisfies the commit-timestamp staleness gate. The two gh-aw-side defects — one stray filename nuking the whole artifact under `continue-on-error`, and `detection` hard-failing on an absent artifact with a misleading message — are upstream, not fixable here.

## 0.3.55 — 2026-06-07

### Skills

- **migrate-to-plugin — new skill for legacy `tile.json` → `plugin.json` migration (closes #108)** — Detects a legacy `tile.json` with no `.tessl-plugin/plugin.json`, runs `tessl plugin migrate`, renames `.tileignore` → `.tesslignore`, removes the obsolete `tile.json`, re-lints, and emits a JSON report with a `residual_files` list. `skills/migrate-to-plugin/migrate.sh` owns the deterministic mechanics per `rules/script-delegation.md`; `SKILL.md` Step 2 owns the judgment the script does not make — reconciling residual "tile" wording to "plugin" while preserving contract surfaces (the `tile.json` filename in historical refs, `v1/tiles/...` API routes, code identifiers, frozen eval-scenario names, external doc page paths). `tests/test_migrate.sh` covers 7 paths (already-migrated no-op, not-a-plugin no-op, legacy happy path, lint failure, migrate-without-manifest, pre-existing `.tesslignore`, JSON shape) with a mocked `tessl`. Part 1 (the manifest + scope-model migration of this repo) landed in 0.3.49; the consumer-facing terminology sweep landed in 0.3.54; this is Part 2 — the reusable migration skill for other legacy plugins. An eval scenario for the Step 2 reconciliation judgment is deferred pending a floor-model lift-check per `rules/plugin-evals.md` (don't admit an unverified scenario).

## 0.3.54 — 2026-06-07

### Rules

- **tile → plugin terminology sweep (closes #113; completes #108)** — Renames the package-sense noun "tile" → "plugin" (and "steering rules" → "rules") across every consumer-facing surface the 0.3.49 entry deferred: the 7 affected rules (`plugin-evals`, `dependency-management`, `ci-safety`, `script-delegation`, `stateful-artifacts`, `context-writing-style`, `skill-authoring`), the `eval-authoring`/`eval-curation`/`install-reviewer`/`release` skills and their prose, `README.md` (description, "20 rules", "ship plugins"), `.github/workflows/publish.yml` (workflow name + concurrency group), and `.github/actions/skill-review/action.yml`. The `tessl tile <cmd>` → `tessl plugin <cmd>` CLI alias is swept in `skills/release` (`SKILL.md`, `SCRIPTING.md`, `verify-publish-landed.sh`) with `tests/test_verify_publish_landed.sh`'s `tessl` mock flipped to switch on the `plugin` subcommand — the CLI confirms `plugin` is canonical (`tessl plugin info` is the same binary, identical output) and `tile` an identical alias. Consumer-facing argument placeholders were renamed too — `tessl plugin info <workspace>/<plugin>` and the `verify-*.sh <workspace> <plugin> …` usages now read `<plugin>`. Deliberate KEEPS, by contract not oversight: the `v1/tiles/<ws>/<name>/versions/<v>` REST endpoint in `verify-moderation-cleared.sh` (no `v1/plugins/` endpoint exists — it 404s), the `<tile>` slug segment inside that route's own doc comment, the `tile` shell variable bound to it, the frozen `evals/*` scenario directory names (rename-stability per `plugin-evals` Naming), and `docs/tessl-rule-frontmatter.md`'s real docs.tessl.io page filenames (`developing-tiles-locally.md`) and factual legacy `tile.json`/`steering`-map descriptions. Eval criteria/task prose (`evals/*`) is intentionally out of scope — editing graded text risks shifting lift history. Net: the CLI surface reads canonical-`plugin` end to end; `tile` survives only where it names a live API path, an argument bound to it, a frozen identifier, or a factual legacy reference.

## 0.3.53 — 2026-06-07

### Rules

- **plugin-evals — Closed-Loop Carve-Out removed (closes #118)** — Deleted the `## Closed-Loop Carve-Out` section from `rules/plugin-evals.md` (plus its `description` keyword, the Coverage "Subject to the closed-loop carve-out below" bullet, and the two `rules/context-artifacts.md` cross-refs). In practice the carve-out was a quota dodge for the `nanoclaw-*` fleet, not a principled no-consumption exemption. #118 established that the only eval lever we control is scenario count — Tessl infra owns running/caching/re-running — and the lift-gated admission + per-skill cap shipped in #120 (0.3.51) is that throttle, so the dodge is unnecessary. Tiles that claimed the carve-out now fall back to the standard lift-gated Coverage requirement; their orphaned `### Rules` declarations are downstream cleanup in those repos. Axis 1 (per-arm input-hash memoization of eval runs) stays a Tessl-infra ask, not in-repo work; Persistence is unchanged.

## 0.3.52 — 2026-06-07

### Rules

- **ci-safety — Content-Only Direct-Push Carve-Out precondition 3 now accepts a client-side diff gate (#119)** — Server-side allowlist enforcement is unsatisfiable on github.com personal repos (the path ruleset is a blocklist, pre-receive hooks are Enterprise-only, push-triggered workflows are disqualified post-push CI), so content sites could never claim the carve-out. Precondition 3 splits into form A (server-side gate, unchanged) and form B (a deterministic client-side diff gate per `rules/script-delegation.md`, permitted only where the platform can't express server-side allowlist enforcement): direct-push only when every changed path matches a content glob, automatic branch+PR fallback otherwise, authority-of-record rule names the gate script. Tradeoff and incident trace in PR #122 / #119.

## 0.3.51 — 2026-06-07

### Rules

- **plugin-evals + eval-authoring — eval coverage is now lift-gated and capped, not a completeness mandate (#118)** — Agents over-generated scenarios because the instructions told them to: Coverage said "*every* skill ships eval cases, positive AND negative," `eval-authoring` Step 7 was titled "Fill Coverage Gaps" (expand-to-cover, no ceiling), and the Lift section anchored "a 10-scenario suite ... beats a 35-scenario suite" (double digits as normal). `tessl scenario generate` only produces a moderate batch — the multiplication came from these instructions, with "coverage" (test everything) as the wrong root metaphor for what's actually wanted (find the few places the tile beats baseline and prove it). Recast `rules/plugin-evals.md` Coverage: a scenario earns its place only by demonstrated lift on the floor model; a default cap of 3 per skill (exceeding it requires a distinct tile-prescribed behavior that clears the bar); admission gate (run a candidate once, drop it if flat — the one-time check costs far less than a dead-weight scenario re-running on every publish) instead of generate-broadly-prune-later; scope to genuine LLM-side judgment (a script-backed decisional core has no LLM surface to eval); negatives only where a refusal/silence contract exists; absence correct when nothing clears the bar. Deleted the "10 vs 35" anchor for explicit scarcity (most skills 1–3; many behaviors zero). `eval-authoring` Step 7 retitled "Add Lift-Bearing Scenarios" and points at the rule for admission rather than instructing enumerate-and-fill; description "coverage gap analysis" → "lift-gated scenario admission"; step-list intro updated. `rules/context-artifacts.md` "Mandatory Evals" first bullet moved off "every skill ships eval cases" to a bare pointer at plugin-evals Coverage (per Consistency Check the contract lives in one rule and others reference it; Step 7 likewise points rather than restates). The cap is an admission gate, not retroactive truncation — existing suites stay under the Lift curation process. Implements #118 Axis 2 (the in-repo throttle) and subsumes the issue's levers 2 (script-delegation scoping) and 3 (negatives only with a refusal contract); Axis 1 (per-arm input-hash memoization of eval runs) stays a tessl tooling ask with Persistence unchanged until it lands. Retired the now-obsolete `coverage-gap-identification-and-fill` eval scenario per the Post-Edit Rule Audit — it exercised the removed coverage-fill Step 7 model.

## 0.3.50 — 2026-06-07

### Rules

- **ci-safety — moderation is now a post-publish install gate (reverses the earlier "registry never rejects" framing)** — Tessl changed: a freshly published version can be blocked from `tessl install` until its moderation state reaches `pass`. The release contract gains a third conjunct — run-success AND registry-advance AND moderation-clear — plus a wait step: poll the published version's moderation state with exponential backoff and fail loud at budget. This reverses the prior 0.3.x framing (`The Tessl registry never rejects a published version`; `moderationPassed: false affects tessl search visibility only`) that earlier entries planted to kill a hedge-hallucination — the anti-hallucination intent survives as "query the real moderation state, never invent one to hedge a failed publish." The advisory-vs-blocking security-finding distinction is retained. Signal: `tessl api v1/tiles/<ws>/<tile>/versions/<v>` → `.data.attributes.moderationStatus` / `moderationPassed` / `moderationError`.

- **CHANGELOG Hygiene rewritten for publish-on-merge vs manual release** — `rules/context-artifacts.md` now matches the CHANGELOG's top-of-file convention to the release model. Publish-on-merge projects (every merge auto-publishes) drop the `Unreleased` bucket: authors add un-headed `### ` entries at the top and the publish pipeline stamps a `## <version> — <date>` heading before publish; an `Unreleased` heading is forbidden because merged work is already published. Manual-release projects keep `Unreleased` and consolidate on release. Prompted by the Tessl registry "what's new" surfacing already-published entries as unreleased. Follow-ups: this repo's publish pipeline must adopt the stamp step (port `scripts/stamp-changelog.py` + the workflow step from `jbaruch/speaker-toolkit#60`) so un-headed entries get versioned; `skills/eval-curation/SKILL.md`'s "under Unreleased" wording needs a model-agnostic tweak. (The stamping claim in this entry is wrong and was corrected in 0.3.57 / #124: `tesslio/patch-version-publish` does not stamp `CHANGELOG.md`; the un-headed convention is now conditional on a separately-wired stamp step, and this repo writes version headings manually.)

### Skills

- **release — moderation-clear gate wired into Step 7 (conjunct 3)** — New `skills/release/verify-moderation-cleared.sh <workspace> <tile> <version>` polls the registry moderation signal with exponential backoff (BASE 5s → MAX 120s, BUDGET 600s; env-overridable for tests) and emits `{"ok",...,"moderation_status","version","attempts","elapsed_seconds"}`. Exit 0 cleared (`moderationStatus == pass` or `moderationPassed == true`); exit 1 blocked (non-null `moderationError` or a terminal block state — fast fail) or still-pending at budget exhaustion (fail loud); exit 2 arg/tool error. Backoff constants and the block-state set live at the top of the script per `rules/script-as-black-box.md`. `SKILL.md` Step 7 and `SCRIPTING.md`'s Wrapping-Step-7 gates call it after `verify-publish-landed.sh`; `SCRIPTING.md`'s former "No moderation states" prohibition is inverted into a required moderation-clear gate. `tests/test_verify_moderation_cleared.sh` covers 13 paths (immediate/boolean/deferred clear, block-by-status, block-by-error, budget exhaustion, arg + env validation, tessl failure, unparseable body); mocks `tessl`/`sleep` as functions over a tempfile response queue.

### Evals

- **migrate-to-plugin-keeps-contracts — lift-verified judgment scenario re-added (closes #108)** — The scenario deferred from #127 (0.3.55) now ships, after a floor-model (`claude:glm-5.1`) admission check: baseline 34% vs with-context 100%, **lift +66**. It presents a synthetic legacy `acme/widgets` plugin and grades whether the migration plan keeps live contracts (`v1/tiles` REST route, `tileRegistry`/`fetchTile` identifiers, the factual legacy `tile.json` CHANGELOG line) while renaming the rest — baseline blindly replaces every `tile` and scores 0% on the `v1/tiles`-route and code-identifier keep-criteria (it does keep the legacy reference). Refined before admission across two runs: removed the zero-lift "Renames package-sense prose" criterion (50%/50% — baseline and with-context alike did the generic `tile`→`plugin` rename but neither dropped the `steering rules`→`rules` framing) and stripped the task to a situation-only brief (no prescribed sequence or per-occurrence format, per `rules/plugin-evals.md` Task/Criteria shape), reconcentrating weight on the keep + distinguish criteria (6 criteria, sum 100) — raising the delta from +43 to +66. Completes the eval item left open on #108 Part 2.

- **pr-merge-and-post-merge-cleanup — conjunction criterion realigned to the 3-conjunct contract** — The "Conjunction check" criterion (max_score 15) previously zero-scored any moderation-state handling as a hallucination on the now-false "registry never rejects" premise. Rewritten to require all three conjuncts (run-success AND registry-advance AND moderation-clear) and to zero-score treating a published-but-unmoderated version as confirmed; context line updated to match. Weights unchanged (sum 100, non-uniform); no moderation literal leaks into `task.md`.

## 0.3.49 — 2026-06-03

### Rules

- **tile.json → plugin.json conceptual migration + tile.json deletion (#108 Part 1 remainder)** — Completes the migration the Build entry below began. `tile.json` is deleted; `.tessl-plugin/plugin.json` is the sole manifest. The big model shift: in the plugin form the manifest lists rule/skill paths only and carries no per-rule config, so rule scope (`alwaysApply`, `applyTo:`) now lives solely in the rule file frontmatter. `rules/rule-frontmatter.md` drops its "tile.json and Rule File in Agreement" section (no second surface to keep in lockstep) for a new "Frontmatter Is the Only Scope Surface"; the always-on/conditional guidance no longer says "in both surfaces." `rules/skill-authoring.md`'s manifest reference is rewritten to the plugin.json schema — required `name`/`version`/`description`; optional `private`/`skills`/`rules` (dir path or path array)/`repository`/`author`/`homepage`/`license`; no `entrypoint`, `keywords`, `summary`, or `steering`. `rules/context-artifacts.md` (Plugin Structure, Surface Sync → "add/remove the rule/skill path in `rules`/`skills`", Consistency Check), `rules/ci-safety.md` (carve-out mis-scope example), `rules/context-writing-style.md` (auto-loaded-artifacts list), and `README.md` (skill-authoring row) swap `tile.json` → `.tessl-plugin/plugin.json`. The unpublished `docs/tessl-rule-frontmatter.md` change-request draft is reworked off the obsolete `steering.alwaysApply` two-surface premise onto the frontmatter-only model. Deferred to a tracked follow-up (#113): the consumer-facing "tile" → "plugin" noun/terminology sweep across rules, skills, and scripts (~84 instances, much of it legitimate generic Tessl usage requiring per-instance judgment), plus the `tessl tile` → `tessl plugin` command-alias sweep in the release skill/scripts (functional aliases). This PR covers the manifest + scope-model migration only.

### Evals

- **rule-frontmatter-* scenarios migrated to the plugin.json model** — The four `rule-frontmatter-*` scenarios graded the now-removed two-surface convention (tile.json `steering` entry + alwaysApply agreement). All four updated: `inputs/tile.json` fixtures become `inputs/.tessl-plugin/plugin.json` with a `rules` path array; criteria drop "tile.json steering entry" and "alwaysApply agreement between surfaces" and add a check that the agent registers the rule path in `plugin.json` without injecting manifest-level scope (the legacy tile.json model). `rule-frontmatter-tile-rule-agreement` is reconceived from "flip both surfaces in lockstep" to a frontmatter-only universal→conditional conversion (flip `alwaysApply`, add `applyTo`, leave the manifest untouched). Directory names unchanged per the rename-stability clause; every checklist re-summed to 100, non-uniform; fixtures still show the opposite pattern from the expected answer so pattern-matching produces the wrong result.

### Build

- **Publish pipeline → plugin form (groundwork for #108)** — tessl migrated tiles to the plugin manifest: with `.tessl-plugin/plugin.json` present it is authoritative and `tile.json` is ignored at publish (synthesised fresh into the tarball from `plugin.json`). The repo now commits `.tessl-plugin/plugin.json` and the publish workflow lints with `tessl plugin lint` (was `tessl tile lint`). The `tesslio/patch-version-publish` action read, bumped, and committed `tile.json` only — for a migrated tile the bump never reached the published version and the publish re-published the stale version or aborted with `already exists`; fixed in `tesslio/patch-version-publish#10`, which resolves the manifest at runtime (`.tessl-plugin/plugin.json`, falling back to legacy `tile.json`) and is backward-compatible for tile.json-only consumers. `v1` is moved forward onto that fix, so this repo's `uses: tesslio/patch-version-publish@v1` picks it up with no edit. `.tileignore` → `.tesslignore` (the legacy filename is deprecated); coupled prose refs updated in `rules/context-artifacts.md` (`.tesslignore`, `tessl plugin lint`) and `rules/rule-frontmatter.md` (`.tesslignore`d). `tile.json` was kept transitionally in agreement so the rules that still described `tile.json` conventions were not dangling; the conceptual migration and `tile.json` deletion land in the Rules entry below. Still deferred: Part 2's `migrate-to-plugin` skill + evals.

### Evals

- **Floor-model N=3 curation — retire 1 null test, repair 2 negative cases into lift-bearing traps** — Re-ran the full suite ×3 on `claude:glm-5.1`, the floor / publish-default model the `eval-authoring` lift requirement targets (run `019e8775`, 2026-06-02), and averaged per-scenario lift. Under N=3 every scenario is non-negative — the earlier single-run negatives were glm-5.1 variance, not signal (the over-retire trap, `#111`). Three scenarios sat at 0 floor lift; handled per `rules/plugin-evals.md`:
  - **Retired** `rule-frontmatter-always-on-rule` — a redundant positive case (rule-frontmatter keeps `conditional-rule` + `mixed-scope-no-narrow` + `tile-rule-agreement`).
  - **Repaired** the two required negative cases instead of exempting them. Both were 0-lift because their tasks **leaked the answer** (No Bleeding), so baseline scored as high as with-context. De-leaked into real traps where baseline takes the naive wrong action and the tile-aware agent correctly refrains: `eval-curation-clean-suite-no-action` (dropped the "0-lift is acceptable" note + the `case type` column that pre-labeled the refusal scenario as exempt → baseline now retires the 0-lift negative; **+98** floor lift, base 2 → 100) and `install-reviewer-refuses-overwrite` (dropped the "not authorized / teammate / out of contact" framing that telegraphed refusal → baseline now proceeds to overwrite the orphan lock; **+28**, base 71 → 99). Both keep the required negative-case coverage and now demonstrate tile value. Suite: 19 → 18.
- **Curation pass — retire 5 null tests, fix 3 negative-lift scenarios** — A value review of the v0.3.43 full-suite run (claude-opus-4-7, 2026-06-01) found 11 of 21 scenarios contributing ≤4 lift, six of those negative. Motivation: the publish pipeline auto-runs the whole suite (2 variants/scenario) on every version bump per `rules/plugin-evals.md` Persistence, and the org eval quota is 200 solutions/day — scenario count is the only sanctioned cost lever, and pruning is mandatory upkeep. Verification is free: the next publish re-runs the full suite and a regression would block it.
  - **Retired (5 null tests)**, baseline already at/near ceiling with no recoverable lift: `eval-curation-task-leak-fix` (base 98, +2 — baseline diagnoses the cause-#2 task-leak unaided), `eval-curation-criteria-grade-universal-competence` (base 100, −4 — baseline diagnoses cause-#3 unaided), `coverage-gap-identification-and-fill` (base 94, +4 — the task supplies copyable scenario examples, so "follow tile format" needs no tile), `version-bump-reasoning-and-manifest-upda` (base 73, +1 — tests universal semver; the task never engages the release skill's publish-confirmation contract), `install-reviewer-missing-gh-aw` (base 100, −10 — the whole scenario, "missing tool → halt, give the `gh extension install` command, explain why, re-run," is engineering-101 baseline nails; no tile-specific discriminator, the −10 is single-run variance on a ceiling scenario). Coverage retained for every affected skill: eval-authoring keeps `eval-scenario-quality-review-and-repair`; eval-curation keeps `eval-curation-clean-suite-no-action` (+98) and `eval-curation-universal-competence-retire`; release keeps `pr-merge-and-post-merge-cleanup` (+39) and `ci-and-review-status-polling` (+24); install-reviewer keeps `install-reviewer-refuses-overwrite` and `consumer-scaffolds-policy-reviewer` (both fixed below).
  - **Fixed `install-reviewer-refuses-overwrite`** (was −51). The task said "a different, custom review automation" without pinning paths, so the agent invented non-canonical filenames, correctly concluded the skill's overwrite guard (`SKILL.md` Step 2 — fires on any of the four canonical reviewer paths) doesn't fire, and offered coexistence — which the refusal criteria penalized. Rewrote the task to the orphan-lock partial-install case (a compiled lock present at a path the skill installs to, source absent) — exactly the "a lock alone indicates deliberate in-progress configuration → halt" condition the skill prescribes, and where a generic agent is likeliest to diverge ("incomplete, I'll regenerate"). No canonical filename leaked into the task per No Bleeding.
  - **Fixed `consumer-scaffolds-policy-reviewer`** (was −20). The deliverable is `scaffold-plan.md` (a plan), but three criteria graded actual execution side-effects ("files present in the working tree," "stages and commits," "pushes the branch"), so the grader penalized a correct plan for not literally executing while crediting baseline's identical assertions. Realigned those three criteria to grade the plan's described command sequence; weights unchanged (sum 100).
  - **Fixed the cause-#1-vs-#3 boundary in `skills/eval-authoring/LIFT_ANALYSIS.md`** (surfaced by `eval-curation-universal-competence-retire`, −63 — base 93 diagnoses cause-#1→retire, but with-context dropped to 30). The reference file's "The default action is `rewrite-criteria`" line biased even Opus into misclassifying a cause-#1 (coincidence, retire) scenario as cause-#3 (criteria-grade, rewrite). Replaced the default-action framing with an explicit discriminator: ask whether a more-specific tile prescription exists that baseline wouldn't produce — none ⇒ cause #1 ⇒ retire; one the criteria merely glossed ⇒ cause #3 ⇒ rewrite-criteria. The scenario is kept as the regression guard for that boundary.
  - **Kept (not retired) despite low single-run lift** — both are tile-specific with score headroom, so their numbers read as single-run variance, not null tests: `eval-scenario-quality-review-and-repair` (+3 net = +20 on one sub-task, −17 on another, on a bleeding-detection rubric) and `review-feedback-addressing-conventions` (−5 driven entirely by the tile-specific `Fixed in <sha>` reply-format criterion failing once).
  - **NOTE — 4 of the 5 retirements above were reverted by the floor-model correction below.** Only `install-reviewer-missing-gh-aw` stayed retired.

- **Floor-model correction — restore 4 over-retired scenarios; measure lift on the floor model** — The curation above computed lift on `claude-opus-4-7`. Re-measured against the `glm-5.1` floor (the CI publish-run solver and the designated weakest consumer-spectrum target, from the v0.3.44 CI run), four of the five retirements are load-bearing: `coverage-gap-identification-and-fill` opus +4 → **glm +28**, `version-bump-reasoning-and-manifest-upda` opus +1 → **glm +26**, `eval-curation-task-leak-fix` opus +2 → **glm +15**, `eval-curation-criteria-grade-universal-competence` opus −4 → **glm +13**. Lift is model-dependent and often anti-correlated: a strong solver's baseline competence masks context the floor model needs (the mirror case in the same data: `eval-curation-clean-suite-no-action` opus +98 → glm 0; `eval-curation-universal-competence-retire` opus −63 → glm +79). Restored all four; `install-reviewer-missing-gh-aw` (opus −10, glm 0 — null on both) stays retired. The 3 fixes from the curation pass are model-independent design fixes and stand. `skills/eval-authoring/LIFT_ANALYSIS.md` gains a "Measure on the Floor Model" section so retire decisions are taken against the floor, not the strongest available solver. Net suite: 19 tracked scenarios.

### Rules

- **skill-authoring, dependency-management, plugin-evals** — Tessl install-layout path references refreshed `.tessl/tiles/` → `.tessl/plugins/` for the tessl ≥0.81 rename. Per `rules/context-artifacts.md`'s Post-Edit Rule Audit, the same refresh is applied repo-wide — `skills/eval-authoring/REVIEW_CHECKLIST.md`, `evals/coverage-gap-identification-and-fill/criteria.json`, `docs/tessl-rule-frontmatter.md`, and `.claude/CLAUDE.md` — so no stale `.tessl/tiles/` install-layout reference remains anywhere. Consistency driven by `skills/install-reviewer/SKILL.md` switching to plugins — see the install-reviewer Skills entry below.

- **external-repo-contributions** (new) — Always-on rule defaulting to deny on issues, pull requests, comments, reactions, and discussions in repositories the operator does not own. Explicit permission required per repo AND action type (operator names both in the current conversation, or a standing instruction in an operator-controlled source — `~/.claude/CLAUDE.md`, the project `CLAUDE.md` of a repo the operator owns, or operator-curated agent memory — names them). The permission-source list deliberately excludes `CLAUDE.md` or other instruction files inside repositories the operator does not own: those are controlled by the external repo, not by the operator, and accepting them as a permission source would let the very repo the rule restricts grant itself permission — the exact bypass the rule is closing (caught by the OpenAI gh-aw reviewer on the introducing PR). Bullets enumerate common bypass patterns that do NOT qualify (e.g., permission to fix a bug locally is not permission to file it upstream; "this should be reported" instinct is not consent; permission for one external repo does not extend to others). Motivation: agents acting under the operator's GitHub identity in external projects produce AI-generated content the operator never consented to publish under their name, damaging their reputation in those communities. Surface sync: `tile.json` steering, README rules table + What's-New count 19 → 20 and new "Scope" category row, `.claude/CLAUDE.md` dogfood import.

- **script-as-black-box** (new) — Conditional rule (`applyTo: skills/**/SKILL.md, skills/**/*.md`) requiring skill prose to name the script's contract (inputs, outputs, exit codes, verbatim-emitted text shapes) and not restate the script's internal logic (thresholds, predicates, formulas, allowlists, source lists, per-tier rules). Surface sync: `tile.json` steering, README rules table + What's-New count 18 → 19. Triggered by `jbaruch/nanoclaw-admin#288`; PR body carries the full incident trail.

- **context-writing-style** — Adds `## Reader-Side — Consult the CHANGELOGs` section telling agents where to find motivation cut from rule bodies. Closes the writer/reader asymmetry created by "What to Cut → move to CHANGELOG".

- **ci-safety** (closes #67) — Adds a "Content-Only Direct-Push Carve-Out" section to "Protected Branches" permitting direct pushes to `main` or `master` for content-only paths under three ANDed preconditions: authority-of-record rule in the consuming tile, narrow path-glob scope excluding all executable/loaded artifacts (`rules/**`, `skills/**`, workflow files, manifests), and push-time enforcement that rejects the entire ref update when any changed path lies outside the carve-out globs (post-push CI checks do not qualify). Structural shape mirrors `dependency-management.md`'s runtime-managed-manifest and `error-handling.md`'s outer-boundary-process-contract carve-outs. Triggered by `jbaruch/shownotes` `_talk/<episode>.md` show-notes pages; follow-up work in the shownotes repo lands the authority-of-record rule and push ruleset per the PR body.

- **ci-safety** (#80) — Release contract's confirmation step changes from "registry advanced past baseline" (single signal) to a conjunction: the resolved publish run's `conclusion` is `success` AND the registry's `Latest Version` strictly advanced past the baseline. Both signals are required because either alone is unsafe: registry-advance alone races interleaved publishes (an earlier PR's publish run can advance the registry between baseline-capture and the post-merge check while OUR run failed — the queued/in-flight race the issue surfaces); conclusion alone can pass a workflow that exited cleanly without publishing (conditional skip, no-op publish step). The rule's "workflows can succeed without publishing, or fail after publishing" parenthetical is preserved and re-cast: the failure mode `succeed without publishing` is now caught by the registry-advance conjunct; the failure mode `fail after publishing` becomes a loud false-negative (operator checks the registry, sees the publish landed) instead of a silent race — the workflow-design fix is to keep the publish step last. The "If the registry didn't advance" bullet is updated to "If either conjunct fails, the publish is not confirmed" — failure of either conjunct (not just both together) breaks the conjunction. Surfaced by Copilot on PR #76 (out-of-scope per `rules/commit-conventions.md`) — that PR's contract changes (exact-run binding via `gh pr view --json mergeCommit` + `event:push` filter) closed the wrong-run race but the queued/in-flight race remained latent until this PR.

- **Evals — `pr-merge-and-post-merge-cleanup`** (closes #59, #77) — Companion update for the post-merge contract changes that landed across PR #54 (0.3.10, watch-to-completion + registry verify), PR #57 (0.3.11, SHA-bound publish-run binding), and PR #80 (the conjunction rewrite above). The scenario's old "Publish CI verification" criterion (max_score 10) graded the fire-and-forget shape — a `gh run list` / `gh run view` "the workflow was triggered" check — that those three PRs successively replaced; a `merge-and-cleanup.sh` wrapper that only confirmed the publish workflow was enqueued would score full marks on this criterion while bypassing the actual rule. Issue #59 named the post-PR-#54 drift in May 2026; issue #77 re-surfaced the registry-advance piece specifically after PR #76's Copilot pass; one criteria.json rewrite closes both. Replaced with four atomic criteria, each measuring one gate of the new contract: (a) **Pre-merge registry baseline captured** (max_score 8) — script must capture `Latest Version` BEFORE `gh pr merge` (`PRE=$(tessl tile info ...)` or equivalent) so the post-merge check has something to compare against; without the baseline, the agent can't tell "this release published" from "a prior release already shipped this version". (b) **SHA-bound publish-run resolution** (max_score 10) — derives the merge-commit SHA via `gh pr view <N> --json mergeCommit --jq '.mergeCommit.oid'` and filters runs by `.headSha == "<merge-sha>"` AND `.event == "push"` (jq string literals); `gh run list --limit 1`, "latest on main", branch-only filters, and workflow-name-only selectors score zero because each can pick a parallel-merge run or a manual `workflow_dispatch`. (c) **Watch publish run to terminal state** (max_score 8) — `gh run watch <id>` against the specific run id, not a poll-once snapshot or trigger confirmation. (d) **Conjunction check: run-success AND registry-advanced** (max_score 15) — gates final success on BOTH conjuncts and explicitly scores zero for four failure shapes the prior PRs identified: run-success alone (PR #80's race), registry-advance alone (same), expected-version derivation from the merge SHA's manifest (PR #57's race), and any moderation-state hallucination (`rules/ci-safety.md`'s explicit non-rejection statement — the registry never rejects published versions; missing-after-success means CI failed, full stop). Other correctly-aligned criteria reduced proportionally so the checklist sum stays at exactly 100 per `skills/eval-authoring/SKILL.md` Step 5; the four new publish-related gates carry 41 points combined (up from the old single criterion's 10), reflecting that the post-merge gate is the most tile-prescribed part of this scenario — `Pre-merge CI gate` drops 15 → 10, `Merge strategy --merge` and `--ff-only` drop 10 → 8 each, `Pre-merge review gate` drops 10 → 6, `branch -d` drops 8 → 5, `Verifies merge landed on main` drops 8 → 5, `Stale remote-tracking refs pruned` drops 7 → 4, `Final summary` drops 7 → 4, `--delete-branch` drops 5 → 4, `Graceful failure` drops 5 → 3, `No hardcoded` drops 5 → 2. No misaligned criterion's weight increased per the eval-authoring rule. task.md's output-spec line updated from "Confirms the publish/release CI workflow was **triggered**" (which both leaked the technique into the task and encoded the stale fire-and-forget contract) to "Confirms the release was actually published successfully before exiting" — situation-only, no technique prescribed; baseline agents are still free to interpret "published successfully" as `gh run list` (and will score zero on the publish-related criteria), policy-aware agents apply the multi-gate sequence. Bleeding check passed: none of the criteria-specific literals (`baseline`, `headSha`, `Latest Version`, `conjunction`, `gh run watch`, `gh run list`, `PRE=`, `merge_sha`, `moderation`) appear in task.md. Scenario directory name unchanged per the rename-stability clause in `rules/plugin-evals.md`'s Naming section.

### Skills

- **adopt-fork-pr** — `emit_jq_missing` now also writes its diagnostic to stderr (keeping the stdout JSON envelope for stdout-parsing wrappers) per `rules/script-delegation.md` self-error-handling, so stderr-only log watchers surface the missing-jq failure. Addresses the deferred non-blocking point the OpenAI cross-family reviewer raised on PR #105 (the only finding declined there). `tests/test_adopt.sh` gains a direct `emit_jq_missing` stdout-JSON-plus-stderr-diagnostic assertion (20 tests).

- **adopt-fork-pr** (new) — Companion to `install-reviewer` that closes the reviewer's fork-PR coverage gap. The gh-aw reviewer pair is fork-guarded (`review-*.lock.yml` job-level `if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.id == github.repository_id`) because GitHub withholds repo secrets from fork-triggered `pull_request` runs — the reviewer needs `ANTHROPIC_API_KEY`/`OPENAI_API_KEY`/`TESSL_TOKEN` it never receives on a fork run, and an LLM reviewer ingesting attacker-controlled diff/body text while holding secrets is a prompt-injection/exfiltration surface. Net effect: every external contribution gets zero policy review, silently. The skill is the maintainer-invoked, human-confirmed bridge: triggered by "check PR N" / "review PR N" phrasing, it classifies the PR (`gh pr view --json isCrossRepository`), passes same-repo PRs straight through to the existing reviewer, and for fork PRs surfaces the fork-guard limitation and offers (via `AskUserQuestion`) to adopt the branch into the base repo. Adoption is deliberately human-initiated rather than event-driven: the maintainer's decision to pull an external branch in-repo IS the trust gate the fork-guard enforces; automating it on PR-open would rebuild the exact "any stranger's code runs through the secret-bearing reviewer" path the guard closes. `adopt.sh` (deterministic, per `rules/script-delegation.md`) checks out the fork head, pushes it to `adopt/pr-<N>-<slug>` on origin **preserving the contributor's commits unchanged** so authorship and any `Co-authored-by:` Author-Model trailer survive (the adopted same-repo PR then satisfies the `rules/author-model-declaration.md` gate without fabrication), opens the same-repo PR, and comments on the original fork PR linking the adopted one. The original is left OPEN with a pointer comment — the contributor closes it on their own (their call, per `rules/external-repo-contributions.md` the comment lands only on the operator-owned base repo). Idempotent with partial-run recovery, keyed on the adopted branch: branch-on-origin-with-an-open-PR re-emits `already-adopted`; branch-on-origin-without-a-PR (a prior run pushed then died before `gh pr create`) recovers by opening the PR rather than no-opping with an empty `new_pr_url` (the OpenAI cross-family reviewer flagged the original no-op-on-any-existing-branch path on this PR). JSON output `{state, adopted_branch, new_pr_url, original_pr, author}`; exit codes 0/1/2/3 (success / operational failure / usage / not-a-fork). `tests/test_adopt.sh` (19 tests) sources the script behind its `main()` guard and mocks `gh`/`git` as subcommand-dispatching functions: slugify folding/trimming, `extract_author_model_line` present/absent, the three argument-validation paths, not-a-fork (exit 3), non-OPEN (exit 1), `git ls-remote` failure (exit 1, not a silent fresh-adoption), `gh pr list` failure on an existing branch (exit 1, not a false recovery), pointer-comment post failure (exit 1, not a warning), pointer-comment read-probe failure (exit 1, not a false "no comment"), idempotent pointer-comment skip when the link already exists, happy-path JSON-shape, Author-Model body-line carry-over, the branch-with-open-PR `already-adopted` no-op, and the branch-without-PR recovery. Review hardening (OpenAI cross-family reviewer, three rounds): the `SKILL.md` body leads with the sequential preamble and splits the fork-handling into one-action steps (Classify / Same-Repo Passthrough / Surface the Fork / Confirm Adoption / Adopt the Branch); Step 1's `gh pr view --json` set includes `headRepository` for the fork's `<owner>/<repo>` label; the `git ls-remote` and `gh pr list` idempotency probes are captured and die on failure rather than discarding stderr and treating a failure as "branch/PR absent"; `gh pr create`/`gh pr view`/`gh pr checkout`/`git push` no longer redirect stderr to `/dev/null`, so the actionable failure reason surfaces; the jq-missing error envelope is documented in the script's output contract; a body-only `**Author-Model:**` declaration is copied from the original PR into the adopted PR body (the preserved commits reproduce a trailer-based declaration but not a body-only one) so `rules/author-model-declaration.md`'s gate passes before the reviewer runs; the pointer comment on the original PR is idempotent (skipped when the link already exists, re-checked even on the `already-adopted` path) and both its existence-probe read and its post are operational errors on failure rather than warnings or a failed-read-as-"no comment" fall-through, per `rules/script-delegation.md` self-error-handling; and an `EXIT` trap restores the caller's branch if a step fails after `gh pr checkout` leaves the tree on the contributor's branch. Surface sync: `tile.json` skills map, README skills table + What's-New, two eval scenarios (`adopt-fork-pr-adopts-fork-branch` positive, `adopt-fork-pr-same-repo-passthrough` passthrough). Motivated by `jbaruch/blog-writer#6` — a `shelajev/blog-writer` fork PR that would never be reviewed once the reviewer is installed, because the fork-guard skips it regardless of secret configuration.

- **install-reviewer** (closes #103) — `scaffold.sh` now creates-or-merges a `.env.example` documenting the reviewer CI secrets per `rules/no-secrets.md` ("Maintain a `.env.example` … For hosted-CI secrets, include a deep link to the platform's secrets configuration page in the file header"). Before this, the secrets were documented only in the ephemeral PR body (`PR_BODY_TEMPLATE.md` §2), so consumer repos that installed the reviewer had no committed secrets inventory — the OpenAI-family reviewer flagged the gap on `jbaruch/nanoclaw-core#49` (the templates reference `CODEX_API_KEY`, which had no documented placeholder), and the non-determinism of the finding made consumer PRs pass or fail inconsistently across the nanoclaw fleet (admin's identical reviewer-path upgrade was not flagged). Two new helpers: `parse_repo_slug_from_url` is a pure (no-git) parser that extracts `owner/repo` from the three remote-URL forms git emits — SCP-style SSH (`git@github.com:owner/repo.git`), `ssh://` URLs, and HTTPS — each with an optional trailing slash (the `.git/` form is stripped by removing a trailing slash before the `.git` suffix, since the reverse order leaves `.git` attached), bounding the owner segment with the last `/` OR `:` so the SCP host:path separator is handled without a regex; `derive_repo_slug` wraps it over `git remote get-url origin` for the secrets-settings deep link, falling back to a literal `<owner>/<repo>` placeholder when origin is unset (preflight normally guarantees it). `ensure_env_example` treats a secret as present when the file already has a line beginning `KEY=`, preserving consumer content verbatim. Because no-secrets.md requires the deep link "in the file header", the link counts as in-header only when it precedes the first `KEY=` assignment: when the file lacks a header link (no link at all, or one sitting below the body) the whole reviewer block (header comment + any missing `KEY=` lines) is PREPENDED above the consumer's existing entries — not appended below them. When a header link is already present only the missing `KEY=` lines are appended. The header-link check is slug-specific — it looks for THIS repo's `https://github.com/<slug>/settings/secrets/actions` link, so a different repo's link, a link below the body, or no link all trigger the prepend. The placement contract was tightened in three steps after successive cross-family OpenAI reviewer findings on this PR: append-at-bottom → prepend → position-aware (link must precede the first `KEY=`) → slug-specific (link must point at the current repo). Stray below-body or wrong-repo links are left in place since consumer content is never deleted. Merge-safe (consumer vars and any pre-set key values preserved), EOF normalized to a single trailing newline, and a true no-op only when both the keys AND the link are already there. The block lists `CODEX_API_KEY` and `OPENAI_API_KEY` as alternatives (the OpenAI workflow reads `${{ secrets.CODEX_API_KEY || secrets.OPENAI_API_KEY }}`), alongside `ANTHROPIC_API_KEY` and `TESSL_TOKEN`. The `.env.example` write lands after the compile-success rollback boundary (same posture as the `.gitattributes` marker) and is added to `scaffold.sh`'s symlink / non-regular-path writable-paths guard so a symlinked `.env.example` is refused in both modes. Surface sync: `commit.sh` stages seven files instead of six (`.env.example` added to `FILES`); `preflight.sh` guards `.env.example` against dirty/untracked/symlink/tracked-deletion state in BOTH modes so `commit.sh`'s `git add` can't sweep a consumer's unrelated pending `.env.example` content (possibly real secret values) into the reviewer commit: override mode adds `.env.example` to `TARGETS` (the existing dirty-target check), and install mode — which doesn't run that check, relying on the skill's Step 2 existence-refusal for the reviewer files — gets a focused `check_env_example_clean` since `.env.example` legitimately pre-exists. The per-target state logic is factored into a `classify_target_dirty` helper shared by both checks. A never-tracked, not-yet-created `.env.example` has nothing to clobber and is not flagged in either mode; `SKILL.md` Step 4 describes the create-or-merge and Step 5's "six scaffolded files" → "seven"; `PR_BODY_TEMPLATE.md` §2 adds the `CODEX_API_KEY` alternative the reviewer flagged and points to the committed `.env.example`; README skills-table row notes the merged `.env.example`. The `preflight.sh` gap was caught by the cross-family OpenAI policy reviewer on this PR (the staged-artifact set and preflight's clobber-guard set must stay consistent per `rules/context-artifacts.md`). New `tests/test_scaffold_env_example.sh` (14 tests): five cover `parse_repo_slug_from_url` across HTTPS / SCP / `ssh://` / trailing-slash-and-`.git/` / empty-and-single-token-reject, nine cover `ensure_env_example` for fresh-create, merge-preserves-existing-content (no duplicate of a present key), deep-link-prepended-into-header-on-merge (asserts the link line precedes the first variable line), link-present-below-body re-prepended into the header, different-repo's-header-link-not-accepted, existing-file-without-trailing-newline, all-keys-plus-link no-op, deep-link-header backfill (all keys present but link absent), and idempotency across invocations. `tests/test_preflight.sh` adds three `.env.example` cases — override-mode uncommitted-edits, install-mode dirty-flagged, install-mode clean-passes (9 total). The fleet sweep PRs that add a per-repo `.env.example` by hand to satisfy the finding stay valid — re-running the upgraded skill against those repos is a no-op once all four keys are documented.

- **install-reviewer** — Adapt the reviewer templates and operator scripts to tessl 0.81.0's `.tessl/tiles/` → `.tessl/plugins/` install-directory rename. As of 0.81.0, `tessl install jbaruch/coding-policy` writes under `.tessl/plugins/jbaruch/coding-policy/` instead of `.tessl/tiles/...` — the "tile" → "plugin" terminology rename carries through to the on-disk layout. The reviewer prompts hardcoded `/tmp/gh-aw/coding-policy/.tessl/tiles/jbaruch/coding-policy/rules/`, so when `tesslio/setup-tessl@v2`'s `version: latest` advanced 0.80.0 → 0.81.0, every consumer repo's reviewer found that path empty and bailed with `Policy load failed: … is missing or empty` on every PR — a fleet-wide review outage. Surfaced on `jbaruch/nanoclaw-flight-assist#32`: the reviewer reported "Policy loaded: 20 rule files" at 0.80.0, then flipped to "Policy load failed" the moment 0.81.0 became latest mid-session, and a re-run an hour later failed identically (ruling out a transient). Fix: the `review-{openai,anthropic}.md` prompts (every rule-path, skills-path, load-indicator, and failure-message reference), `scaffold.sh` / `preflight.sh` `TEMPLATE_DIR`, `SKILL.md` operator-command launch paths, and `PR_BODY_TEMPLATE.md`'s load-indicator example all switch to `.tessl/plugins/…`. Plugins-only with no `.tessl/tiles/` fallback: the reviewer workflow pins `tesslio/setup-tessl@v2` `version: latest`, so CI always runs ≥0.81 and never sees the legacy layout — and a partial fallback (scripts only, not the `SKILL.md` launch paths) would be internally inconsistent. The dogfood `.github/workflows/review-*.md` are unaffected — they read the PR head's local `rules/` directly, not an installed tile. The same rename is applied to `skills/eval-curation/SKILL.md`'s `compute-lift.py` invocation and `rules/skill-authoring.md`'s tile-mount-path example so the documented convention matches the new layout — the gh-aw reviewer flagged that inconsistency on this PR when `SKILL.md` switched to plugins while the rule still showed tiles.

- **install-reviewer** (closes #100) — Mirrors the `CHANGED_FILES` allowlist guard from `skills/install-reviewer/review-openai.md` into `skills/install-reviewer/review-anthropic.md` so the Anthropic reviewer no longer attempts `create_pull_request_review_comment` calls on paths outside the PR diff. Without the guard, GitHub returns HTTP 422 ("Path could not be resolved") on the off-diff comment and the subsequent `submit_pull_request_review` cascade-fails, silently dropping the verdict (this is exactly what `rules/error-handling.md` "Actionable Messages" and "Graceful Fallback" forbid). Three additions, each verbatim from the OpenAI template: (1) Step 3 builds `CHANGED_FILES` from `gh pr view --json files`, (2) Step 5 validates each `create_pull_request_review_comment` `path` against `CHANGED_FILES` before calling — off-diff findings fold into the review body instead, (3) Guardrails names the closed allowlist and expands "unchanged lines" with the 422 trap rationale. Same triple-fix also mirrored into the dogfood `.github/workflows/review-anthropic.md` so the two reviewer surfaces stay aligned (the `.lock.yml` recompiled via `gh aw compile` and sanitized for trailing whitespace per `skills/install-reviewer/scaffold.sh`'s routine). Surfaced by both Copilot and the gh-aw OpenAI policy reviewer on `intent-integrity-chain/kit#64` — the consumer-side fix can't land without diverging from the upstream tile, so the asymmetry has to be closed here first.

- **release** (#83) — `skills/release/poll-pr-reviews.sh` now passes `--paginate` to both `gh api` calls (`/pulls/<N>/reviews` and `/pulls/<N>/comments`) and slurps the paginated stream through external `jq -s 'add | ...'` instead of relying on the per-page `--jq` filter. The pre-fix call returned only the first 30 entries; the `| last` filter then picked the last review on page 1 — not the actual newest review across all pages — and the Step 5 gate could approve a merge against a stale `COMMENTED` state while a fresh `CHANGES_REQUESTED` sat on page 2. Caught on PR #82 during the merge-state diagnostic review cycle: `poll-pr-reviews.sh` reported `gh_aw.submitted_at: 17:43:28Z` while the live latest was three `COMMENTED` reviews at 17:59 / 18:02 / 18:04; `gh api ... --paginate` returned the truthful latest immediately. The comment count had the same per-page truncation bug — now sums across pages. `per_page=100` (API max) is set explicitly so the request volume stays bounded on PRs with many reviews. The fix REMOVES `--jq` from the calls because `gh`'s `--jq` applies per page (so `[...] | last` would emit one result per page, not one across the stream); the external `jq -s` is mandatory to consolidate. Test mock updated to (a) refuse calls without `--paginate` so a future regression is caught loudly, (b) accept multi-page fixture bodies via `MOCK_REVIEWS_BODY` / `MOCK_COMMENTS_BODY`. Five new tests exercise the pagination path: latest-from-last-page selection, empty-reviews `none` return, cross-page login filtering, comment-count summing across pages, and zero-comments handling.

- **install-reviewer** (#92, closes #79) — `preflight.sh`'s `check_no_dirty_target_edits` (override mode) now also flags targets that are missing from the working tree but still tracked at HEAD. The pre-fix loop iterated only over targets that still existed on disk (`[[ -e "$t" || -L "$t" ]] || continue`), so a consumer who ran `rm <target>` or `git rm <target>` to intentionally remove a reviewer file (and hadn't yet committed the deletion) passed the dirty check silently — `scaffold.sh` then re-created the file from the tile template, clobbering the consumer's removal with no warning. `git diff --quiet --diff-filter=D HEAD -- "$t"` (negated) catches both forms in one exit-code check (working-tree-only deletion via `rm` and staged-deletion via `git rm`). New diagnostic tag `(tracked deletion)` keeps the failure distinguishable from the other three dirty-states the check already covers. Recovery hint in `push_failure` extended to include `restore` (consumers may want `git restore <path>` rather than commit/stash/remove). SKILL.md prose for both modes updated to describe the four states the upgrade refuses to clobber (uncommitted edits, untracked content, symlinks, tracked deletions) — the previous wording said only "uncommitted edits" and drifted from what preflight actually enforces; the file's preamble switched from "Steps are sequential — complete each before moving to the next" to the literal `Process steps in order. Do not skip ahead.` mandated by `rules/skill-authoring.md`. preflight.sh file-header overview re-described override mode to name the four clobber-states (the prior wording said only "no-uncommitted-target-edits"). Four new tests cover `rm`-form deletion, `git rm`-form deletion, multi-target aggregation, and the unmodified-targets sanity case; tests source preflight.sh in a fresh sandbox with the six targets pre-committed to HEAD, relaxing only errexit after source (matching `test_poll_pr_reviews.sh`) so nounset and pipefail still catch the bugs the rest of the suite catches. Surfaced by Copilot on PR #76 (out-of-scope per `rules/commit-conventions.md` "Keep PRs focused" — that PR was a prose-discipline pass, so the script-level fix landed as a separate PR).

- **release** (#80) — New `skills/release/verify-publish-landed.sh` makes the new conjunction contract executable: takes `<workspace> <tile> <pre-baseline> <run-id>` and emits `{"ok": bool, "reason": "...", "run_conclusion": "...", "pre": "...", "current": "..."}`. Exit 0 iff both conjuncts hold; exit 1 names which one failed; exit 2 on argument/external-tool errors. Internally calls `gh run view <id> --json conclusion --jq '.conclusion'` and `tessl tile info <workspace>/<tile>` (captures output first, then greps `Latest Version` / awks last field with a trailing `|| true` so a grep no-match falls through to the explicit `-z` empty-`current` diagnostic instead of dying under `set -e` + `pipefail`). Version comparison parses `major.minor.patch` as integers in pure bash so the contract stays portable across GNU coreutils (Linux CI) and BSD userland (macOS) — `sort -V` is a GNU extension and isn't guaranteed on BSD sort. The `gh run view --jq '.conclusion'` call returns the literal string `null` for in-flight runs; the script treats both `null` AND empty conclusion as "still in flight" and exits 2 with a "run `gh run watch` first" diagnostic — without this guard, a pre-terminal run would surface as `conclusion != "success"` and report "publish failed" against a run that hadn't actually finished. Run-id positive-integer regex is `^[1-9][0-9]*$` (matches `resolve-publish-run.sh`'s `validate_positive_int`, rejects the bare zero a permissive `^[0-9]+$` would accept). Early jq-installed guard hand-rolls a minimal JSON envelope on missing jq so wrappers parsing stdout still see a parseable failure (same pattern as `install-reviewer/preflight.sh`). External-tool failure branches (`gh run view`, `tessl tile info`) carry actionable recovery hints per `rules/error-handling.md` (auth-check, install-check, direct re-invocation commands). `set -euo pipefail` and the standard SKILL/SCRIPTING contract — JSON-producing, self-error-handling on stderr with actionable diagnostics. `SKILL.md` Step 7 references the script in place of the inline `current > PRE` check; the watch step explicitly notes it's a timing precondition for the conjunction script, not the gate. `SCRIPTING.md` Wrapping-Step-7 and `rules/ci-safety.md` Release-contract are trimmed to directives per `rules/context-writing-style.md` (rationale moved here in CHANGELOG). `tests/test_verify_publish_landed.sh` covers 16 paths: happy success+advance, the issue #80 race (failure + interleaved advance), success+no-advance (skipped publish), failure+no-advance, cancelled conclusion, semver-aware advance (0.3.9 → 0.3.10), downgrade rejection, four argument-validation paths (non-numeric, empty pre, wrong arg count, run-id == 0), in-flight `null` / empty conclusion handling, `tessl tile info` parse-miss reporting, an explicit run-under-`set -euo pipefail` regression test, and JSON-shape sanity. Tests mock `gh` and `tessl` as shell functions and call `main` directly inside a subshell so `exit 1`/`exit 2` terminate only the substitution.

- **install-reviewer** (#64) — Both `skills/install-reviewer/review-{openai,anthropic}.md` templates' `description:` block now includes the `https://github.com/<owner>/<repo>/settings/secrets/actions` deep link per `rules/no-secrets.md`'s "For hosted-CI secrets" requirement. Surfaced by the same-family eval cell on `jbaruch/coding-policy-evals#3` while reviewing the new eval-cell forks; the cross-family policy reviewer (which IS these templates running in production) missed it. Same fix mirrored into this repo's dogfood `.github/workflows/review-{openai,anthropic}.md` headers (which previously had no secrets-doc block at all, so this PR adds one with the same deep link); dogfood lock files recompiled and re-sanitized for trailing whitespace per the same scaffold sanitation routine.

- **install-reviewer** (#65) — `scaffold.sh`'s `.gitattributes` append/sanitation logic extracts into an `ensure_gitattributes_marker` helper that runs two-stage EOF normalization whenever the target file exists, regardless of whether the append branch fired. EOF normalization is OUTSIDE the marker-absent branch so a previously-affected consumer (marker present, trailing newline missing) gets repaired by re-running the skill — short-circuiting on marker-present would trap such consumers in the broken state until they hand-edit. Stage one is a bash-only authoritative fix: if the last byte isn't a newline, append one with `printf '\n'` (POSIX, no perl dependency, never fails silently). Stage two is a perl best-effort follow-up — `perl -i -0pe 's/\s*\z/\n/'` (zero-or-more, NOT one-or-more) — that also collapses multiple trailing newlines to one. The `\s*\z` form is mandatory: when EOF is a non-whitespace byte (the reported failure mode), `\s+\z` requires at least one trailing whitespace to match and no-ops, leaving the missing-newline violation in place. The append printf was landing the file without a trailing newline on real consumer installs (cross-family policy reviewer caught it on `jbaruch/coding-policy-evals#1`), violating `rules/code-formatting.md`'s "End files with a single newline" basic. New `tests/test_scaffold_gitattributes.sh` (7 tests) covers fresh consumer, existing file with/without trailing newline, marker-present canonical (no-op), marker-present-missing-newline (repaired, no duplicate marker), and idempotency on both canonical and repaired states. Tests use POSIX `cksum` + `od` for byte assertions; trailing-newline-count uses perl (same dep the scaffold itself optionally uses).

- **release** (#78) — New `skills/release/resolve-publish-run.sh` polls `gh run list` for the merge SHA's publish run on a 2s interval up to a 30s budget, emitting `{"database_id": N}` once the publish workflow is enqueued. The previous Step 7 / SCRIPTING.md flow ran a single `gh run list` immediately after merge; if the publish workflow hadn't been enqueued yet (which can take several seconds), the lookup returned empty and the downstream `gh run watch <run-id>` errored out. Wraps the existing `headSha == $merge_sha` + `event == push` filter so manual `workflow_dispatch` runs sharing the SHA are still excluded. Correctness details: (a) selection happens inside `--jq '[...] | .[0] // empty'` rather than `| head -n 1`, so the pipeline can't be turned into a `pipefail` failure by SIGPIPE-on-head-exit; (b) `--limit 100` is passed explicitly so busy main branches with many recent runs don't push the match off `gh run list`'s default 20-row page; (c) `RESOLVE_PUBLISH_RUN_INTERVAL_SEC` / `RESOLVE_PUBLISH_RUN_BUDGET_SEC` / `RESOLVE_PUBLISH_RUN_LIST_LIMIT` env vars are validated as positive integers up front with named diagnostics, `INTERVAL > BUDGET` is rejected, the per-iteration sleep is capped at `BUDGET - elapsed` so total *sleep* time never exceeds the budget on non-divisible intervals (real wall-clock can still exceed by gh API latency), and the loop bound is `<= BUDGET_SEC` so a run that becomes visible at the boundary itself isn't missed; (d) JSON output uses `printf` instead of `jq`, so the script has no client-side jq dependency (gh's `--jq` is internal to gh); (e) the run-id from gh is regex-validated as numeric before being formatted into the JSON envelope; (f) SKILL.md's `gh run watch` snippet no longer passes `--exit-status`, so a publish-workflow failure can't short-circuit `set -e` wrappers before the authoritative registry-advanced check per `rules/ci-safety.md`. JSON output satisfies `rules/script-delegation.md`'s "JSON-producing" requirement; call sites in SKILL.md Step 7 and SCRIPTING.md "Post-merge verification" extract the ID with `jq -r '.database_id'` before passing to `gh run watch`. New `tests/test_resolve_publish_run.sh` covers immediate-hit / deferred-hit / budget-exhausted / arg-validation / env-var-validation / budget-cap / numeric-validation paths (9 tests) with tempfile-backed gh+sleep mocks.

- **release** — Step 1 (Verify Readiness) gains a pre-push self-audit bullet alongside the existing tests + linter gates: audit the diff against every governing rule or skill whose domain covers the touched paths, grep the diff for the markers each one names, run any local check it prescribes, and recognize the paired cross-family reviewer as a backstop rather than the first read.
- Surfaced by PR #82's eleven push/review cycles for the merge-state diagnostic — most findings were reachable by a pre-push grep against the rules whose domain the diff touched. Skill bullet rather than `alwaysApply: true` rule because the trigger is procedural (the moment before push) and Step 1 already houses the peer pre-push gates.
- **release** — `skills/release/poll-pr-reviews.sh` adds a top-level `merge_state` field (`{status, mergeable}` from `gh pr view --json mergeStateStatus,mergeable`); Step 5 documents the new field and adds a diagnostic bullet for the conflict-blocked workflow-dispatch case. Triggered by `jbaruch/nanoclaw-admin#268` (issue #81): a merge conflict against base meant GitHub couldn't compute `refs/pull/N/merge` and silently skipped `pull_request: [opened, synchronize, reopened, edited]` workflows across three pushes — `ci.status` stayed `none` with `checks: []` while Copilot kept firing on its separate event path, so the agent had no in-poll signal pointing at the cause and burned ~30 min rediscovering it. Surfacing `merge_state` in the same snapshot avoids a second `gh pr view` call during diagnosis and lets the Step 5 loop guidance name the exact JSON path the agent should check; `DIRTY` / `CONFLICTING` means rebase onto current `main`, resolve, force-push.
- **eval-curation** (new) — Sibling skill to `eval-authoring` for pruning, trimming, and shaping an **existing** eval suite. Six-step sequential workflow: run the suite (`tessl eval run .`), pull per-scenario lift via `tessl eval view --json <run-id>` (or a tile's own scoring driver where present, e.g., `compute-lift.py --from-tessl-run-id <UUID>` in `jbaruch/coding-policy-evals`), classify each scenario by lift band per `skills/eval-authoring/LIFT_ANALYSIS.md`, diagnose every weak / no-lift scenario against the three causes from `rules/plugin-evals.md` "Lift, Not Attainment" (universal-competence coincidence → retire; leaked technique → fix the task per No Bleeding, keep the criterion; criteria-grade-universal-competence → rewrite criteria or retire), apply the per-scenario decisions (`git rm -r evals/<dir>` for retires — stages the deletion in the same step, task-edit for leaks, criteria-rewrite + re-weighting for grade-fails), and re-run + verify the curated distribution. The split from `eval-authoring` is by execution mode rather than by content overlap: authoring is the new-suite workflow that starts with `tessl scenario generate .`; curation is the existing-suite workflow that starts with the run-and-measure pass. Both skills share `LIFT_ANALYSIS.md` (lift bands + three-cause diagnosis + negative-case treatment) and `REVIEW_CHECKLIST.md` (No Bleeding / No Leaking rules) by reference — no duplication, single source of truth. Triggered by the `speaker-toolkit` failure mode: 35 evals shipped, half producing near-zero lift; the suite paid Tessl run-cost on every publish for zero added signal, and the agent at the keyboard had `eval-authoring` (a generation workflow) but no dedicated curation workflow to invoke — so the bloat shipped. The new skill plus the matching `plugin-evals` "pruning is mandatory upkeep" rule clause closes that gap.
- **release** — Step 7's "After merge" section now codifies the post-merge duty per `rules/ci-safety.md`'s extended Always Watch CI clause: (1) verify the merge landed on main, (2) **watch the publish workflow to completion** with `gh run watch <run-id> --exit-status` (the previous "check it triggered" wording was fire-and-forget — a silent publish failure would still report success), (3) **verify the new version is live in the registry** with `tessl tile info <workspace>/<tile>` (or the equivalent for non-tile artifacts), (4) report the outcome including registry confirmation. `SCRIPTING.md`'s "Post-merge verification" gate gains the same three-confirmation contract for any `merge-and-cleanup.sh` wrapper. Triggered by repeated cases where the previous "check it triggered" verification scored zero against the actual failure mode — a publish workflow that fails to advance the registry version. If the registry does not show the new version after the publish run returned `success`, that is a CI failure, full stop; the Tessl registry has no moderation queue / hold / rejection state and the rule explicitly forbids inventing one as a plausible explanation.
- **release** — Step 7 now documents the worktree-launched cleanup path alongside the base-checkout one (per the new `rules/agent-worktree-isolation.md`): from inside an additional worktree, the post-merge sequence is `cd` back to base → fast-forward `main` → `git worktree remove <worktree-path>` → `git branch -d <branch>`. Order is mandatory because `git branch -d` refuses to delete a branch checked out in any worktree; reversing produces a "checked out at `<path>`" error and leaves a stranded branch. `SCRIPTING.md`'s Wrapping-Step-7 section gains a corresponding "Worktree-aware teardown" gate so any `merge-and-cleanup.sh` wrapper detects the worktree case (`[ "$(git rev-parse --git-dir)" != "$(git rev-parse --git-common-dir)" ]`) and runs the right teardown sequence instead of surfacing the confusing branch-delete error to the user. Triggered by the surface-composition gap noticed immediately after `agent-worktree-isolation.md` landed on PR #52: the rule mandates worktrees for concurrent agents, but Step 7's cleanup block still assumed the agent was in the base checkout, so following the rule literally produced a broken cleanup.
- **release** — Three quick-fix bundle for Copilot findings on `speaker-toolkit#29`. (a) `#42`: `request-copilot-review.sh`'s `discover_copilot_bot_id` filter matched the bare login `copilot-pull-request-reviewer`, but the GraphQL `Bot.login` is inconsistent — some contexts return the `[bot]`-suffixed form (matching the REST surface), some don't. The filter now accepts either, so the discovery fallback can't silently miss a real Copilot review and run the mutation against an empty actor ID. (b) `#43`: `fetch_pr_node_id`'s `gh api graphql --jq '.data.repository.pullRequest.id'` returned the literal string `null` (with exit code 0) when the PR was missing/invalid; the downstream mutation then ran with `pullRequestId: "null"` and surfaced as a confusing GraphQL error several steps removed from the actual root cause. The function now uses `// empty` to collapse null and refuses with a concrete diagnostic before the mutation can run. (c) `#44`: `GH_AW_DETAILS.md` pinned `claude-opus-4-6` for the Anthropic-side reviewer but the install-reviewer-shipped templates are on `claude-opus-4-7` — the doc was wrong from a consumer's perspective and the dogfood drifted. Replaced the literal model with a pointer to the workflow source's `engine.model` as the single source of truth, so future model bumps land in one place and the doc never goes stale. New `tests/test_request_copilot_review.sh` covers both new code paths (5 tests, all passing): null-PR refusal, real-PR ID return, and bot-login matching with/without/missing the `[bot]` suffix. Tests source the script (the `main()` guard prevents auto-run) and override `gh` with a fixture-driven function so the actual jq filters are exercised end-to-end.
- **install-reviewer** — `preflight.sh` hardened on two fronts. (a) `#40`: jq is required for emitting the script's documented JSON envelope, but the script never checked jq was present — a missing jq produced `jq: command not found` on stderr and no JSON on stdout, leaving the agent with nothing to parse and the install workflow stalling before Step 2 could surface the recovery command. preflight now hand-rolls the missing-jq diagnostic as JSON before any other work, so the failure satisfies the contract the agent depends on (`{"check": "jq-installed", "reason": "...brew install jq...apt install jq..."}`). (b) `#41`: the `branch-not-local` recovery command no longer recommends `git branch -D` (force-delete, destroys unmerged commits silently) — it now recommends `git branch -d` (refuses on unmerged) and offers `git branch -m <branch> <branch>.bak` as a non-destructive alternative; surfacing `-D` to a fresh user running a routine install was a footgun. New `tests/test_preflight.sh` covers the missing-jq path in both install and override modes (asserts JSON validity, exit code, override field, and the OS-specific install hints in the reason).

### Rules

- **plugin-evals** — Naming section's 40-char cap gains a narrow carve-out for rig-shaped scenario directories whose programmatic shape can't fit the default. The original cap is preserved as the default and its driver named (interactive tooling: `tessl eval view`, `tessl scenario generate`), so the obligation stays clear for the common case. The carve-out applies only when four preconditions hold together: (1) the rig's `evals/instructions.json` declares the actual safe length AND names every tessl-eval tool the rig touches end-to-end, (2) the rig bypasses the interactive tools that drive the default cap (custom name-builder, `tessl eval run <path>`, custom scorer), (3) the rig owns naming-collision and truncation-driven scoring drift in its own scorer, (4) the rig's CHANGELOG records the carve-out under a `### Rules` entry. Rename-stability still applies regardless of cap. Triggered by the `jbaruch/coding-policy-evals` rig where the `<rule>-<fixture_type>-<cell>-run-<n>` shape produces names up to 67 chars (`general-bug-percentile-off-by-one-violation-cross-with-policy-run-1`) that are un-compressible without losing the 4-tuple needed for lift bucketing in `compute-lift.py`. The eval cell that surfaced this on `coding-policy-evals#12` cited the cap correctly under the previous wording — the rule itself was missing the legitimate case, matching the structural shape of `error-handling.md`'s outer-boundary carve-out and `dependency-management.md`'s runtime-managed-manifest carve-out. The owner-CHANGELOG-declaration precondition mirrors the closed-loop-carve-out's affirmative-declaration precondition, keeping bypass-cope from accreting (no implicit "we'll declare it later" path).
- **maintainer dogfood** — Adds `/.claude/CLAUDE.md` in this repo with direct `@-`imports of every rule under `rules/`. The maintainer working inside the policy repo now reads (and edits) the live source rules; the previous setup vendored the rules into `~/.tessl/tiles/jbaruch/coding-policy/rules/` via a project-level `tessl.json` at `~`, producing drift between the editor's view and the source — caught when the agent reviewing PR `coding-policy-evals#12` cited a rule the local context didn't have. The vendored copy was at 0.3.18 against the registry's 0.3.26. Companion cleanup removed `~/.tessl/` entirely; consumer projects continue to install the tile normally.

- **Scoping pass** — Six tile-authoring rules flipped from `alwaysApply: true` to `alwaysApply: false` + `applyTo:` (em-dash glob+prose pattern) in **both** the rule file frontmatter and the `tile.json` steering entry, keeping the two surfaces in agreement. Scoped rules: `context-artifacts` (`tile.json, rules/**, skills/**, evals/**, .tileignore, .tesslignore, CHANGELOG.md, README.md`), `context-writing-style` (`rules/**/*.md, skills/**/*.md, README.md, CHANGELOG.md`), `plugin-evals` (`evals/**, skills/**/SKILL.md`), `rule-frontmatter` (`rules/**/*.md, tile.json`), `script-delegation` (`skills/**, scripts/**, skills/**/*.sh, skills/**/*.py, skills/**/SKILL.md`), `skill-authoring` (`skills/**/SKILL.md, skills/**/*.md, tile.json`). Each scoped rule also gains a `description:` field summarizing its sections. Rules that remain universal (`alwaysApply: true`, no `applyTo:`): `agent-worktree-isolation`, `author-model-declaration`, `boy-scout`, `ci-safety`, `code-formatting`, `commit-conventions`, `dependency-management`, `error-handling`, `file-hygiene`, `no-secrets`, `stateful-artifacts`, `testing-standards` — they mix file-bound and broad guidance, and `applyTo:` is exclusionary at the model layer so a too-narrow scope would silently drop the broad bits (`dependency-management`'s "stdlib first" fires when writing code, not only when editing manifests). The `applyTo:` value is read by the agent's model, not by a parser — Tessl preserves source frontmatter at install and the rule is loaded into context regardless of scope; the scope governs whether the model *acts* on the rule for a given task. `context-artifacts.md`'s "Rule Format" bullet updates from "two `alwaysApply` flags" to "scoping with `applyTo:`" to match the model.
- **Evals — `rule-frontmatter-*` scenarios (new)** — Four eval scenarios verify the new rule's lift. Each scenario gives the agent a small Tessl tile fixture and asks for a rule-authoring change; the criteria check tile-prescribed specifics (`alwaysApply` value in both surfaces, presence/absence of `applyTo:`, glob+prose pattern when scope is declared, lockstep agreement between rule file and tile.json). (1) `rule-frontmatter-always-on-rule` — add a universal code-style rule; tile prescribes `alwaysApply: true` in both surfaces and no `applyTo:`. Fixture rules are deliberately conditional so pattern-matching from the fixture produces the wrong answer; with-policy resists the pattern and applies the universal convention. (2) `rule-frontmatter-conditional-rule` — add a dependency-pinning rule that only fires on manifests; tile prescribes `alwaysApply: false` in both surfaces plus `applyTo:` combining glob patterns with a natural-language clause after an em dash. (3) `rule-frontmatter-mixed-scope-no-narrow` — add a "stdlib first + pin if you add" rule whose two halves fire in different contexts (code vs manifest); tile prescribes keeping mixed-scope rules universal because path-scoping would silently drop one half. Baseline failure mode this scenario tests for: copying the conditional pattern from a sibling rule. (4) `rule-frontmatter-tile-rule-agreement` — convert an existing universal rule to a conditional one; tile prescribes flipping `alwaysApply` in BOTH the rule file and tile.json in lockstep. Common baseline failure: flipping only one surface and leaving them inconsistent. All four directory names ≤ 40 chars per `plugin-evals.md` "Naming" section; all criteria checklists sum to 100.
- **rule-frontmatter** (new) — Codifies the frontmatter convention for rule files: Tessl preserves the source rule file byte-identical during `tessl install`, so whatever frontmatter the author writes reaches every consuming agent's context. The agent's model reads the frontmatter and applies the rule according to its declared scope. Verified by Tessl engineering with a TypeScript-scoped rule that fired on TS files and skipped Python. Two patterns: **always-on rules** set `alwaysApply: true` in both `tile.json` and the rule file frontmatter, omit `applyTo:`; **conditional rules** set `alwaysApply: false` in both surfaces and add `applyTo:` with the required em-dash glob+prose pattern (e.g., `applyTo: "skills/**/SKILL.md — when authoring or modifying skills"`). Accepted field-name aliases for `applyTo:` are `globs:` and `paths:` (same em-dash glob+prose value, different field name — pick one per file). `description:` is a rule summary, never a scoping substitute for `applyTo:`. `context-artifacts.md`'s "Rule Format" section now points at the new rule for frontmatter conventions. Companion `docs/tessl-rule-frontmatter.md` draft (not part of the published tile; `.tileignore` excludes `docs/`; intended as a contribution to `docs.tessl.io`) maps the convention onto specific Tessl docs pages with proposed text for `reference/configuration.md`, `create/developing-tiles-locally.md`, `reference/custom-agent-setup.md`, `reference/glossary.md`, and `introduction-to-tessl/how-tessl-works.md`. Triggered by an audit observing that all existing rules in this tile use only `alwaysApply: true` even where prescriptions are clearly file-bound; the new rule documents the convention so authors (in this tile and consumers) can apply scope deliberately.
- **context-writing-style** (new) — Adds a meta-rule codifying prose discipline for context artifacts that load into agent context: rules (always-on per `tile.json` steering), skills (loaded on invocation), READMEs (loaded on tile fetch). CHANGELOG entries fall outside the strict cuts — they are the archive that carries the motivation and incident references stripped from rules. Three sections: What to Cut (logical connectives, intensifiers, reinforcement, meta-justification, reference incidents, worked examples, cross-rule rationale shoulder-taps, CHANGELOG pointers, anti-patterns buried in prose), What to Keep (file paths, command names, flag literals, version numbers, dates, constraint-bearing descriptors, carve-out preconditions), and Structure (atomic bullets, numbered preconditions for carve-outs, parenthetical limit, active voice). The rule resolves a directive contradiction in `context-artifacts.md`'s "Rules Are Prose" section, which previously required "include both why and how" — the source clause that licensed accreted incident references, reasoning prose, and worked-example case-walking across the long-running rules. Triggered by an audit showing the 8 longest rules carried ~38% of total rule tokens in narrative bloat, compounding across every load: each chat turn, every paired PR review run across consumer repos, every `tessl skill review` CI step. Surface sync: `tile.json` steering map adds `context-writing-style`; README rules table adds the new row; `context-artifacts.md` "Rules Are Prose" section drops "include both why and how" and "Rule Format" defers section count / line budget / prose discipline to the new rule. Same audit added two new clauses to `context-artifacts.md`: a "CHANGELOG Hygiene" section codifying release-time Unreleased-entry consolidation (group related entries, collapse redundant detail, dedup across PRs that reworked the same rule) and a Consistency Check extension that extends the no-duplicated-bullets rule to rule↔skill duplication (don't repeat long command literals or contract statements between the rule and the skill or its script).
- **Prose pass** — Companion application of the new `context-writing-style.md` to the existing long rules and skills per `context-artifacts.md` Post-Edit Rule Audit obligation. Per-rule word-count cuts: plugin-evals (1992 → 1101, -45%), context-artifacts (1263 → 773, -39%), skill-authoring (827 → 668, -19%), agent-worktree-isolation (579 → 345, -40%), ci-safety (533 → 452, -15%), dependency-management (500 → 351, -30%), error-handling (451 → 288, -36%), author-model-declaration (441 → 255, -42%). Net rule load: 8579 → 6479 words (24% cut, including the 253-word new rule). The cuts strip reference-incident paragraphs, "the reasoning is structural..." meta-justification, worked examples tracing the rule through specific cases, parenthetical clarification chains, and run-on `; AND ...; AND ...` carve-out sentences — replacing them with numbered preconditions per the new rule's Carve-Outs guidance. Companion pass on the four shipped skills (release, install-reviewer, eval-authoring, eval-curation) tightens the same patterns in SKILL.md prose. The narrative content the rules previously carried (reference incidents, motivation, "why this carve-out exists") now lives only in the CHANGELOG entries that introduced each rule — agents reading the rules get the directive; readers wondering "why?" get `git blame` and the CHANGELOG as archive, not ongoing context.
- **plugin-evals** — New "Naming" section codifies the conventions eval scenario directory names should follow: kebab-case, skill-prefix when testing a specific skill (`<skill>-<descriptor>`, e.g., `install-reviewer-refuses-overwrite`) or behavior-direct when cross-cutting (`pr-merge-and-post-merge-cleanup`), descriptor names the behavior not the implementation, **40-char hard cap** on the directory name, and **no rename once a scenario has been run through `tessl eval run`** because the per-scenario lift history identifies scenarios by directory name. Rig conventions (programmatic shapes like `<rule>-<fixture_type>-<cell>-run-<n>` in `jbaruch/coding-policy-evals`) get an explicit carve-out for the shape but still inherit the 40-char cap + rename-stability clauses. The 40-char cap is set AT the observed safe threshold: a scenario authored as `version-bump-reasoning-and-manifest-update` (43 chars) was committed at `version-bump-reasoning-and-manifest-upda` (40 chars) when some tooling surface in the tessl-eval pipeline silently truncated it. Existing scenarios predating this rule are grandfathered by the rule's own rename-stability clause (renaming would break lift continuity); the conventions apply prospectively to new scenarios. Specifically named grandfathered names that exceed the new 40-char cap and would not be authorable under the new rule: `eval-curation-criteria-grade-universal-competence` (49 chars, shipped in PR #74) and `eval-curation-universal-competence-retire` (41 chars, same PR). Separately, `version-bump-reasoning-and-manifest-upda` is at-the-cap (40 chars exactly) and would still be authorable under the new rule — it appears here as the truncation artifact that anchors the cap's rationale (intended `version-bump-reasoning-and-manifest-update` at 43 chars; on-disk name is the truncated form), not as an over-cap grandfathered case. The Post-Edit Rule Audit obligation is satisfied without renaming for the same continuity reason.
- **plugin-evals** — "Lift, Not Attainment" section gains a new bullet that makes **pruning mandatory upkeep, not optional cleanup**: a suite where a non-trivial fraction of scenarios consistently produce near-zero lift across runs is bloated, and bloat is a bug to fix before the next publish — not noise to keep paying for. The section previously *described* the three-cause diagnosis ("Coincidence with universal competence" / "Task leaked the technique" / "Criteria grade universal competence") but did not *compel* retirement of the scenarios the diagnosis identified — readers could land on "diagnose, then ship anyway", which is the exact failure pattern we observed in `speaker-toolkit` (35 evals shipped, half producing near-zero lift; suite paid Tessl run-cost on every publish for zero added signal). The new bullet names the obligation, points at `skills/eval-curation/SKILL.md` for the workflow, sets the bar as per-scenario lift contribution rather than raw scenario count (10-scenario healthy beats 35-scenario bloated), and records the failure mode in the rule prose so future readers see the concrete reason the obligation exists. Structurally parallel to `plugin-evals.md`'s closed-loop carve-out reasoning: evals are an instrument, and an instrument producing no signal is pure cost — the carve-out exempts tiles with no consumption from running evals at all; this clause forbids tiles WITH consumption from running scenarios that produce no signal. Both close arms of the same "measurements that never become signal" failure mode from different sides.
- **context-artifacts** — Mandatory Review section now states the changed-only scope of the CI review loop affirmatively, and the Surface Sync "CI workflow" bullet is updated so it stops contradicting that scope: `tessl skill review` runs as a single changed-skills loop (one workflow step that iterates over `git diff --name-only <prev-sha>..HEAD -- 'skills/'` results), not as static per-skill CI steps. The implementation landed in PR #69 (`perf(publish): review only skills that changed`), but the rule wording stayed ambiguous and internally inconsistent — "every skill change must pass review" was interpretable as changed-only, while two companion bullets ("add a `tessl skill review … skills/<name>` step to the CI workflow" + Surface Sync's "add/remove the `tessl skill review` step for each skill") still described per-skill static integration that's incompatible with the diff-driven loop. New wording: (a) Mandatory Review describes the action-based integration as the canonical form, names the `git diff` filter, points at `.github/actions/skill-review/action.yml` as the reference implementation (consumed via `uses: jbaruch/coding-policy/.github/actions/skill-review@<ref>` from consumer repos), and explicitly notes that adding a skill requires no CI YAML edit because the loop picks new paths up via the diff; (b) Surface Sync's "CI workflow" bullet now says no edit is needed when the canonical loop is in use, and only bespoke workflows need to confirm their path glob covers the change. The fallback behavior is documented precisely against the shipped action: the loop reviews every skill only when the diff base is absent (manual `workflow_dispatch`, initial push, all-zeros sentinel SHA); when the base is set but unreachable in the local clone the action hard-fails rather than silently degrading — both "review all on unreachable base" and "no changes on unreachable base" would mask a missing review under the changed-only contract, so the action refuses to do either. Reasoning: `tessl skill review` is LLM-backed and credit-consuming; running it against unchanged content reproduces the prior rubric output and adds zero signal — the same structural argument as `plugin-evals.md`'s persistence-runs reasoning, applied to skill review instead of eval execution. Triggered by direct owner feedback that the rule never reflected the changed-only implementation that's been running in CI since PR #69 merged on 2026-05-09.
- **plugin-evals** — Persistence section simplified: dropped the "AND on a recurring cadence" requirement, leaving Tessl-publish-time eval execution as the single persistence point. The Tessl publish pipeline (`tessl tile publish` / `tesslio/patch-version-publish`) already runs the eval suite automatically, so a tile-repo CI step or a scheduled workflow re-running `tessl eval run` was duplicate cost producing the same numbers the maintainer already sees at publish time. "Regressions block the release" is preserved and explicitly relocated to the Tessl-publish layer ("Regressions block the publish") — a parallel CI step is now affirmatively forbidden by the rule because it could mask the publish-layer failure. The carve-out paragraph's quoted reference to the Persistence section is updated to match the new wording so the rule remains internally consistent. No CI workflow changes ride along: no shipped `coding-policy` workflow ever ran `tessl eval run` (`publish.yml` runs `tessl tile lint` + `tessl skill review --threshold 85` + `tesslio/patch-version-publish@v1`; the publish action is where Tessl-side eval execution kicks in), and the 4-way reviewer matrix in `jbaruch/coding-policy-evals` is a separate measurement rig with manual Phase 2 `tessl eval run` invocation — not a tile's own eval suite — so it's unaffected by this rule edit. The same structural argument the closed-loop carve-out merged on 2026-05-10 made about silent recurring runs ("recurring runs were silent on the silent-success regressions they nominally watched for") applies universally, not just to fully-automated tiles: if the maintainer already reads publish-time eval output, a cadence run produces no additional signal; if the maintainer doesn't, the cadence run is generating measurements that become no signal anywhere — the same zero-decisions failure mode the carve-out was authored against. Triggered by direct owner direction during the `coding-policy-evals#6` plumbing session: once `compute-lift.py --from-tessl-run-id <UUID>` was wired to consume Tessl-side eval output directly, the question "do we also need CI to re-run them?" had the obvious answer no — Tessl-publish is already the canonical execution point and any additional cadence is duplicate cost.
- **plugin-evals** — Coverage section gains a "Narrow exception for closed-loop automated systems with no human eval-result consumption" carve-out, and the existing Coverage clause is restated from "every skill with decisional logic ships eval cases — no exceptions" to "every skill with decisional logic ships eval cases, subject only to the closed-loop carve-out below" so the rule has one coherent obligation-and-exception boundary instead of the contradiction the prior wording would create. The structural reasoning: evals are an instrument, not a deliverable; they produce measurements that only become signal when a human (or downstream automated gate) reads them and acts. A tile whose entire CI/release/operations loop is fully automated end-to-end, with no eyeballs ever landing on `tessl eval run` output, is generating measurements that never become signal — every eval run is pure cost (Tessl budget, scenario-authoring effort, fixture maintenance) producing zero decisions, and the suite has no theory of how it would catch a regression (real regression manifests → eval flags it → report posts to a workflow log nobody reads → regression ships anyway). Owner-approved 2026-05-09 with `jbaruch/nanoclaw-*` named as the reference example: fully-automated agent loop, no human eval-result review, multi-month observation period confirmed the predicted failure mode (40-scenario suite running on non-gating daily cadence, scenarios were not catching real regressions, several retired for ~zero lift, recurring runs silent on the silent-success regressions they nominally watched for). The condition is narrow and affirmative: a tile must demonstrate no-human-review to claim it; "we don't currently look at the results, but we plan to" does NOT qualify — intent without follow-through is bypass-cope dressed as future-work, the exact framing other rules in this tile (`boy-scout.md`, `context-artifacts.md`'s "Disagreeing With the Reviewer") were authored to close. Tiles that DO have human review — including `coding-policy` itself, where the maintainer reads scenario lift on every publish — are NOT exempt. Pairs with the matching `nanoclaw-admin` PR removing the `evals/` directory and `evals.yml` workflow, which the `review-openai` policy reviewer (correctly) blocked under the previous unconditional rule. Structurally parallel to `dependency-management.md`'s "Narrow exception for runtime-managed manifests" and `error-handling.md`'s "Narrow exception for outer-boundary process contracts" — name, condition, scope, owner approval, reference example.
- **boy-scout** (new) — Adds the Boy Scout Rule as an always-on steering rule: leave the codebase in better shape than you found it; "pre-existing" is not a valid concept; if you see something wrong while doing something else, fix it. The rule's purpose is to close the gate-evasion source — most CI bypass attempts and review-gate evasions ("not my regression", "pre-existing", "out of scope for this PR") share the "I didn't break it so I don't have to fix it" framing; the existing forbid-the-bypass rules (`ci-safety: Never Skip Tests`, `context-artifacts: Disagreeing With the Reviewer`) play whack-a-mole on the exits, this rule rules out the framing. Reconciliation with `commit-conventions: Keep PRs focused`: focus governs the SHAPE of the bundle (one logical change), boy-scout governs whether the agent walks away from problems noticed (no); in-scope cleanups bundle into the current PR, out-of-scope discoveries open follow-up PRs/issues with a reference from the current one — walking away with no record is the failure mode the rule prevents. Triggered by a recurring agent failure pattern observed when publishing skill updates: the `tessl skill review --threshold 85` gate fires on a sibling skill the agent didn't author, and the agent's recommendation flips into one of two forbidden bypass paths ("publish manually from this machine" or "lower the threshold in CI") with the justification that the failure is pre-existing. Both bypass paths are already forbidden; this rule names the underlying reasoning so the policy reviewer can flag the framing in PR bodies and review feedback before the bypass even gets proposed.
- **ci-safety** + **release** (skill + scripting) — Strikes a hallucination this very tile previously planted: the "moderation hold" / "moderation queue" / "moderation rejection" framing in the post-merge registry-verification clauses. **The Tessl registry never rejects a published version.** A failed moderation flag (`moderationPassed: false`) means the version still lands and is fetchable but won't surface in `tessl search`; a security finding means `tessl install` requires an override flag to install — neither is rejection, neither prevents the version from being in the registry. Agents (this one included) kept inventing rejection as a plausible-sounding explanation whenever a registry check failed, instead of reaching the correct diagnosis: "the publish workflow failed; go look at the run." All three live surfaces (`rules/ci-safety.md` "Always Watch CI" extension bullet, `skills/release/SKILL.md` Step 7 third sub-bullet, `skills/release/SCRIPTING.md` Post-merge verification gate) now state the precise capability set explicitly so the hallucination has nowhere to land: registry never rejects, moderation flag affects searchability only, security finding affects install-flag requirements only, registry-not-showing-version-after-CI-success = CI failed. The two prior Unreleased CHANGELOG entries that planted the same wording are also corrected to keep the rule and the changelog from disagreeing. Triggered by direct user feedback after the agent in this conversation kept reaching for the moderation-rejection explanation as a hedge clause across multiple PR cycles — concrete example the user cited: `jbaruch/nanoclaw-trusted 0.1.56` flagged `moderationPassed: false`, with the agent claiming "the registry rejected it" when the bump had in fact landed in both git and the registry; only its searchability was affected.
- **error-handling** — Adds a narrow "outer-boundary process contracts" carve-out to "Specific Exceptions" (#56). Without it, the rule's "no catch-all + let unexpected propagate" clauses produce review-cycle deadlock for precheck scripts and other process boundaries where the caller treats non-zero exit / invalid stdout as a silent-failure signal — letting a programming-bug exception propagate there silently disables the scheduled task, the exact failure mode of the 2026-04-16 → 2026-04-18 outage that `heartbeat-precheck.py` was hardened against. Triggered by the deadlock on `nanoclaw-admin#149` (and predecessor `#139`, merged via override): under the previous 2-bullet rule, the reviewer was correct on both flip-flops — `except Exception:` IS a catch-all, AND a 12-class enumerated `except` list IS broad programming-error trapping; the rule itself was missing the legitimate case. The carve-out matches the structural discipline of `dependency-management.md`'s runtime-managed-manifest exception: name, condition, permitted action with explicit non-permitted form (`except Exception:` only — never `except BaseException:`, which would catch `KeyboardInterrupt`/`SystemExit` and make processes resistant to Ctrl-C / `sys.exit()`), three ANDed preconditions (the literal grep token `outer-boundary-process-contract` always present on the catch line or the comment immediately above it so a single tile-wide grep audits every instance regardless of language, additionally accompanied by the language-native catch-all suppressor on the catch line itself where a linter requires one — Python/Ruff: `# noqa: BLE001` must be inline with the `except Exception:` to suppress BLE001 (placing it on the comment above does not work); rationale comment naming silent-failure shape + contract-keeping emit + propagation-would-break-contract reason; outermost-boundary-only), a "every other catch still specific" reset, and a reference incident. The mandatory `outer-boundary-process-contract` token (always present, in any language, alongside whatever native suppressor the linter requires) makes future instances auditable across consumer codebases via one grep pattern — a plain catch-all is still a violation; only the token-marked form is sanctioned.
- **context-artifacts** — Adds a "Disagreeing With the Reviewer" section codifying the protocol when a skill fails the `tessl skill review --threshold 85` gate. Anti-patterns explicitly forbidden: lowering `--threshold 85` to make the skill pass (the threshold-specific prohibition this section adds); generic CI bypass via local publish, `[skip ci]`, or disabling the review step is already forbidden under `rules/ci-safety.md`'s "Never Skip Tests" — both shift "fix the skill" into "hide the failure" and rot the gate. Correct response when the agent disagrees with the reviewer's conclusions: `tessl skill review --optimize <skill>` run locally after backing up `SKILL.md` and any reference files. The optimized output is a hypothesis, not the answer — the reviewer's judge is not a subject-matter expert and routinely strips load-bearing context the local agent (with project context) knows is necessary. Diff against the backup, keep the genuinely-improving moves (tighter triggers, less prose, better `Skill()` typing), reject the over-aggressive cuts, then re-run the review and iterate. The goal is to learn the reviewer's optimization patterns and re-apply them with subject-matter expertise the judge lacks. Triggered by repeated cases where review-gate failures pushed the agent toward bypass suggestions instead of toward the local-optimize-and-curate workflow that actually fixes the skill. Section also explicitly forbids the "I'll run `--optimize` and ship because the score improved" pattern, which has the same gate-rotting effect as dropping the threshold but disguised as a fix — the score reflects what the rubric measures, not the content the optimizer stripped that the rubric doesn't penalize but the skill's author/local agent knows is load-bearing. Use the optimizer as a diagnostic signal (it surfaces *what kinds* of issues exist — actionability, progressive-disclosure, redundancy) and curate the fixes manually with judgment; never ship its applied output verbatim.
- **ci-safety** — "Always Watch CI" section gains a release-extension bullet: for plugin/tile/package releases, the watch duty extends past merge through the publish workflow AND into registry verification. A merged PR with a green PR-CI but a failed publish workflow is not "shipped"; a green publish workflow with a registry that doesn't have the new version is also a CI failure (the `success` was reported but the registry side didn't actually advance). Either way the diagnosis is "look at the publish run" — the rule explicitly forbids inventing moderation queues, holds, or rejection states (the Tessl registry has none of these), which agents have repeatedly hallucinated as plausible-sounding failure modes. Pairs with the matching Step 7 update in `skills/release/SKILL.md` and the strengthened "Post-merge verification" gate in `skills/release/SCRIPTING.md`.
- **agent-worktree-isolation** — Cleanup section gains a release-skill composition bullet spelling out the post-merge sequence for worktree-launched flows: `cd` back to base, fast-forward `main`, `git worktree remove`, then `git branch -d`. Order is mandatory because `git branch -d` refuses to delete a branch checked out in any worktree. Pairs with the matching Step 7 update in `skills/release/SKILL.md` and the new "Worktree-aware teardown" gate in `skills/release/SCRIPTING.md` — landed in the same PR so the three surfaces stay aligned.
- **agent-worktree-isolation** (new) — Mandates `git worktree`-based isolation any time a task that mutates the repo may run concurrently with another agent on the same checkout. Read-only inspection on the shared checkout is permitted; the moment the task crosses into `Edit`/`Write`/side-effecting `Bash`, the agent must operate in a worktree (the Agent tool's `isolation: "worktree"` parameter is the canonical mechanism for spawned subagents; `git worktree add` covers human-launched parallel sessions). Triggered by repeated cases of multiple concurrent agents racing on the same checkout — branch state, index, working tree, and stash all have last-write-wins semantics, so the agent that "loses" a race produces garbage and reports success. The rule also covers cleanup (`git worktree remove`, never `rm -rf`) and the single-reader exception that evaporates the moment the inspection turns into "let me just fix this one thing." No existing skill in this tile uses `Agent()`-based parallelism on the shared checkout, so no skill changes ride along; the rule applies prospectively to consumer flows where multiple agents share a repo.
- **skill-authoring** — Script References section now distinguishes path-form by audience (#50): repo-relative (`skills/<name>/<file>.<ext>`) for rule prose, documentation, and skill cross-references; the path that resolves at the **invocation site** for step bodies the agent executes — repo-relative when the skill runs from a clone of this repo (e.g., `skills/release/poll-pr-reviews.sh`), the consumer's tile-mount path when the skill runs inside a consumer (e.g., `.tessl/tiles/jbaruch/coding-policy/skills/install-reviewer/preflight.sh`, or whichever absolute path the consumer's runtime documents — `/home/node/.claude/...` is common for hosted-runner mounts). Triggered by `nanoclaw-admin#99`'s script-delegation cleanup epic, where both gh-aw and Copilot reviewers flagged the runtime-absolute paths that the nanoclaw fleet (35+ existing invocations) and nanoclaw's own onboarding doc (`copilot-instructions.md`) treat as the convention; neither reading was wrong, the rule itself was the inconsistency. The previous "Reference scripts with full paths: `scripts/foo.sh`" example was also a third convention distinct from both camps and matched no shipped skill in this tile or the nanoclaw fleet — no skill in coding-policy or nanoclaw places scripts in a `scripts/` subdir; they sit at `skills/<name>/<file>.<ext>` or `<name>/scripts/<file>.<ext>` depending on the tile. Both shipped skills in this tile already exemplified the two-context split before the rule was clarified: `install-reviewer/SKILL.md` uses `.tessl/tiles/...` mount paths because it runs inside a consumer repo, while `release/SKILL.md` uses `skills/release/...` repo-relative paths because it runs from a clone of this repo. The rule update also adds a "don't mix conventions inside one SKILL.md" bullet so authors can't half-migrate a file. No skill changes ride along — the rule now describes what both skills already do; unblocking `nanoclaw-admin#99` is a downstream consequence (the existing 35+ invocations are conformant under the new wording).
- **skill-authoring** — Step Structure clarifies that the "Each step is one action — if a step has an 'and', split it" bullet is a **title heuristic**, not a body-atomization mandate (#39). A step's body MAY list sub-tasks or supporting bullets that all serve one cohesive action; the split signal is two verbs in the H2 title (e.g., "Build and Deploy"), not multiple bullets in the body. Triggered by `jbaruch/speaker-toolkit#27`'s vault-ingress review cycle: across three rounds, the OpenAI reviewer kept finding "another bullet that should have been a step," landing on an 11-step proposal for what is logically a 4-phase batch workflow. The strict body-atomization reading would also retroactively reject `install-reviewer/SKILL.md` Step 4 (scaffold.sh runs cp + compile + .gitattributes update as one atomic operation) and similar steps elsewhere in this tile — a tell that the body-atomization reading isn't the intended rule. Audit confirmed no existing skill in this tile has a step title that combines verbs with "and"/`&`, so no skill changes ride along.
- **skill-authoring** — Title and Preamble + Step Structure sections now explicitly distinguish **sequential workflows** (the default — release / deploy / migrate, "Process steps in order. Do not skip ahead.") from **action routers** (group-management, scheduler-config, anything where the agent picks one of several alternatives by user intent — "This skill is an action router — pick the step that matches the user's intent and execute only that step. Do not run other steps; do not parallelize."). Triggered by the `manage-groups` skill in `nanoclaw-admin#74`: the OpenAI policy reviewer rejected an action-router preamble as conflicting with the rule's literal "force sequential execution" wording, even though the skill is genuinely a router (find / add / remove / list / change-trigger / configure-allowlist / configure-mounts are alternatives, not phases). The previous rule conflated "non-parallelization" with "in-order processing"; routers need the first guarantee but cannot honour the second. Both modes share the same flat-numbered `## Step N` heading format — the difference is the preamble's execution-mode declaration, not the heading shape. Action-router preambles must also be exhaustive on chaining (e.g. "Step 7 is typically chained after Step 3"); the default in routers remains "execute only the chosen step and finish".
- **dependency-management** — "No Vendoring" section gains a Tessl-specific bullet: never commit tile content (e.g., `.tessl/tiles/<workspace>/<tile>/...`) into the consumer repo; install via `tessl install` at runtime. Two failure modes have surfaced on real consumer installs and motivated the explicit prohibition: (1) a vendored copy silently drifts from the registry-published version, so consumers run stale rules without noticing — the rule that's actually loaded into the agent's context is whatever was last committed, not what the registry's `Latest Version` reports; (2) a workspace-local `.tessl/` is wiped by `actions/checkout`'s default `clean: true` in CI runs (the same gap that drove `install-reviewer`'s 0.2.x switch from `--global` install to `/tmp/gh-aw/coding-policy/`), so any reviewer or agent that expects to read tile content out of the workspace finds an empty directory and either fails loudly or — worse — silently runs with no policy loaded. The general "Don't copy library source code into the repo" bullet already covered this in spirit, but consumers were treating tiles as policy artifacts rather than dependencies and committing the install output anyway; the explicit restatement closes that gap. The bullet also points at the install-reviewer changelog entries as the reference incidents so future readers can find the concrete failure modes without re-deriving them.
- **dependency-management** — Adds a narrow exception clause to the Pinning section for runtime-managed manifests: when a tool rewrites a manifest in-place at runtime AND the resolved-version state is gitignored, pin-or-lock produces silent drift between git and the running deployment, so the manifest may use a floating-but-explicit specifier (e.g. `"version": "latest"`) and skip the lock file. The exception is gated on three per-manifest preconditions: (1) the consuming project documents an authority-of-record rule in its own tile naming the carve-out and listing every covered manifest, (2) a deploy-time check fails the deployment if any disallowed specifier (literal pin, range, tag, or anything other than the permitted floating specifier) appears in that manifest, (3) each covered manifest is named explicitly in the authority-of-record rule. Multiple named manifests are permitted iff each independently satisfies all three preconditions; the carve-out doesn't widen to "any manifest" and doesn't apply transitively. Triggered by two NanoClaw incidents: `tessl-workspace/tessl.json` accumulated a 22-day silent drift on 2026-04-27 because `tessl update` rewrites the manifest in-place; the same `tessl update` invocation also rewrites the project-root `tessl.json` (a separate manifest `tessl install` consumes to populate gitignored `.tessl/tiles/` for `@.tessl/RULES.md` resolution at agent runtime), which accumulated silent `vendored`-mode + pin drift on 2026-05-03. Both manifests are now covered by the same authority-of-record rule (`nanoclaw-host: tessl-version-floating`) with one combined `scripts/deploy.sh` walk-and-verify check. The deploy-time gate's specifier-rejection scope was broadened from "literal pin" to "any disallowed specifier" — rejecting only literal pins lets a non-literal pinned/ranged value slip through while still violating the carve-out's intent. Surfaced by the gh-aw OpenAI policy reviewer's `CHANGES_REQUESTED` on `jbaruch/nanoclaw#485` against the unbroadened single-manifest wording, plus Copilot review on closed PR #61 against the deploy-time gate's specifier scope. Companion PRs: `jbaruch/nanoclaw#485` (flips project-root `tessl.json` to `mode: "managed"` + `version: "latest"`, broadens `scripts/deploy.sh` step 3b to walk every `tessl.json`); `jbaruch/nanoclaw-host#18` (broadens authority-of-record rule to cover both manifests).

## 0.3.0

Second formal minor since 0.1.0 — `install-reviewer` gains an in-place upgrade path (`--override`) so consumers no longer have to manually `git rm` four files (or hand-backport fixes, defeating the managed-scaffold contract) every time a fix lands in this changelog. The skill description's "Use when..." clause now lists upgrade trigger phrases (`upgrade`, `update`, `refresh`, `pull latest reviewer templates`, `override`); each of the five scripts (`preflight.sh`, `branch.sh`, `scaffold.sh`, `commit.sh`, `push.sh`) accepts `--override` and emits an `override: bool` field in its JSON output. Step 3's branch-selection logic moved out of SKILL.md prose into a new `branch.sh` script per `rules/script-delegation.md` (deterministic operations belong in scripts, not embedded decision trees the agent reproduces) and `rules/skill-authoring.md` (flat one-action steps, no sub-step bullets).

### Skills

- **install-reviewer** — Added `--override` flag for upgrading existing scaffolded reviewer files in place (#35). Without `--override`, the skill runs in install mode (current behavior; Step 2 refuses if any of the four target files exist). With `--override`, the skill switches to upgrade mode: a different feature branch (`feat/upgrade-coding-policy-review`), a different commit message and PR title (`ci(review): upgrade ...`), preflight skips the branch-clear checks (the upgrade branch may legitimately already exist from a prior in-flight upgrade attempt) and instead refuses if any of the four target files have uncommitted local edits — mirrors how `git pull` refuses to overwrite a dirty working tree, forces the consumer to commit/stash before the scaffold replaces their files; scaffold snapshots ALL four target files (sources + locks) before the cp/compile and restores them on compile failure (extending the existing `actions-lock.json` snapshot+rollback contract); commit/push pick the upgrade branch automatically. Each of the five scripts (`preflight.sh`, `branch.sh`, `scaffold.sh`, `commit.sh`, `push.sh`) accepts `--override` as a positional argument (parsing is order-independent across `$@`) and emits an `override: bool` field in its JSON output so callers can verify the mode they're running under. Step 3's branch-selection logic moved out of SKILL.md prose into the new `branch.sh` script per `rules/script-delegation.md` (deterministic operations belong in scripts, not embedded decision trees the agent reproduces) and `rules/skill-authoring.md` (flat one-action steps, no sub-step bullets) — Step 3 now invokes `branch.sh` and the install-vs-upgrade branch dispatch lives in one place. SKILL.md adds a "two modes" preamble plus per-step bash blocks showing both invocations; the description's "Use when..." clause now lists upgrade trigger phrases (`upgrade`, `update`, `refresh`, `pull latest reviewer templates`, `override`). Fixes the unblocking pain consumers had on every prior fix in this changelog: today the only options were manually `git rm`-ing four files, hand-backporting fixes (defeating the managed-scaffold contract), or accumulating drift.
- **install-reviewer** — OpenAI reviewer prompt now treats the PR's changed-files list as a closed allowlist for inline-comment paths (#32). Codex was hallucinating inline-comment paths outside the PR diff (e.g., posting on `tile.json:3` when the diff only touched `.github/workflows/*`); GitHub rejected the resulting `create_pull_request_review_comment` calls with HTTP 422 ("Path could not be resolved"), which cascade-failed the subsequent `submit_pull_request_review` so the substantive verdict never landed on the consumer's PR. The workflow's safe-outputs handler caught the API error gracefully — masking the failure as a green run with no review posted. Hard fix: Step 3 now explicitly extracts `CHANGED_FILES` from `gh pr view --json files`; Step 5 instructs the agent to validate `path` against `CHANGED_FILES` before each `create_pull_request_review_comment` call (drop the inline comment and fold the finding into the review body if the path doesn't match); Guardrails restate the constraint with the concrete failure mode (422 → cascade-fail). Same change mirrored in the dogfood `.github/workflows/review-openai.md`. Anthropic-side reviewer not affected (Claude wasn't observed hallucinating paths), but the same `CHANGED_FILES` allowlist could be added defensively if the failure mode ever appears there.
- **install-reviewer** — Three quick-fix bundle for issues surfaced from real consumer installs (#30, #33, #34). (a) `scaffold.sh` post-compile sanitization extended: in addition to stripping per-line trailing whitespace (already there), it now collapses trailing whitespace at EOF (`\n\n` → `\n`) via `perl -i -0pe 's/\s+\z/\n/'` so consumer working trees don't ship lock files with the blank-EOF-line drift gh-aw's compiler emits — both forms violate `rules/code-formatting.md`'s "Basics" and were causing reviewer noise on consumer PRs (#30). (b) `engine.env` now coalesces `CODEX_API_KEY` and `OPENAI_API_KEY`: `OPENAI_API_KEY: ${{ secrets.CODEX_API_KEY || secrets.OPENAI_API_KEY }}`. gh-aw's compiled validation step accepts either name, but the Codex CLI / API-proxy path only reads `OPENAI_API_KEY` at runtime; without the fallback, a consumer who set `CODEX_API_KEY` only would pass validation and then fail with empty-credential after burning setup time (#33). (c) The `network.allowed` block's comment in `review-openai.md` no longer claims the allowlist gates `tessl install`. The install runs in the workflow's `steps:` on the runner host, BEFORE the awf sandbox starts; the allowlist applies to agent-runtime traffic only (gh / MCP / API calls + Codex UI/telemetry). Pure doc fix; the host list itself is unchanged (#34). All three fixes mirrored into the dogfood `.github/workflows/review-{openai,anthropic}.md` where applicable; lock files recompiled and sanitized.
- **release** — Reframed Copilot from a temporary trial that retires "once gh-aw is validated" to a deliberate second reviewer kept indefinitely. The two have complementary lenses: gh-aw enforces `rules/*.md` compliance (its prompt loads each rule file and checks the diff against it), while Copilot reads for doc accuracy, cross-step consistency, and ambiguity that no rule file specifically targets. PR #29's review cycle made the case in production: Copilot caught outdated model IDs, unpinned install commands below floor, Step 5/Step 7 contradictions, vacuous-pass loopholes in the merge gate, and `pull_request` trigger ambiguity — none of which were rule violations gh-aw could flag. SKILL.md description, Step 4 phrasing, and Step 6 re-run language updated; the "Trial — keep Copilot in parallel" framing replaced with "Also request Copilot." `GH_AW_DETAILS.md` updated to explain the dual-lens design and drop the "retires once gh-aw is validated" language. Step 7's merge gate is unchanged — it already required `state != none` for both reviewers.
- **install-reviewer** — Reviewer setup now installs the tile under `/tmp/gh-aw/coding-policy/` instead of `~/.tessl/` (`--global`). Diagnosed against a real consumer install on 0.2.2: the OpenAI reviewer was failing every PR with `Policy load failed: $HOME/.tessl/tiles/jbaruch/coding-policy/rules/ is missing or empty`. Two constraints have to hold simultaneously — `actions/checkout`'s default `clean: true` rules out a workspace-local install, and the awf firewall sandbox the agent runs inside doesn't mount `${HOME}`, which rules out `--global`. Switching the install root to `/tmp/gh-aw/coding-policy/` satisfies both: it's gh-aw's canonical runtime directory (where it also keeps its own prompt and logs, so the awf sandbox is already wired to read from there), and it lives outside the workspace so `actions/checkout`'s clean leaves it alone. Implementation moved from `pre-steps:` to top-level `steps:` so the install runs AFTER gh-aw's `Create gh-aw temp directory` step (which creates `/tmp/gh-aw/`); pre-steps were too early. The agent prompt now hardcodes `/tmp/gh-aw/coding-policy/.tessl/tiles/jbaruch/coding-policy/...` — `${{ runner.temp }}` isn't on gh-aw's safe-expressions list for prompt bodies. The fix supersedes the 0.2.0 #23 fix that introduced `--global` — that change closed the actions/checkout-wipe gap but accidentally moved the install outside the awf sandbox's mount set.
- **install-reviewer** — `.mcp.json` neutralization landed (#15). The Anthropic reviewer template now sets `engine.args: ["--strict-mcp-config"]`, which tells Claude Code to load only the MCP servers gh-aw injects via its own `--mcp-config` and ignore any project-local `.mcp.json` the consumer ships. The previous workaround (preflight `root-mcp-json-present` warning + asking the consumer to gitignore the file) is removed: the warning is gone from `preflight.sh`, the related "Known limitation" paragraph in `review-anthropic.md` is replaced with a one-line note pointing at the fix, and the SKILL.md descriptions of the warning are dropped. Requires gh-aw >= v0.71.0 (where `engine.args` was added) and Claude Code CLI >= 2.1.x (where `--strict-mcp-config` was added); the gh-aw floor is already enforced by preflight from #22. The same flag is mirrored into this repo's dogfood `.github/workflows/review-anthropic.md` so the dogfood reviewer is robust against a future committed `.mcp.json`. Lock files recompiled.
- **install-reviewer** — Four consumer-install bugs fixed (#21–#24). (a) `review-openai.md` `network.allowed` now lists the gh-aw ecosystem identifiers `github` (covers github.com / codeload / raw / objects) and `threat-detection` (covers api.github.com), plus `chatgpt.com` and `ab.chatgpt.com` for the Codex engine's UI/telemetry hosts — Codex's `defaults` allowlist excludes all of these, which was blocking `tessl install` on the OpenAI cross-family review for every claude-authored PR (#21). The same allowlist plus the `edited` trigger were also applied to this repo's own dogfood reviewers in `.github/workflows/review-{openai,anthropic}.md`, with lock files recompiled on gh-aw v0.71.0 — without that, every dogfood PR review would keep hitting the same firewall noise. (b) Pre-steps now run `tessl install jbaruch/coding-policy --global --yes` so the tile installs to `~/.tessl/` (outside the workspace) instead of `.tessl/` (inside) — gh-aw runs pre-steps before `actions/checkout`, whose default `clean: true` was wiping the workspace-relative install before the agent could read it (#23). The Step 2 prompt path is now `$HOME/.tessl/tiles/jbaruch/coding-policy/rules/`. (c) `pull_request: types` now includes `edited` so the gate re-fires when a contributor adds a missing `**Author-Model:**` line via PR-body edit instead of forcing an empty commit (#24.1). (d) Step 1's "Otherwise" fallthrough now spells out all three branches (both paired families present, neither paired family present, only the other paired family present) — the neither-family case was previously implicit and read as an oversight. (e) Step 5 violation-citation form changed from `rules/<name>.md` (a non-resolving consumer-repo path) to `` `jbaruch/coding-policy: <rule-name>` `` so PR-comment readers see a tile-prefixed name instead of a broken link (#24.3). (f) `preflight.sh` now checks `gh aw --version` >= v0.71.0 with a concrete pin command (`gh extension remove gh-aw && gh extension install github/gh-aw --pin v0.71.0`) — github/gh-aw marks every release from v0.69.0 onward as prerelease, so `gh extension install github/gh-aw` installs v0.68.3 (the latest stable), which compiles lock files with the deprecated Claude SDK `bypassPermissions` flag (#22). The Anthropic workflow's `.mcp.json` known-limitation comment dropped its specific gh-aw version reference now that the floor moves with consumer installs.

### Rule tightenings

- **author-model-declaration** — Mixed Authorship section now covers the neither-paired-family case (e.g., `gemini-2.5`, custom model names, `human`-only) explicitly: both paired reviewers run because both are cross-family relative to the author, and the duplicate review is accepted noise rather than picking one reviewer arbitrarily and silently halving coverage. The wording calls out that this is deliberate, not an oversight in the gate logic.

## 0.2.0

First formal minor since 0.1.0 — promotes everything accumulated across the 0.1.x patch line into a tagged release, plus eval-driven skill tuning that lifted the with-context aggregate from 93 to 98 (lift +17 → +22). Per-scenario wins from the tuning pass: Coverage Gap 74 → 100 (+26), Review Response 85 → 97 (+12), Copilot via GraphQL 89 → 97 (+8), PR Merge Cleanup verified at 99 (held). Three negative-lift / low-with-context regressions in v0.1.20 were diagnosed against the run-019dbffe artifacts and fixed at the skill layer rather than by softening criteria.

### Skill tuning (driven by eval log analysis)

- **eval-authoring** — Step 7 (Fill Coverage Gaps) now spells out the `criteria.json` wrapper format (`{context, type: "weighted_checklist", checklist: [...]}`) with a code example, requires `max_score` weights to sum to exactly 100, and warns against mirroring plain-array seed fixtures from the test repo under evaluation. Step 7 also makes the Steps-4–5 review/fix loop explicit for newly-authored scenarios — the prior "Repeat Steps 4–6" got skipped on self-authored content. Step 5 (Fix Issues) now states that a misaligned, leaking, or otherwise unsalvageable criterion must be **removed and the remaining criteria reweighted to sum to 100** — bumping a bad criterion's weight to keep the math tidy makes the scenario worse, not better. Eliminated the Coverage Gap regression (74 → 100) and the Quality Audit "increase the misaligned criterion's weight" failure mode.
- **install-reviewer** — Step 1 (Preflight) now states the gh-aw extension's canonical owner in SKILL.md prose (`gh extension install github/gh-aw`, with an explicit note that the extension lives under the `github` org, not the tile owner). Previously this string lived only inside `preflight.sh`'s failure message, where the agent overlooked it and substituted `jbaruch/gh-aw` from tile-owner pattern-matching.
- **release** — Step 2 (Create PR) and Step 7 (Merge + Cleanup) each gained a "When wrapping this step in a reusable script" paragraph: scripts that automate PR creation must enforce the tile's readiness gate (tests + linter), construct/validate the conventional-commits title format, and preserve the exact `**Author-Model:**` line (with the bold-marker literal); scripts that automate merge must enforce the same pre-merge gates the agent does (CI green, every bot review `APPROVED` or non-blocking `COMMENTED`, no unresolved threads), use `git branch -d` (never `-D`) for the local-branch delete, and verify the publish workflow fired before printing the final summary. Step 6 (Address Feedback) reinforces the prescribed reply literals: accept replies must begin with the literal phrase `Fixed in <sha>` (`Done` / `Resolved` / `Accepted and fixed` do NOT satisfy); decline replies must begin with `Declining — <reason>` using a U+2014 em dash followed by a space (NOT a period, hyphen, or en dash). Eliminated the Copilot-via-GraphQL low-with-context regression and the PR Merge Cleanup mid-iteration regression; recovered Review Response Guide to ~100.

### Rules

- **author-model-declaration** — Every PR declares its author model via a `**Author-Model:**` line in the body (preferred) or a `Co-authored-by:` git trailer (fallback). Defines model families (anthropic, openai, google), plus a special `human` value that maps to no family, and mixed-authorship semantics so the paired reviewers can pick a cross-family reviewer and dodge self-review bias. Missing declaration blocks the PR via early `REQUEST_CHANGES` before the diff is read.
- **stateful-artifacts** — Cross-invocation JSON state files a skill writes and reads between runs. Every artifact has a documented schema, a single owner skill, a `schema_version` field, and a writer/reader contract. Artifacts are hints, not authority — verify against the live source before acting on a recalled value. Migrations happen under the owner skill, not as a side effect of unrelated runs.

### Rule tightenings

- **skill-authoring** — Document `user-invocable: false` as an optional frontmatter field for background-knowledge skills the runtime loads as context but the user should never invoke directly.
- **script-delegation** — Spell out the precheck-gating JSON contract with `wake_agent` as a boolean and `data` as an object (e.g., `{"wake_agent": true, "data": {}}` on the last line), the zero-token-cost rationale for no-op runs, and the single-fetch gate-and-payload pattern.
- **context-artifacts** — Enumerate the review rubric: frontmatter validity, sequential-execution preamble, flat step numbering, typed `Skill()` calls, silence-rule compliance, channel-appropriate formatting.
- **plugin-evals** — Reframe around the load-bearing shape: **task describes the SITUATION** (no technique), **criteria check whether the output matches the specific manner this tile prescribes** (flag choices, format literals, sequences, chosen identifiers). That conformance IS the tile's contribution — checking for it measures tile value, not testing reading. Bleeding is narrowed to strictly one case: a criterion literal appearing verbatim in the task description. Fix bleeding at the task (strip the leaked literal), keep the criterion. No-Leaking is narrowed to tile-internal implementation details only (action names, `.tessl/tiles/...` paths, tile-only identifiers); tile-prescribed conventions and invented literals are allowed — a competent engineer without the tile would not produce those specific choices, which is precisely why they measure tile value. Adds a "Lift, Not Attainment" section with a three-cause diagnosis for near-zero lift (coincidence with universal competence / task leaked the technique / criteria grade engineering-101 instead of tile-specific manner) — so "low lift = retire" is no longer the reflex; high-lift scenarios that test specific tile choices must be kept, not softened toward "testing reasoning". Includes the v0.1.1 → v0.1.19 regression diagnosis (lift halved from 26 → 13 absolute points) as the motivating data. The earlier in-session iterations that said tile-invented conventions were leaking or that criteria satisfied by grepping the skill files were "testing reading" were wrong and are superseded by this wording.

### Skills

- **install-reviewer** — Scaffold the paired gh-aw PR policy reviewers (OpenAI Codex + Anthropic Claude Code) into a consumer repo. Ships `review-openai.md` and `review-anthropic.md` as packaged templates; the skill copies both into the consumer's `.github/workflows/`, compiles atomically with `gh aw compile`, commits all six artifacts, and opens a PR. Each workflow runs `tessl install jbaruch/coding-policy` as a pre-step, then self-gates on the PR's `Author-Model:` declaration: the same-family reviewer emits a one-line "skipping: self-review-bias" COMMENT and exits; the cross-family reviewer reviews. Each reviewer's verdict now begins with a one-line load indicator (`"Policy loaded: N rule files from .tessl/tiles/jbaruch/coding-policy/rules/ (installed tile)."`), so a passing review visibly confirms `tessl install` reached the runtime. A missing or empty installed-tile path causes an early `REQUEST_CHANGES` with a diagnostic rather than a silent zero-context review. Preflight now emits a `warnings` array alongside `failures`; a committed root-level `.mcp.json` raises a `root-mcp-json-present` advisory (the Anthropic reviewer can't launch sandbox-unreachable MCP servers like `tessl mcp start`), and the install-reviewer PR body surfaces every warning verbatim in an "Action required before merge" section so the consumer acts before the first workflow run fails. The skill deliberately does not auto-modify the consumer's `.mcp.json` or `.gitignore` — the consumer decides. Three secrets required: `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `TESSL_TOKEN`. Clean-verdict reviews use `event: COMMENT` with a pass body (GitHub rejects `APPROVE` from `github-actions[bot]` with HTTP 422).
- **release** — Step 2 PR body template now includes a mandatory `**Author-Model:**` line per `rules/author-model-declaration.md`; Step 4 updated to describe the paired reviewers and their self-gating behavior.

### Evals

- **install-reviewer** — 3 scenarios graded against the paired-workflow layout: 1 positive (`consumer-scaffolds-policy-reviewer`) covering the happy path through preflight → branch → copy both templates → compile → commit all six artifacts → PR with the three required secrets (`OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, `TESSL_TOKEN`) and the cross-family rationale, and 2 negative (`install-reviewer-refuses-overwrite`, `install-reviewer-missing-gh-aw`) covering the skill's two decisional guards.
- **Retired zero-lift scenarios** — removed `merge-with-failing-ci-rejection` and `release-on-main-branch-rejection`. Apr 18 eval run on v0.1.1 showed both scored 100/100 baseline, i.e. zero lift: the criteria grade responsible-engineer judgement (don't merge red CI, don't push to main) that any competent baseline LLM produces without this tile. Keeping them inflates the attainment average without measuring tile value.
- **Reduced task-level hints** — rewrote `coverage-gap-identification-and-fill` and `eval-scenario-quality-review-and-repair` to strip task text that enumerated the tile-specific techniques or defect taxonomy. Baseline agents can no longer pattern-match their way to the criteria from the task alone; the remaining scoring concentrates on tile-specific behaviour (correct weighted_checklist format, no bleeding in authored scenarios, negative-case coverage, discovering defects without being handed a taxonomy).
- **Restored tile-prescribed-conformance criteria on five scenarios** — after the corrected plugin-evals rule (see above) distinguished bleeding from conformance, restored specific-convention checks that were over-removed earlier. Each scenario now grades whether the output matches the specific manner the tile prescribes, NOT whether the agent can reason its way to some equivalent. Changes: (a) `pr-merge-and-post-merge-cleanup` — criteria now check `gh pr merge --merge --delete-branch`, `git pull --ff-only`, `git branch -d` (safe delete, not `-D`), `git remote prune origin`; (b) `review-feedback-addressing-conventions` — criteria check the tile's prescribed reply formats (`Fixed in <sha>` for accepts, `Declining — <reason>` for declines), with substance checks as complements; (c) `ci-and-review-status-polling` — criteria check the tile's prescribed mechanisms (`gh pr checks <N> --json name,bucket` with a caller polling loop, `gh api .../pulls/<N>/reviews`, `gh api .../pulls/<N>/comments`) with an explicit "NOT `/issues/<N>/comments`" check for the GitHub API trap; (d) `copilot-review-via-graphql` — criteria restore the tile's prescribed hot-path-plus-fallback bot-ID pattern (pinned `BOT_kgDOCnlnWA` with dynamic-discovery fallback) rather than the earlier "don't depend on hardcoded IDs" flip, which had inverted the tile's actual choice; (e) `version-bump-reasoning-and-manifest-upda` — task stripped of the "CI auto-bumps patch" leak (was teaching the agent the tile's policy inside the task text) so the agent must now apply that policy from the tile; criteria check the tile-prescribed CI delegation for patch, specific semver targets, and risk-based release sequencing. All rubrics still sum to 100.

### Skill updates

- **eval-authoring** — Step 4 (Review) and `REVIEW_CHECKLIST.md` rewritten around the new task/criteria shape: is the task a SITUATION (no technique leak) and do the criteria grade the specific manner the tile prescribes? Leaking narrowed to tile-internal implementation details only; tile-prescribed conventions and invented literals are explicitly allowed. Step 9 (Analyze Results) centres on lift (`with_context - baseline`) rather than attainment, with a three-cause diagnosis for near-zero lift: (1) coincidence with universal competence; (2) task leaked the technique — fix the task, not the criterion; (3) criteria grade engineering-101 rather than tile-specific manner — rewrite to grade the specific prescription, not retire. High-lift scenarios testing specific tile choices must be kept, not softened toward "testing reasoning" that baseline already does.

## 0.1.3

Add context artifact authoring rules and eval-authoring skill. Self-audited the tile against its own rules and iterated until 99% eval average. (Originally drafted as `## 0.2.0` in this changelog but actually shipped as patch 0.1.3 by the auto-bumper; relabelled in the 0.2.0 release to make the version timeline consistent with the registry — patches 0.1.4 through 0.1.20 fall between this entry and the new 0.2.0 above.)

### Rules

- **context-artifacts** — Plugin structure, rule format spec, lifecycle guarantees, mandatory review (with iteration guidance), mandatory evals, surface sync checklist, consistency checks
- **skill-authoring** — SKILL.md structure, frontmatter, step numbering with descriptive titles, typed calls, tile.json manifest reference
- **script-delegation** — Deterministic → script, reasoning → LLM, the regex trap, script requirements (references file-hygiene baseline)
- **plugin-evals** — No bleeding (with concrete example), no leaking (public API distinction), persistent eval coverage, negative case guidance, fixture hygiene

### Skills

- **eval-authoring** — 10-step workflow: generate, review, fix, fill coverage gaps, run evals, analyze scores, iterate. Review checklist extracted to separate file for progressive disclosure

### Changed

- **release** skill: headings aligned to `## Step N — Title` format, sequential preamble added, GraphQL queries extracted to `COPILOT_REVIEW_GRAPHQL.md`
- CI pipeline reviews both skills before publish
- Removed `docs/index.md` — all documentation consolidated in README.md entrypoint
- 9 eval scenarios: 5 positive (release), 2 negative (release), 1 quality audit (eval-authoring), 1 coverage gap analysis (eval-authoring)

## 0.1.0

Initial release.

### Rules

- **commit-conventions** — Imperative mood, ~50 char subject, body = "why", one logical change per commit, PR hygiene
- **testing-standards** — Outcome-based assertions, deterministic tests, no binary fixtures, test independence
- **error-handling** — Specific exceptions, actionable messages, graceful fallback, structured logging
- **dependency-management** — Stdlib-first, pinned versions, lock files, separate test/dev groups
- **file-hygiene** — Proper .gitignore, no generated files committed, standalone scripts, exit codes
- **ci-safety** — Never modify CI without asking, never skip tests, branch naming conventions
- **no-secrets** — No credentials in code, env vars or secrets manager, pre-commit scanning
- **code-formatting** — Use project's formatter, don't mix formatting with functional changes

### Skills

- **release** — Structured PR creation, Copilot review, merge + cleanup workflow
